# 你好口令与三角度播报（2026-09-16）

用户要求替换启动口令：规范化后的 final 以“你好”开头即启动云台，例如你好、你好 OpenVela、你好小维；单独启动/openvela不再触发。停止与否定仍优先，只在识别 completed 且无歧义后执行。

启动播报：OpenVela 云台已启动。您可以说开始测肤。
开始测肤/皮肤检测：进入原生姿态检测，播报开始测肤，请正对镜头。
正脸保存：正脸拍照完成；然后提示向自己的左侧转头。
左脸保存：左侧脸拍照完成；然后提示向自己的右侧转头。
右脸保存：右侧脸拍照完成；随后播报三视图已暂存，真实上传受理后才播报已提交。

拍摄阶段来自原生照片存储 epoch/done（0→1→3→7），不能用提示音或界面框代替拍摄成功。沿用原生阶段 worker、缓存 TTS；补充正常各阶段、重复状态和跳跃补报回归。实时拍摄到声音的延迟仍待真人验收，不能把缓存命中等同零延迟。语音10 dB、短音24 dB保持不变。

Python20项通过；原生口令/照片阶段O0和O2通过。独立 hello-keyword-20260916 构建通过，2807928字节，SHA256 a9ddaae467447079d0426b29da81b2114be0a6fdfb4193ff3a6c4ca69540cd29。保护同步只涉及intent和prompt表两文件；未覆盖未知SDK差异。Windows网关PID56256已预热26条/2181120字节，聊天关闭，证据gateway.log。

当前板子已软件重启并进入Fastboot；Ubuntu仅识别QinHeng串口，等待用户接入USB download gadget。候选尚未加载，之前Wi-Fi在线快照已失效。窗口3418已关闭释放串口。下一步tools/load_hello_keyword.py --load；启动后部署新版界面（--connect，不使用--resume-camera），再App配网和测试。无eMMC/STM32刷写或远程提交。


加载完成覆盖：hello-keyword已RAM启动，固件SHA a9ddaae467447079d0426b29da81b2114be0a6fdfb4193ff3a6c4ca69540cd29；单包19585144字节1.939秒，整包CRC576b3abb、固件CRCc4f2f621、四段无线CRC通过，见startup.log。usb.json是USB传输阶段快照，其中ram_crc_verified/firmware_started=false不代表后续校验失败；后续以startup.log与deployed-state.json为准。BLE registered1、Wi-Fi shared start ret0。新版窗口PID11934，仅初始化Host与视频链路，未启动采集、录音或运动。用户回复已联网后板端新鲜回包仍wifi0/ipv4_ready0；蓝牙ATT及PROV命令有回包，但本轮联网尚未验收。Ubuntu仅QinHeng，等待App重新提交Wi-Fi及openvela link probe接回。请勿沿用旧IP在线状态。


最新覆盖：本次已实查radio_generation1/wifi1/ipv4_ready1，视频OTG1209:0001已接入；当前相机尚未启动、frames0、busyfalse、heldtrue。下一步窗口确认0/0后相机待命，再开始语音整机测试；不是网络或USB阻塞。证据network-confirmed.json。


右侧捕获阻塞实查：console-175358.log中epoch2正脸q5882和左脸q6667存储成功，q8718开始lost1，持续st6(VF_TARGET_CHANGED)/v0/done3直到q9471。不是证明右侧角度不达标；k7_photo.c目标锁定丢失后已有部分图片则不自动重置，避免混入不连续目标。触发可能为多脸、缺失超过350ms或框重叠低于0.30；本轮精确触发分支尚无区分日志，不归因于用户或背景人员。后续日志已有lost0，说明出现新测肤会话，不代表旧组三图完成。UI源码已增加角度无效不显示0°、匹配帧POSEQ lost1明确提示整组重拍；12项测试通过。当前正在实时测试，未重启/替换运行窗口，该UI修复尚未部署，板端失锁恢复策略未修改。
