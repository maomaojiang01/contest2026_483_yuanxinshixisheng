# Wi-Fi 上电复验：2026-09-10

新版 wifi-arp-20260909 已在 K7 RAM 启动，源代码和构建哈希检查、板端完整 CRC 校验通过。SHA256：`9dc19c97c2d2aa5f49bc3bca36d04f026c25c096ec3894605ba7ac4d7dc4ce5d`。

## 实际结果

- Windows BLE 加密连接，设备报告 connect:true。
- id9100 非法短密码返回 invalid_password（预期负例）。
- id9101 正确凭据提交后约 3.6 秒收到 wifi_connected，IP 10.3.0.214；id9102 状态一致。
- 首次访问前无网关 ARP 缓存。网关 10.3.0.1 的 Ping 5发5收，首包成功；RTT 39–231 ms。
- ARP 参数 250 ms/5次已读回；统计从0变为请求发送1、回复接收2。A-MSDU完成1组、交付2子帧、拒绝0。
- BLE与Wi-Fi保持60秒，id9103仍返回相同IP；随后测试程序主动断开BLE，Wi-Fi继续在线，配网服务注册保留。

本轮未复现旧首包失败，支持继续验证ARP等待调整；单次5包不能证明根因已完全消除。没有完成新版本长测、Android/iOS实测、DNS/互联网、WPA3、自动重连或断电持久启动验收。

## 上电和配对说明

上电后实际运行9月4日基础NuttX，其help无k7radio；旧RAM固件没有断电保留。本轮只重新加载RAM，未刷eMMC/STM32，未启动云台。

Windows测试最初受设备访问/配对状态影响失败，失败记录01–03保留；允许设备访问并清除这块K7的旧Windows配对记录后，04成功。板端此前反复连接/断开记录不能删除，也不能算作本轮保持期间的断开。

## 前端继续联调

连接 VelaVision K7，订阅Notify并使用新版 connectWifi()，或cmd=connect。submit_credentials仍仅确认收件，不联网。匹配事务id/session，等待wifi_connecting和wifi_connected或error；成功事件含真实IP。

当前Wi-Fi已在线；先查询status，不发送X（在线扫描尚不支持）。若手机保存旧配对且重连失败，可删除这一个设备的旧配对后从App重连。

## 证据

evidence/wifi-arp-20260910/ 下 ramload-progress.txt、ble-connect-04.jsonl、gateway-cold-01.json、final-status.json、device-state.json。对应构建在 evidence/build/wifi-arp-20260909/。工具新增本版RAM校验/加载/启动入口；BLE测试增加可选保持检查，默认流程不变。AI日志归属同仓logs/maomaojiang01，未远程提交。
