# Ubuntu 整机视频联调界面

入口：host/whole_device/integration_panel.py；部署启动：tools/launch_integration_panel.py。Ubuntu当前窗口PID36755，输出目录work/velavision-project/evidence/integration-panel-20260916。已打开并检查截图，尚待实际USB视频接入验证。

窗口集成USB JPEG实时预览、板端匹配命令、姿态角度、正左右三图完成位、后端受理状态以及停止操作。命令/姿态/上传来自板端日志，不由PC替代推理或控制算法。界面不显示云端任意识别原文，显示的是板端匹配的命令枚举。图片受理不等同分析完成。

操作顺序：
1. 点击“连接设备 / 视频通道”：独占串口，查询网络，启动USB Host及linkvideo；不录音、不运动。
2. VMware中将新出现的openvela link probe接入Ubuntu。使用既有1209:0001、端点81二进制JPEG协议，并校验产品标识、端点、帧头/图像CRC。
3. 按既有v1.3-xcenter流程复位0/0，确认无遮挡，勾选确认后开启相机待命。每次运行窗口仅允许一次采集启动尝试；失败后不得反复重开采集。
4. 视频在3秒内持续更新且板端待命、联网时，才启用“开始语音整机测试”。点击后启动板端60轮上限device-loop，听短音说“启动云台”或“皮肤检测”；这会真实驱动硬件。
5. 可点击停止；界面等板端日志确认，不将按钮点击等同已停止。关闭时若仍运行，先请求停止。

窗口打开不自动占用串口、不录音、不运动。串口由单一线程持有；外部脚本需先关闭窗口释放串口。USB图像过期则清空，不把旧帧当实时图像。日志串口可能丢行，未收到完成信息时保持未确认，不能把UI进度当作完整硬件验收。

Python状态测试3项通过，涵盖识别完成不结束命令循环、三图完成不等同后端受理、姿态/网络解析。尚未验收本窗口视频与语音同时运行、真实三图上传；不声称脱离电脑完整闭环。固件仍是skin-command-20260916，本次仅增加主机界面。

用户已被请求连接视频设备、确认复位后开启待命，等待反馈；勿额外启动其他串口观察脚本。

最新接入：用户先接入的是CH340串口，窗口未点连接。本次已启动USB Host与linkvideo，板端LINK running3600s/JPEG及CAMERA ENUMERATED32e6:9221，Wi-Fi在线。Ubuntu尚未见1209:0001，已请求用户将openvela link probe接入。修正窗口命令发送为逐字4ms、命令间隔2秒，避免初始连续命令交错；在确认未启动采集后替换窗口PID37167，使用--connect --attach仅接管现有会话，未重复linkvideo。相机待命仍须用户0/0确认后点击。实时视频未验收。

真机预览通过：用户确认接入并待命后发现窗口未收到采集操作；接管窗口后首轮因遗漏UVC probe在采集前返回-19、零帧、g_capture_attempted尚未设置。补上probe committed1成功门控后才发送voicecam，增加测试，4项通过。当前窗口PID37546，camera=true/video_fresh=true/frames249、wifi=true/held=true，TRACK x0 y0 tx0，实时JPEG预览已验证。用户已被提示可点击开始语音整机测试；尚未发起本窗口语音/拍照/上传验收。不可再次启动本次相机采集；窗口保持串口所有权。

布局修正：用户截图显示旧窗口最小高度过大、操作区在屏幕外。已将四个操作按钮、确认项和说明置于顶部，侧栏明确字符宽度，默认1100x700；核实无录音且held后仅替换主机窗口PID37711，--resume-camera接回现有采集，未重启/重复采集。实际截图panel.png显示开始语音整机测试按钮可见可用，实时视频28帧、Wi-Fi在线。

并发问题未解决：用户点击整机测试并说话后，多轮云ASR均150帧/completed但空文字、intent0；第19轮result-5，随后loop_done-5/applied0。无云台动作，不能冒认口令匹配成功。单独语音成功与视频并发失败需做音频对照。网关新增可选K7_CAPTURE_NEXT_PCM一次性私有3秒保存，默认关闭、请求文件消费后不持续保存；15项回归通过。当前网关PID43264、26提示预热就绪；窗口PID37910新增单次语音检查，4项状态测试通过，视频会话保留。等待用户主动点击单次检查，不自动录音。


按钮禁用排查（2026-09-16）：窗口37910状态wifi=false、busy=false、held=true，实时视频正常。console-153455.log末尾曾有radio_generation1/wifi1/ipv4_ready1，之后命令无回包；console-154007.log持续TRACK但radio-status、device-stop、halt均无有效回应。不能把“串口可打开”视为控制通道健康，不能声称真实断网或已经停止确认。确认界面空闲后终止旧窗口，仅尝试Ctrl-C/Ctrl-U清行和radio-status，console-recovery.log仍无NSH/K7CLOUD回复；未重启板子、未重新启动相机。已部署窗口38317，保留实时视频，新增恢复控制通道按钮与禁用原因，网络解析仅接受完整字段，5项测试通过。串口驱动重新绑定需sudo而sudo -n不可用，未执行。等待用户仅断开重接QinHeng串口，不动视频OTG/供电；重接后需恢复窗口串口所有权并验证新radio-status，再允许语音测试。当前按钮故障未完成真机修复，云ASR视频并发问题仍未解决。


2026-09-16 视频结束复核：console-160041.log证明UVC requested_s=1800、elapsed_ms=1800226、53802帧、29.88fps、result0；TRACK END tx0/errors0，视频正常到期，不是新USB掉线。重连串口后radio-status及echo CONSOLE_READY均无响应，控制台仍未恢复。窗口已关闭，Ubuntu reset-watch PID38885独占串口，等待用户RESET最长600秒，看到U-Boot后拦截并fastboot usb1；没有主动发reboot或写闪存。恢复监听状态见reset-watch-state.json。UI源码增加采集结束清除camera就绪与恢复10秒超时，7项测试通过，尚未重新部署该增量。后续必须先查看watch状态，避免争用串口；成功进入Fastboot再接OTG和加载已校验固件，之后重新配网。

用户已按RESET；reset-watch成功拦截U-Boot并进入fastboot usb1，状态fastboot_ready。Ubuntu lsusb仅CH340、未见18d1:4d00，等待USB download gadget接入。旧Wi-Fi与相机运行快照失效。load_skin_command.py本地哈希/大小与脚本校验通过，尚未重载。

恢复完成：console-recovery-01独立证据目录，19581048字节OTG1.9106秒，包CRC d2370841、固件CRC9af381e8及四段无线校验通过，skin-command哈希不变。BLE registered1、wifi-service ret0、NSH恢复；panel39175已部署超时/采集结束提示。仅初始化Host/linkvideo，不启动采集、录音或运动。等待App重新配网，先单独ASR再视频并发对照；尚未修复并发根因。原第一次重载因usb.json证据保护在传输前拒绝，新增--run-label独立目录避免覆盖。

恢复后补查发现Windows8000/8001均无监听，旧服务已退出（退出原因未知）。已恢复speech PID69872仅127.0.0.1:8000、gateway PID42496仅10.3.1.125:8001；health200、26条提示2158080字节预热完成。不能把此前空ASR直接归因本次服务退出；仍需真实音频对照。panel39175已连续收到radio_generation0/wifi0，USB视频通道接入但未启动采集。

本次重新配网已实查：radio_generation1/wifi1/ipv4_ready1/IP10.3.0.214连续回包，panel wifi=true、camera=false、busy=false、frames0，8000/8001服务监听正常。下一步用户点击单次语音检查建立无视频采集基线，再测并发；尚未录音。

视频并发单轮通过：panel48030、camera=true/video_fresh=true/frames2632/held=true；板端capture0 produced150 consumed150 receive0、asr0/completed1，云端准确final“皮肤检测。”，网关150帧/无错误，最大帧间隔32ms。会话起点到final4453ms、完成5156ms；无视频对照4313/5000ms，不是用户说完延迟。仅单轮通过，不等于长期稳定或三图闭环。证据video-concurrent-success.json与metrics/1789548193957643600.json，原始录音仅private。下一步用户点击真实整机测试，先启动云台再皮肤检测；当前相机已采集，不可重复启动。

用户要求参照旧版视觉融合：已找到host/vision/photo_preview.py与track_overlay.py；旧版mirror=True、显示板端TRACK BOX及姿态提示。当前核心人脸/姿态模型、fusion、track、JPEG源码逐项与accepted-source-comparison快照一致，证据accepted-vision-review.json。新窗口原200ms刷新上限5FPS、无框、未镜像；已在源码复用display_box（序号差10/350ms过期门限），加入默认镜像及切换、原始640x480 JPEG显示，独立33ms渲染刷新、接收/显示FPS计数和姿态方向提示。镜像仅显示，上传照片及板端坐标不变；不承诺实际30FPS或提高算法精度。14项主机测试通过。Ubuntu SSH连续握手超时，尚未部署或实机视觉验证；K7仍停在Fastboot、新启动关键词镜像待加载。用户被询问VM是否卡住。

Ubuntu恢复后新启动关键词固件已RAM加载：19585144字节/1.9149秒，包CRC8ea7f21b、固件CRC1d3e3e81、无线四段校验通过，SHA1a4276bf…99ef44d。BLE registered1、Wi-Fi服务ret0。新版镜像/人脸框/33ms渲染窗口已部署PID2828，GTK/cairo及GI桥接验证可用，启动日志无异常；截图已检查布局，但无视频设备，不能算画面实测通过。Windows语音9568/网关39244正常监听。等待App配网、openvela link probe接入及用户相机待命；尚未真实新口令动作或三角度验收。

2026-09-16 17:38 相机就绪按钮修复：用户确认新版镜像预览流畅；窗口 Wi-Fi 在线、视频30fps但 camera=false。原串口日志没有一次性 VOICE SESSION ready，导致整机测试按钮灰色。新增有界 HOLD 确认：同模式 hold 不增加板端 revision，因此不能只等 applied；接受 requested=0/result=0 后的新 TRACK_HALTED 也可确认相机会话就绪。只收到命令回包、仅视频或失败回包均不能开放按钮。11项状态回归通过。窗口 PID3418 接回既有采集，无固件重启、无自动录音或云台运动。实查 camera=true、voice_enabled=true、held=true、busy=false、video_fresh=true，TRACK tx=0；证据 evidence/integration-panel-20260916/readiness-recovery.json。下一步用户点击整机测试说“启动”，真实新口令和三角度上传仍待验收。
