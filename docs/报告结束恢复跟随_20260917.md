# 报告结束恢复跟随

用户要求：完整三图→后端上传→语音报告完成后恢复人脸跟随和云台运动。过去自动完成三图会切hold，报告之后未恢复，属于流程缺项。

本次在自动photo_prompts链加入：报告前获取已应用hold的revision；报告成功后确认同一photo_epoch且三图齐全，在tts_lock下核对命令epoch和取消标记，再请求按该revision恢复tracking；等待worker应用ACK最多3秒，打印report_resume_tracking result。pipeline持锁比较revision，若期间有halt/切换模式则拒绝，避免覆盖用户停止。人工单独report-play不隐式启动运动。未增加物理电机执行ACK，仍需现场观察确认。

实际模式代码O0/O2回归验证正常恢复、重复恢复拒绝、停止使旧revision失效；实际photo_prompts抽取测试覆盖报告成功、失败、取消、失效请求。合并第四轮失锁重试和3小时时限至独立report-follow-20260917候选，构建中，未上板、未实机验收。不会把旧长测或旧跟随结论算为此版通过。

最新覆盖：合并固件report-follow构建/哈希校验通过，5011616B，SHA3642d9155f8456d216ddb14cc4cacfb9139a6f19bc5997f507638a804306260b。首次构建接口声明缺失失败日志保留，第二次增量通过。更新前新鲜状态held=true/busy=false，已释放UI82847、软件重启并进入Fastboot；Ubuntu未接到OTG，已请求用户连接。新固件未加载，旧Wi-Fi/视频快照失效，后续需重新配网。当前不运行长测，soak-run-01观测失败记录保留。

部署覆盖：report-follow已RAM启动，21788832B/2.199秒，整包CRC64af471d/固件CRC13147139/无线四段CRC通过。BLE服务registered1、Wi-Fi服务ret0，新界面启用忙时单串口遥测；未自动录音或运动。等待App重新配网及openvela link probe相机待命，报告结束真实恢复跟随仍待实机验证。
