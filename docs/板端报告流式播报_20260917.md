# 板端报告流式播报接续（2026-09-17）

用户要求报告文字流直接送TTS以缩短首声等待，并明确同意当前mock SSE先作联调。当前Swagger和实查仍明确MOCK，不能当真实皮肤分析。真实brief当前conclusion=balanced、description=null，不能编造分数或建议。

实现：host/whole_device/report_narration.py 验证start/text_delta/done、requestId/taskId/reportId及seq从1严格连续，增量delta按标点/80字符分段；有界音频队列2段，一段播放时合成后段。断流、error、身份改变立即终止，不重放。report_audio.py 先查真实任务状态，mock-sse显式首播“以下为联调测试文案，不是真实测肤结果”，每段下发前再次核对任务身份。未接mock为正式讲解。真实模式后续需后端确认后再启用real-sse。

板端board_speech_bridge.c新增K7S1R协议，R携带36字节taskId；W等待、J状态、D为16kHz单声道S16LE音频、播放成功回A，E完成。音频每段上限960000B，最多40段，音频锁与epoch/tts-stop取消保护；不把后端文字执行为命令。正常三图自动上传受理后进入报告播放；单独入口k7cloud report-play <gateway-ip> <taskId>。保留语音10dB、短音24dB、本地固定提示、TCP发送缓冲和持续视频修复。此为板端掌握请求/播放，认证和SSE/TTS适配仍在Windows网关，不是直接板端HTTP/TLS。

37项主机测试通过，包括首句在done之前产出、后续合成等待时先下发首段、板端A确认、任务替换等。实际mock SSE+当前TTS主机联调3段125440/171520/199680B，首段约1734ms，总4250ms；这是主机可下发时间，不是喇叭实听延迟，board_played=false。

独立report-speech-20260917固件编译通过，5007520B，SHA ae08e24fe74e836f370cd7a85d97625f174666068165943acd6e19e257a86dad；只审计同步board_speech_bridge.c和k7cloud_main.c。tools/report_speech_build.py、load_report_speech.py已准备。板子已由停止状态软件reboot进入Fastboot，旧照片已保存且后端受理，无未上传照片；当前等待用户把USB download gadget接入Ubuntu，尚未加载新版。不再次按RESET。

Windows新网关PID51104，K7_REPORT_NARRATION_MODE=mock-sse，8001/SO_RCVBUF4096，日志evidence/report-speech-20260917/gateway.log和metrics，26提示预热成功；旧PID4568已按身份核验停止。speech服务与报告查询sidecar保持。旧GUI46465已停止以释放串口；加载后用tools/launch_integration_panel.py --connect --report-task aa9c678f-c7b7-4e0c-a93e-05e6a071a389打开新增播放按钮。须App重新配网；可直接读旧任务测试，不用重拍。任务被后端替换则不得继续旧任务。

下一步USB接入→load_report_speech.py --load→检查CRC与蓝牙启动→恢复GUI并配网→点击报告播放，核对板端report_segment/report_done和gateway指标，询问用户是否听到清楚连续播报。目前不得宣称板端语音报告验收完成。新程序及日志未远程提交，无eMMC/STM32写入。

新版已RAM加载，传输21784736B/2.3132秒，包CRC fbdbb7d3、固件CRC6705f457与无线四段校验通过；NSH、BLE registered=1、Wi-Fi服务ret0。已恢复带现有taskId的联调窗口，等待App配网和板端报告实听。

用户配网后真实板端report-play完成：3段均返回A，report_done result=0，网关K7S1R completed=true/error=false/duration30843ms（含播放，不是首声延迟）。语音10dB。日志有Wi-Fi RX -28但TCP最终传输成功，重复稳定性/停止测试尚未完成，人工实听待确认。证据board-report-verification.json与board-report-play.log。随后恢复GUI并附着现有板端服务，不重启板子、不录音、不运动。

人工验收通过：用户确认“都听到了，间隔可以”。保持当前语音10dB与mock-sse配置；GUI当前PID56003，网关PID51104。报告语音单独入口真机通过；拍摄完成自动衔接报告的完整新一轮尚未复测。后端仍MOCK，不能以本次测试分数当真实皮肤结果。

整轮自动衔接推进：提取真实native photo_prompts与device_photo_prompts，在Ubuntu GCC O0/O2 -Wall -Wextra -Werror测试成功后仅一次报告、上传失败不播、过期capture不播，均通过（tools/test_photo_report_chain_vm.py）。硬件仍Wi-Fi在线、camera=false/held=true/done0；等待用户接回openvela link probe并通过窗口确认现场准备。没有自动录音或运动，完整新一轮实拍尚未完成。

完整新一轮自动衔接已取得板端证据：done7，新任务 02a6805c-70e2-4803-b0e5-a36bdf431408 后端受理，随后report_segment 1/2/3、report_done result0，非单独手工report-play。当前云台停止、Wi-Fi/视频保持。用户尚未单独确认此整轮声音，但先前单独播放实听通过。见whole-cycle-verification.json。
