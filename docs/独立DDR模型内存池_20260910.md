# 独立DDR模型内存池增量：2026-09-10

model-arena-20260910已在K7从RAM启动，完成独立DDR稀疏测试与无线回归。候选范围0x60000000–0xa0000000共1GiB，位于已核对低地址bank内并避开既有暂存/保留区。此阶段不启用4GiB以上bank，不改变普通malloc堆或USB/NPU DMA地址策略。

新增port/new/nuttx/arch/arm64/src/rk3576/rk3576_model_arena.c和公共头nuttx/mm/k7_model_arena.h，提供显式initialize/alloc/free/available。采用独立NuttX mm_heap、64字节对齐，初始化由显式调用触发。仅CPU用途；现有语音推理库尚未接入此分配器，因此不代表sherpa/ONNX Runtime已自动获得1GiB。

新增app/k7mem测试入口：status读取状态，test分配384MiB，对每4KiB页的首个cache line写地址模式及反码，清理/失效cache后核对，两轮完成后释放并检查空闲量恢复。它是跨页稀疏测试，不是每个字节全覆盖DDR测试，实际已运行通过；这仍不代表整个1GiB每个字节已测试。实测free命令会分别显示k7-model和Umem两行，不能只看原系统堆一行。

新board profile为velavision_model_arena_local，保留已验证无线配置；主用无线profile没有开启此特性。SDK同步审计通过；首轮构建脚本因CRLF换行失败，修正后独立编译通过。最终固件哈希和源码清单见evidence/build/model-arena-20260910/verification.json，补充检查见candidate-check.json。输出曾沿用模板BUILD_AND_TEST_PASS字样，只代表该脚本流程结束，未执行内存硬件测试；脚本已改为BUILD_PASS。

后续上板步骤：核对加载地址/镜像hash和所有保留区→RAM启动候选→确认原系统堆和模型池分别显示→执行k7mem test→核对Wi-Fi/BLE与USB/NPU边界。失败则恢复已验证wifi-arp镜像。SMP和eMMC变更不混入此候选。


## 2026-09-10 真机结果

- 实际加载镜像 SHA256：`8ed6d36770de6fa251065f6ee4a30b5d44d4efe8581d43f22932b97423c6b969`，完整CRC和NSH启动通过。
- 初始专用池未初始化；执行test后可用1073741104字节。
- 分配402653184字节（384MiB），98304页，两轮地址/反码稀疏读写通过。释放前后空闲容量均1073741104字节。
- 系统堆仍126869496字节，独立模型堆单独显示；并未将全部4GB纳入普通malloc。
- 无线初始化通过；Windows加密BLE请求完成WPA2/DHCP，返回真实IP10.3.0.214，status一致。网关5发5收，五个序号均收到；串口RTT汇总尾部截断，未据此推导平均值。
- BLE/Wi-Fi保持30秒后status仍一致，测试客户端主动断开，Wi-Fi继续在线、BLE服务保留。不是长时间稳定验收。
- 未在本轮调用USB/NPU执行任务，未启用SMP，未写eMMC。语音运行时的分配器接入尚待实现。

证据：evidence/model-arena-20260910/下ddr-test.json、ble-connect.jsonl、gateway.json、device-state.json及ramload-progress.txt。构建报告中的hardware_tested:false保留为构建时快照，以此独立真机证据补充。
