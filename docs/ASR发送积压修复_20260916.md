# ASR发送积压修复（2026-09-16）

本轮未启动相机采集，窗口 frames=0。用户执行两次单次语音检查：首轮板端150帧/completed=1、网关final并completed；第二轮板端SOUND result=-28、frames_sent=102、completed=0，网关收到101帧后连接失败。不能把该故障仅归因视频并发。首轮完整诊断录音仅保存在private，不纳入公开材料；峰值2250、RMS205.9，16kHz/单声道/16位/48000采样。

stream_ring的32帧容量只能保留640ms。生产者填满未消费的槽后返回-ENOSPC，取消整个会话。修复为150帧，覆盖当前固定3秒录音窗口（96KB PCM，原20.48KB）；仍逐帧发送，满缓冲仍显式失败，不覆盖、不丢帧、不增加有意等待。此修复不保证网络永不超时，也不代表已解决此前空识别或控制台阻塞。

增加真实失败条件回归：暂停消费者，产出150帧后恢复消费者，逐帧核对顺序和PCM内容；同时保留环绕、符号、半帧发布、超容量失败与取消测试。Ubuntu cc -O0/-O2均通过。board_speech_bridge额外记录capture_rc、produced、consumed、slots、receive_rc，避免只看到上层EIO。

构建入口tools/asr_backlog_build.py，SDK同步先审计差异，仅修改stream_ring.h和board_speech_bridge.c；sync_sdk.py新增精确前驱哈希白名单。本轮候选尚未上板，旧skin-command仍运行且Wi-Fi在线。证据evidence/asr-backlog-20260916/。

构建完成：1818步成功，nuttx.bin 2803832字节，SHA256 e8a4948cf0b74312e1291d5b59cce295e23962134232c250f383a2ebb51cb4d5，collect逐文件哈希通过。已确认panel空闲且camera=false后结束窗口，软件reboot拦截成功，板子当前Fastboot；等待用户OTG接入。当前Wi-Fi离线，不能沿用旧IP在线快照。加载工具tools/load_asr_backlog.py，尚未上板。

用户反馈已连接后，下载器未找到匹配USB序列号，在传输前退出；Ubuntu lsusb仍无18d1:4d00。Windows USB download gadget序列号正确，ProblemCode28（不是Code43），尚未被VMware交给Ubuntu。已请求精确连接该设备，未重启或写闪存，候选仍未加载。AI日志18文件41300事件官方校验ALL OK。

load-02已完成OTG：19581048字节、1.823449秒、包SHA256 2edffe47d166cbcc350331d2cdad1f2e22f1b93fb8367bb8dd56f33605078358。首次等待U-Boot提示符超时，未继续复制/启动。重新发送Ctrl-C+换行后获得=>；后续校验传输回执sha/长度并沿用原包CRC与每段CRC检查流程继续，无重传、无重启、无闪存写入。启动服务初始化中，尚无真人复验。

已RAM启动修复版：包CRC0a988bf2、固件CRC425c025b和无线四段全部通过；BLE registered1、wifi-service ret0，NSH命令正常。重新打开窗口--connect --attach，仅接管串口和查询状态，不启动USB Host/linkvideo或相机，做更干净的单独语音基线。等待App配网和真人复测。

修复版首轮实测：Wi-Fi10.3.0.214、无相机，capture result0/produced150/consumed17/slots150/receive-5，网关仅收16帧，13.547秒后RuntimeError、无final。证明本轮完整采集且无ENOSPC，但发送/服务链路仍失败，不能宣称ASR修好。新增主机错误分类client_audio_timeout、upstream_audio_send_timeout、upstream_result_timeout，live_client保留安全白名单错误码写入metrics；23项测试通过。服务已更新：speech9568回环8000、gateway39244设备网卡8001；板端未重启，窗口47749仍保持。下一轮只需用户再点击单次语音检查。

最新单次成功：capture0/produced150/consumed150/receive0、ASR result0/completed1，云端准确final“皮肤检测。”；网关起点到final4313ms、completed5000ms，不是说完到响应。上一轮失败未复现，不能宣称稳定或根因全修。单次UI原只等timing行而该行丢失导致busy卡住；现直接解析asr完成摘要，8项测试通过，panel48030已部署。确认识别结束后初始化Host/linkvideo，1209:0001已自动接Ubuntu，Wi-Fi连续回包，camera=false。等待用户相机待命后继续单次ASR对照，不启动真实动作/拍照。

视频并发单轮通过：panel48030、camera=true/video_fresh=true/frames2632/held=true；板端capture0 produced150 consumed150 receive0、asr0/completed1，云端准确final“皮肤检测。”，网关150帧/无错误，最大帧间隔32ms。会话起点到final4453ms、完成5156ms；无视频对照4313/5000ms，不是用户说完延迟。仅单轮通过，不等于长期稳定或三图闭环。证据video-concurrent-success.json与metrics/1789548193957643600.json，原始录音仅private。下一步用户点击真实整机测试，先启动云台再皮肤检测；当前相机已采集，不可重复启动。

真实命令未命中：临时私有final观察器捕获“启动。”，未含云台，故intent0且不动作；随后用户停止，VOICE applied0/loop_done-125，视频正常。前述一次性WAV时间1789548745早于final1789548777，不能把该WAV当作这句语音的录音；它出现显著满幅样本，仅可作为另一录音质量异常线索。现有源码同时支持精确口令“开启云台”，不放宽为单独“启动”，下一次对照继续保留真实音频。
