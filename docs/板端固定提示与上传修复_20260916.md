# 固定提示板端内置与三图上传排查（2026-09-16）

用户明确要求固定提示保存在板端，避免每次从网关下载；继续修复三图上传。当前三图已完成一次：epoch3，front q11685/30066字节、left q12073/29213字节、right q12459/29132字节；保存全部result0/complete1，随后hold applied0，但photo_upload result=-5 accepted0，未拿到后端taskId，不能算云端测肤完成。Windows private/photo-upload-spool尚无本轮落盘三图。旧网关最后一条失败只有TimeoutError/10000ms且没有mode，不能确证具体上传子阶段。当前Swagger仍提供POST /api/v1/skin-assessment-tasks，metadata+front/left/right、设备token及幂等键合同与现有代码一致。

网关15840已部署mode、photo_stage及每张接收字节诊断。retained-upload-retry尝试沿用当前RAM集合重新运行旧阶段worker，但第一次云ASR连接失败即退出，未进入照片重传。该工具终止窗口11934、未重启板子，结束发送device-stop/halt。之后UART仍持续输出TRACK，radio-status曾回wifi1/IP10.3.0.214，后续命令无回应；Ctrl-C/Ctrl-U以及用户重连QinHeng后仍无回包。未重启以尽量保留三图，照片后续可读性未验证。此时不能再声称窗口仍运行或上传成功。

固定提示26条从现有网关缓存冻结为2196480字节s16le/16000Hz/mono，SHA256 38a1f29e18b010e96f9d7bdd0c4d08e6d74d8d9602b0f2c691ea0850eb701953。原始PCM+manifest在app/k7agent/cloud/assets，工具tools/freeze_board_prompts.py生成board_fixed_pcm.inc。固定key直接播放固件只读PCM，不调用connect_server、不分配网络PCM缓冲；保留audio_owner互斥与停止回调。动态聊天和tts-test仍走原云端路径。这里的板端保存是固件内置、RAM启动后可离线播放，尚未刷写eMMC，不表示断电保留运行固件。音量仍使用10dB播报路径，短音24dB。

local-prompts候选独立构建通过：5003384字节，SHA256 4589c4a8b3236452e3bdcd4c4c06b95f310e46fe367a4e6cc9b5c09ad8fb47e7，尚未加载。25项资产/网关/UI测试通过。后续photo-retry合并候选加入k7cloud photo-retry <gateway>，只允许完整保留三图与停止状态；不录音、不调用新拍摄；同boot/epoch沿用captureSessionId。增加每次1024字节上传写入、底层errno和copy/connect/protocol/metadata/front/left/right/commit/backend_ack诊断。O0/O2真实retry入口guard测试通过；合并候选正在构建，尚未上板。分块发送是缓解候选，不是已证实根因或已实测修复。

下一步：收集photo-retry构建/哈希、生成固定哈希RAM loader；现板串口无法恢复时，更新会丢失未导出的RAM三图，应明确告知用户并确认重启重拍。加载后先在网络不可用时验证固定提示可播，再配网拍三图，读取明确photo_transport阶段及后端taskId。UI失锁提示源码已修但尚未部署，原帧率优化保持。不要因构建通过宣称整机闭环完成。

合并候选photo-retry已构建并通过哈希校验：5007480字节，SHA256 2cde51b7c1a228a883e1d54b3276250047b2623397abc3a22682c2653d8b055f。tools/load_photo_retry.py为固定哈希加载器，固件目标末端低于OTG暂存区，无区间重叠。尚未加载；等待用户确认重启会失去当前RAM三图、需要重拍。

用户已允许RESET并重新配网重拍，随后按键成功；reset-watch确认fastboot_ready。原RAM三图已不再可用。当前Ubuntu仅识别QinHeng，等待USB download gadget接入，尚未加载photo-retry固件。新UI已增加“重传已拍三图（不重拍）”，只在联网、三图齐全、停止且空闲时允许；重传回包解除busy，13项UI测试通过，尚待随新固件部署。监听日志在evidence/photo-retry-20260916/reset-watch.log。


最新部署覆盖：photo-retry已RAM加载，固件SHA256 2cde51b7c1a228a883e1d54b3276250047b2623397abc3a22682c2653d8b055f；21784696字节OTG2.1269秒，包CRC a6e64082、固件CRC1f3c7e57、四段无线校验通过。BLE/Wi-Fi服务恢复；本地提示tracking_stopped实播result0，故意使用127.0.0.1作为云服务参数，实际日志local_prompt证实本地路径。首次脚本等NSH行尾超时，随后的tts-state为result0；修正脚本兼容配网日志交错后重测通过。重测时用户已配网，wifi1/ipv4_ready1，不能描述为断网实测；用户确认“听到了，清楚”。26条固件音频不依赖网关，动态TTS/云ASR仍需网络。窗口28435已部署新失锁提示、无效角度显示与独立重传按钮，camerafalse、视频尚未接入，等openvela link probe及用户相机待命；网关51424正常ready。三图重拍上传尚未验证，不能标记闭环通过。

最新三图复测：epoch2 front808/31180字节、left1635/28997字节、right2100/30351字节全部存储成功；photo_transport stage=left result=-11，accepted0。错误定位到左图TCP写入超时，尚未到后端受理；不能称接口没实现，也不能称上传已完成。候选网关PID4568增加可选--receive-buffer4096，监听socket实际SO_RCVBUF4096，未改系统全局网络参数。约92KB大包通过小接收缓冲的本机测试通过，但真机效果未验收。窗口保留三图、云台停止，已请用户点击“重传已拍三图”做同一组重试；不重启、不重拍。证据upload-left-timeout.json、gateway-small-rx.log。

### 本轮进一步定位：保留三图原生重传
- 已在确认 done=7、held=true、busy=false 后释放界面串口，执行 `k7cloud photo-retry 10.3.1.125 &`；无重启、无重拍、无驱动云台。
- 返回 `photo_transport stage=connect result=-5`、`photo_upload epoch=2 result=-5 accepted=0`。此轮失败发生于 TCP 建连，尚未调用后端 HTTP 上传接口。
- Windows 网关进程4568仍监听10.3.1.125:8001，主机地址10.3.1.125/22；K7报告10.3.0.214、wifi=1、ipv4_ready=1。电脑对K7两次Ping均超时，邻居表MAC全零、无有效解析。
- K7无线host-status为fault=0、tx=493、ACK=493、RX=1043；这些计数不能证明当前IP数据链路可用。执行板端Ping后，后续wifi-status/host-status未收到有效返回，需进一步定位网络阻塞，不能宣称无线已恢复。
- 当前证据将阻塞缩小至K7到电脑网关链路；尚不能区分板端协议栈/无线驱动/AP链路根因，也不能用此覆盖此前left阶段-11的独立故障。
- 已保留RAM三图，未重启。界面重新挂接后照片勾选可能因主机状态重建清空，不等于板端照片已丢失；不要为此直接重拍。
- 证据：evidence/photo-retry-20260916/direct-native-retry-result.txt、network-diag.txt、wifi-diag.txt；原始串口日志同名位于Ubuntu对应evidence目录。
