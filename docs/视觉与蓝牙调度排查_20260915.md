# 视觉运行后 App 通知订阅失败

用户此前配网成功，启动 camera-reconnect 固件的 voicecam 后 App 提示“通知订阅失败，请重新配对”。诊断串口首次EIO，随后恢复；没有据此认定无线损坏。

抓取到真实 BLE connection status=0、encryption status=0/enabled=1；后续 PROV connected=1/subscribed=1/busy=1，说明此次通知订阅实际已在板端生效。App 反复读状态，Wi-Fi仍未取得IP。历史17次连接/17次断开，后续19次连接/18次断开；这些计数本身不能证明用户主动断开。

源码及线程状态发现视觉解码线程以FIFO优先级90继承CPU0亲和性，配网worker优先级85。视觉连续有帧时可能饿死配网worker；后者需要执行才能清除旧任务busy及回复扫描结果。BLE ATT位于较高优先级工作队列，因此仍可连接/加密/读取状态。这是当前最强的原因假设，尚待修复前后真机对照，不能仅凭源码宣称已修好。

候选 vision-ble-coexist-20260915：只修改 k7_pipeline.c，视觉解码优先级降至80，SMP核心数大于2时绑定CPU2（双核回退CPU1；单核仍保留低于配网worker的优先级）。USB采集/回填优先级不变，现有ASR采集CPU1不变。九模块ARM64编译通过，模式切换实际C O0/O2通过。单文件SDK精确前版哈希审查同步通过，独立完整构建中。当前板端仍9e28d2ed版，尚未换候选。

验收顺序：构建收集与哈希核对 → RAM加载 → 先启动视觉保持会话 → ps核对解码CPU亲和性和优先级 → App连接/订阅/扫网/提交密码 → 板端wifi1/IP与App回包 → 持续视觉期间保持蓝牙。若仍失败，继续取证，不以重新配对作为已证实结论。未运动/刷写eMMC。

最新：共存候选全量构建通过，2803824字节，SHA256 30f6a77c377574f53bdb11696a6936279f290e05c519edcd87ffcc792df79305。已进入Fastboot等待OTG，不再运行蓝牙配网服务。Windows现识别USB download gadget，ProblemCode=28；Ubuntu lsusb无18d1:4d00。当前App连接失败与此前运行期busy问题应区分，不能在下载模式测试BLE。已请求VMware接入后运行tools/load_vision_ble_coexist.py --load。

本仓frontend/vela-provision.mjs的connect流程为创建链路→服务/特征发现→Notify订阅→轮询加密就绪，并非所有步骤失败都等于通知订阅失败；截图App源码未在此处确认。前端应保留原始错误码及失败阶段，分别展示连接失败、特征发现失败、通知订阅失败、加密等待超时与配网任务繁忙，不应统一提示重新配对。实际App如何映射错误仍需其代码或日志确认。


2026-09-15 最新实测覆盖：vision-ble-coexist / 30f6a77c 已 RAM 加载，完整 CRC 与启动通过；实际 decode 线程 CPU2、FIFO 优先级80。用户 App 已配网成功，WiFi 1/IP10.3.0.214，40.23秒 BLE subscribed1/busy0/errors0 与视觉帧递增并存，无云台运动。仅有限共存验证，非长期稳定验收。证据 evidence/vision-ble-coexist-20260915/device-state.json。

用户提供 proof 后真实设备登录和 gimbal-ai/messages SSE 中文回答已通过；私密认证仅在 private/，不写交接明文。新增 host/streaming_asr/conversation.py 与板端 chat-loop 候选：openvela 控制提示音、3秒录音、回答播放和下一轮；网关只调用后端与云语音，不驱动串口/运动。独立 B/C 协议避免污染普通 ASR、固定提示或回声测试。空语音不提问，问题60秒单次消费，90秒总超时和等待保活，取消不回退复述。74项 Python、实际 C 停止/响应 O0/O2通过；云回答→TTS实测5047ms、199680字节PCM，板端尚未播放。独立 native-chat 构建进行中。当前为无状态轮次、固定3秒录音、整段PCM，尚非上下文记忆/VAD/边合成边播放，也尚未真人连续聊天验收。
