# eMMC 原生只读诊断接入

2026-09-10 最新验收：`emmc-vfs-20260910` RAM镜像 SHA256 `06320f4b86326ea91d682fb6192dad5c64f7893279f9d73ed57490f80b9fe10b`，只读VFS连续64KiB和跨扇区4字节读取均返回0；GPT/16节点及块接口检查通过。BLE真配网IP10.3.0.214、网关5/5、30秒保持通过，客户端主动断开，Wi-Fi在线。证据 `evidence/emmc-vfs-20260910/device-state.json`。没有写盘或挂载文件系统，非完整模型文件读取验收。

## 当前接入进度

1. `emmc-gpt-20260910` 已真机通过主备 GPT 头和两份分区项 CRC 检查，15 个分区，容量 31,272,730,624 字节。见 `evidence/emmc-gpt-20260910/partition-summary.json`。
2. `emmc-block-20260910` 已真机注册 `/dev/emmc0ro` 与15个只读分区节点；只读 geometry、拒绝写调用、越界检查和分区地址转换通过。底层没有发出存储写命令。
3. 同版本普通 `dd` 打开返回 ENXIO，确认为 `CONFIG_BCH` 未启用导致 VFS 不提供块转字符代理。`emmc-vfs-20260910` 启用 BCH 和 BCH_DEVICE_READONLY，已独立编译/镜像校验，RAM 加载中，尚未验收。
4. 当前 SDK 的 fs 目录没有 ext2/ext4 驱动；cache 仅识别 ext 系列魔数，userdata 签名未知。这不证明文件系统损坏或已加密，不可直接挂载或当作空闲空间。没有格式化、调整分区或写盘。

### 下一阶段验收

普通文件接口连续读取、非对齐跨扇区读取及退出码；无线服务恢复；随后根据只读元数据和实际文件系统支持决定模型文件读取路径。初始400kHz/1位PIO用于可验证接入，不作为最终模型加载性能。

## 首次原生读取历史结果

`emmc-readonly-20260910` 已 RAM 上板。初始化成功：61079552 个 512B 扇区、EXT_CSD revision=8、用户分区、legacy 1-bit。LBA0/LBA1 各读两遍一致，CRC32 分别 8936a974/a63321cb，识别 MBR/GPT 签名。证据 `evidence/emmc-readonly-20260910/probe-01.json`。没有写盘。

之后无线恢复：Windows BLE 真配网获得 IP 10.3.0.214，网关 5/5，BLE/Wi-Fi 30秒保持通过；客户端主动断开。记录在同目录 ble-connect.jsonl、gateway.json、device-state.json。未重复 DDR 稀疏测试，CPU仍单核。这是有限回归，不是生产稳定性验收。

下方保留首次读取版本实现范围；后续完整 GPT 和块设备验收见上方当前进度。

当前源码已经迁入统一项目 `app/k7emmc/`，此前 `work-in-progress/emmc-readonly/` 仅保留历史说明，不再保存第二份实现。VoiceLink 并行目录未改动。

## 实现范围

- RK3576 专属 MMU 映射和独立构建配置 `velavision_emmc_readonly_local`。
- 复用已启动的电源、管脚和总线门控；未发现已供电状态则拒绝执行，不猜测电源配置。
- CLKSEL_CON89 选择 24MHz 晶振，SDHCI 分频至 400kHz；关闭 HS400/增强选通，按官方 SDK 的低速 DLL 参数设置 1 位 PIO。此为初期诊断模式，不是最终吞吐配置。
- CMD0/1/2/3/9/7/8/13 初始化，检查高容量 OCR、EXT_CSD 容量、用户分区、512B 数据扇区及最终 READY/TRAN 状态。
- CMD17 单块读取，R1/错误状态检查，总截止时间覆盖命令与数据阶段。失败禁止继续读取，必须重新恢复和初始化。
- `k7emmc status` 只读寄存器；`k7emmc probe` 初始化后分别读取 LBA0、LBA1 两次，对照内容并输出 CRC32 和 MBR/GPT 签名。没有任意地址写盘、擦除、分区切换命令，也没有文件系统挂载。

## 验证与边界

主机测试覆盖 PIO 接收、CMD17、初始化状态机和底层复位/命令白名单；`tools/test_emmc_readonly.py` 可复现。证据在 `evidence/emmc-source-review-20260910/host-*-test.json` 及 `host-test.json`。这些是模拟寄存器测试，不能替代真机验证。

`emmc-readonly-20260910` 已独立编译、链接通过，SDK 同步审计通过；镜像 SHA-256 `fd1462a94c3fad9692cbdd6e3d9fe3bbfb4f4920220eb12997b1a22a201881fe`，入口 0x40400000，镜像内存范围检查通过。构建证据 `evidence/build/emmc-readonly-20260910/verification.json`。

首次构建时曾处于 RAM 加载阶段；其真机寄存器状态、初始化和读取结果已在 `evidence/emmc-readonly-20260910/` 单独记录，未看到成功证据前不能声明原生 eMMC 可读。板端测试期间无线服务暂停，随后需要恢复。未写 eMMC/STM32，未启动云台。

即使 LBA0/1 读取通过，后面仍要完成只读块设备、GPT 范围和 CRC 校验、文件系统及模型文件读取。四核/八核和全 DDR 可用区扩展是独立任务，本次没有启用 SMP。
