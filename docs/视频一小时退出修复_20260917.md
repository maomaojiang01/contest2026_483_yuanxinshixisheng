# 视频一小时自动退出修复（2026-09-17）

用户要求继续三图上传验收。读取现有界面/串口/USB后确认：板端仍响应radio-status，Wi-Fi=1，Ubuntu没有1209:0001，Windows也没有openvela视频设备。实际日志为 `LINK running 3600s mode=JPEG video`，随后 `LINK END ... result=0 stop=0`、`VIDEO frames=0`、`linkvideo result=0`。

源码k7_link_probe.c的视频模式此前固定运行3600秒；退出后attempted防重入禁止同次启动重新运行。此次是定时正常退出，不应当继续归因于VMware抢占、USB Code43或要求用户反复拔插。

修复：产品视频模式连续运行；显式bulk诊断仍保持600秒/1024块限制；fault仍立即结束并按原流程停止DMA。独立video-continuous-20260917构建沿用velavision_photo_tcp配置（TCP发送积压8KiB、IOB预留8KiB、总64KiB），只同步经审计的k7_link_probe.c，保护未知SDK改动。未移除故障后必须重启的硬件保护。

本次更新前done=0，无待保留三图。先编译校验，再通过有NSH握手的软件reboot接管Fastboot，OTG RAM加载；不写eMMC/STM32。真实三图上传与后端受理仍为必须验收项，不能仅以视频或Ping通过结案。

新版已RAM加载，固件5007520B/SHA89881e468bb6bd290efc0a3fe302ad19839bbfd8ca6423cca20d1a9b11d39314；单包21784736B/2.1602秒，包CRC2c132322、固件CRC b0cd60a6和无线四段CRC通过。BLE服务registered1、Wi-Fi服务ret0。界面PID46465，仅连接控制/视频通道，未录音或运动；等待App重新配网、openvela link probe接入和用户现场准备。连续超过一小时实测未完成，三图后端受理未完成。旧photo-tcp在线快照已经失效，应以新鲜radio-status为准。


## 2026-09-17 真机三图上传验收
用户完成三角度拍摄并确认提交。板端 photo_transport stage=backend_ack result=0；photo_upload epoch=2 result=0 accepted=1，任务 aa9c678f-c7b7-4e0c-a93e-05e6a071a389。网关保存正面26344B、左侧25785B、右侧26124B，后端 accepted=true/status=queued/photoVersion=1。上传后 Wi-Fi 仍在线，后续多轮 ASR produced=150/consumed=150/result=0，视频仍持续接收。本轮上传闭环通过；报告未完成，重复大图及长期稳定性未验收。脱敏证据 evidence/photo-upload-accepted-20260917/verification.json；原图保留 private，不复制到公开证据。
