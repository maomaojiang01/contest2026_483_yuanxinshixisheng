# 手机蓝牙断连排查（2026-09-09）

## 后续修正：phone-pairing-20260909

### 手机复测结果

后续收发联调：15:38:10 观察到 reason=19 断开，15:38:56 再连接并通过已存密钥完成加密；尚未由用户确认此次断开的操作原因。15:40:01 收到一次 ATT Write Request（1 字节值），但未记录业务命令；随后只读状态为 `connected=1 subscribed=0 commands=0`。此时阻塞点是 Notify 未订阅，不应把该写入当作 X 扫描已完成。证据 `phone-observation-02.jsonl` 与 `phone-x-status-01.bin`。最新只读观察 PID 文件为 `private/phone-watch-03.pid`，新记录 `phone-observation-03.jsonl`，最长 1800 秒。

15:31 手机重新连接，完成 SMP 密钥交换，日志出现 `RADIO BLE encryption status=0 handle=17 enabled=1`。用户确认“蓝牙调试器”仍显示已连接。约 100 秒后串口复核：`connections=1 disconnections=0 encrypt_status=0 encrypted=1`，配网 `connected=1 subscribed=0 commands=0`。此次未复现一两秒断开；配对及初步保持连接通过，长时间稳定性、断线重连以及此镜像的手机业务收发尚未验收。

`subscribed=0` 表示 App 尚未启用 Notify，因此下一步先订阅 `6b7a0003-78c3-4f2e-9c2f-3ecbc2d74680`，再向 Write `6b7a0002-78c3-4f2e-9c2f-3ecbc2d74680` 写入 X。当前后续观察进程 PID 文件为 `private/phone-watch-02.pid`，只读 COM8，最长 1800 秒。

证据：`phone-observation-01.jsonl`、`phone-live-ble.bin`、`phone-live-provision.bin` 和更新后的 `device-state.json`。记录容量修正后首次手机复测成功支持此前容量问题的判断，但旧固件未打印 TX 失败码、此次又包含重启，不能反推所有旧失败的唯一根因。

以下启动说明为手机复测前记录。

修正版已实际 RAM 启动，UART 加载/CRC 阶段约 546.2 秒。无线模块返回 `cp_ready=1 wifi_ready=1 bt_ready=1`，后续状态确认 `host_mode=1 fault=0`、配网 `registered=1`。Windows 仅扫描、不配对或连接，观察到正确服务 UUID。记录在 `evidence/phone-pairing-20260909/device-state.json` 和 `advertisement.json`。手机复测/长连接仍待用户操作，不能宣称修复验收通过。

初始 `bt-host` 串口窗口只捕获了前 5 秒，后段启动提示未被捕获；使用后续实际服务/传输状态和广播观察确认启动，没有补造缺失的启动日志。

15:05:29 的新记录显示手机发起 Pairing Request，完成 Confirm/Random 交换后，板端发送 Pairing Failed，随后远端断开。此次已经不只是手机 0x09 退避；旧固件未打印发送方向的失败原因，无法区分确认值校验失败与密钥分配失败。

用户要求放开设备接入限制后，统一源码作以下修正：

- 集成配置的配对记录从 1 增至 8；同时连接数仍为 1，按单个前端事务运行。
- 新增默认关闭的 `CONFIG_BLUETOOTH_RECYCLE_IDLE_KEYS`，仅允许单连接配置启用，K7 集成配置已启用。记录满时轮换回收未连接设备的记录，清除所有密钥链表引用和内容，保护当前连接的密钥指针及设备地址。被回收的旧客户端下次需重新配对；不是无限保存配对记录。
- 修正原分配器遇到空槽就返回的行为：先搜索所有已有设备，避免前面的空槽导致后面的已配对设备被重复分配。
- 发送方向的 Pairing Failed 增加原因码日志；新连接和断开时清除旧连接的加密显示状态，避免历史成功误导诊断。

实际 `bt_keys.c` 使用平台桩运行 ASan/UBSan 测试，覆盖默认不回收策略、记录满、回收后的所有链表清理、密钥内容清零、活动连接指针/地址保护、连续超过容量的设备接入及空槽后的既有设备查找。两种策略测试均通过。测试代码在 `tests/bluetooth/test_pairing_pool.c`。

第一次完整编译输出沿用了旧缓存 `.config`，配置核验失败后未上板。改为独立目录 `cmake_out/velavision_phone_pairing_20260909` 重新构建并确认新配置。最终证据：`evidence/build/phone-pairing-20260909/verification.json`；固件 SHA-256 为 `b22d8cd9baeecd0894176064a764f3c49ddb8fdef36e9314cc9e7a008b9344be`。RAM 启动和手机结果以 `evidence/phone-pairing-20260909/` 的后续实际记录为准，编译通过本身不代表手机故障已消除。

以下为修改前现场记录。

当前固件仍为 RAM 中运行的 `credentials-20260909`。本次没有重启、替换固件或执行 Wi-Fi 联网，也未启动云台或刷写存储。

## 已确认的现场结果

用户使用“蓝牙调试器”，出现配对确认框，但连接后很快断开。取消手机旧配对后仍复现。

15:02:14 至 15:02:19（北京时间）的完整状态记录在 `evidence/credentials-20260909/frontend-live-observation-02.jsonl`：

1. 连接事件成功，板端发送 SMP Security Request。
2. 手机执行 GATT 服务/特征发现。
3. 15:02:16 手机发送 SMP Pairing Failed，reason=9，即 `BT_SMP_ERR_REPEATED_ATTEMPTS`（0x09）。
4. 15:02:19 控制器报告断开 reason=19（0x13，远端终止）。

本次未记录到加密成功，也未进入应用层命令处理。手机界面的“已连接”仅能证明链路曾建立，不能证明配对、加密或收发服务已就绪。当前问题发生在安全协商阶段；不能归因于没有发送 X，也不能把此前 Windows 验证当作 Android 长连接通过。

错误码含义与 Ubuntu SDK 的 `nuttx/wireless/bluetooth/bt_smp.h` 一致。0x09 说明这次手机因重复尝试拒绝配对；尚不能从这个错误反推最初几次失败的原因。

## 设备状态与后续检查

本次串口状态：`host_mode=1 fault=0 tx=637 ACK=637 RX=1461`；配网服务 registered=1、connected=0、busy=0、commands=4。四条命令来自此前 Windows 验证，没有新增手机业务命令。详见同目录 `phone-host-status-01.bin` 与 `phone-provision-status-01.bin`。

当前配置 `CONFIG_BLUETOOTH_MAX_PAIRED=1`。SDK 密钥池没有空槽时返回 NULL，配对分配失败返回 0x08。此前 Windows 客户端是否仍占据唯一槽尚未读取确认；这是一项待核对的设备端限制，不能当作已经证实的此次根因。本次手机在发出完整新配对请求前就回报 0x09，尚未走到此分配路径。

暂时停止自动重连和连续配对，待手机重复尝试限制解除后，再捕获一次完整的新配对过程。关闭再开启手机蓝牙可能有助于恢复状态，但不保证清除系统的配对退避计时。若仍在限制中，应继续等待，避免密集重试。

连接与加密稳定后，才继续选择自定义 Write / Notify 特征并测试 X；UUID 与操作见 `frontend/前端蓝牙联调操作手册.md`。

## 本次工具变更

`tools/watch_frontend_serial.py` 增加白名单状态行：SMP 操作码/失败原因、加密状态、ATT 操作码和 HCI 命令完成状态。只读 COM8，不记录密钥、ATT 内容或密码。旧记录保留；第二次观察窗口为 15:01:29 起 1800 秒，PID 保存在 `private/frontend-watch-02.pid`，到期自动停止。

手机持续连接、扫描及信息接收仍待现场验收，本报告不宣称已修复。
