# 代码、日志与验证对应表

2026-09-14 18:36：新增默认关闭的 `EXAMPLES_K7CLOUD_RADIO_READINESS`。`k7radio_get_link_snapshot()` 以锁保护返回 DHCP-backed generation/Wi-Fi/IPv4 快照；cloud radio bridge 对断链、旧代、同代矛盾和非法 IPv4 fail closed，只更新 Wi-Fi/IPv4 两门。O0/O2 新增 radio bridge 6 组且全套通过，runtime sync 严格编译通过。该增量尚未 SDK 同步、ARM64 整包构建或上板；其余六门仍关闭。证据 `evidence/k7cloud-radio-readiness-20260914/host-verification.json`。未操作硬件、未保存 Token、未提交远程。

2026-09-14 18:23：`ble-wifi-cloud-speech-orchestrator-20260914` 增加离线 `k7cloud status`、`mimo-profile`、`speech-status` 入口和独立编译配置。云语音 22 个精确文件经 SDK 前驱哈希审计后同步，全新 ARM64 构建 1809 步成功；`.config` 三门均为 y，ELF 含五个关键符号，`nuttx.bin` 2,778,832 字节、SHA256 `865390c1e9080fd22ca722f63cdd49f07146a87e1df998c86e39d95d42375ef7`。全量同步仍保护性拒绝 `voicelink/native_asr_runtime.cpp` 的未知差异，未覆盖。前端 26、主机桥 16、k7cloud O0/O2 回归均通过。此版本仍未接真实 radio/DHCP、熵源、可信时间、CA、TCP、API readiness 和 Token 注入，未打包上板、未联网请求、未写 eMMC/STM32、未启动云台、未提交远程。证据 `evidence/build/ble-wifi-cloud-speech-orchestrator-20260914/verification.json`。

2026-09-14 18:27 AI 日志：最终手动刷新三个显式项目主会话，原版校验器 16 文件/35,571 事件 `ALL OK`，可提交快照已更新。秘密扫描发现旧常驻 watcher 未加载后来增加的 `tp-` Token 脱敏规则；核验其命令行后将 PID 29776 替换为同脚本 PID 43616，重新导出后用户 MiMo key 及历史调用命令均已脱敏。新增脱敏回归，与 hook 范围/分日写入合计 10 项通过；覆盖合成 MiMo/OpenAI Token、Bearer、私钥、已知密码转义和媒体。实时数量继续增长，以 `logs/maomaojiang01/manifest.json` 为准；旧 15 文件/30,747 事件只保留为历史。未上传日志。

2026-09-14 18:10：新增 `docs/模型接力交接_20260914.md`，统一记录换模型后的产品主线、当前 RAM 板端状态、前端/主机/板端源码入口、业务与音频合同、OTG 恢复、安全边界和后续验收顺序。同步修正 `logs/README.md` 中已经过时的“两会话/hook 未验收”说明。AI 日志实时入口为 `logs/maomaojiang01/manifest.json`；交接时为 6 个会话且当前会话仍在 30 秒 watcher 中增长，最终数量不得引用旧的 15 文件/30,747 事件快照。收尾仍需手动刷新显式主会话、人工查密钥并运行原版校验器。本次仅文档和日志说明更新，未操作硬件、未调用远端 API、未提交远程。

2026-09-14 09:37：`tts-spoken-flow-arena-pathfix-20260914` 已通过一次 116,524,928 字节 OTG 单包在 RAM 启动；三组模型 CRC 和 Sherpa 的已挂载 VITS 路径校验通过，随后 ONNX Session 初始化仍以 `std::bad_alloc` 返回 `-5`。失败后 `k7mem status` 显示模型池已初始化并完整回收到 536,870,144 字节，无泄漏。新候选 `tts-spoken-flow-arena-pathdiag-20260914` 将分配记录容量从 2048 调至审计上限 4096，并加入 `reason/request/live/count/peak/quota` 首次失败统计；运行库 SHA256 `f453afafc6bb83723779167b9068a4a906438b29b0625087645d92904f3aa2cd`，固件 SHA256 `8528072c2713b2928abf13a040ed450482ca279afda32c75e6fda0abe0ad6ee9`，116,525,024 字节 RAM-only 包 SHA256 `87b92835c550ed3940ef4fb676bc54ad6f9255f1e748574f498862c3ab28b043`，全部静态门禁通过。当前仅因 Ubuntu `/dev/ttyUSB0` 返回 I/O error 尚未重启进入 Fastboot；未写 eMMC、未调用云端 API、未保存用户密钥。

2026-09-14 09:07：MiMo v2.5 非流式 ASR/TTS 官方 Chat Completions profile 已迁入 `app/k7agent/cloud/`，由默认关闭的 `EXAMPLES_K7CLOUD_MIMO_V25` 控制，不包含 endpoint 选择、凭证或自动网络请求。正式 `tests/k7cloud/run_host_tests.ps1` 在 O0/O2 各通过 DNS 9 组、HTTPS 9 组和 MiMo 12 组；两个新增源文件用现有 K7 工具链隔离 ARM64 编译通过，结果见 `work-in-progress/mimo-cloud-client-20260913/arm-result.json`。没有调用 API、没有读取/保存用户密钥、没有改 SDK、没有操作板子。合并配置 22 项静态门禁通过，但当前 OTG 单包距既有上限仅 915,584 字节，正式 combined 构建后必须重新检查包大小。

2026-09-13 21:43：`tts-spoken-flow-arena-gated-20260913` 单个 RAM-only OTG 包已完成哈希、布局、唯一入口、异常表和 ASR/TTS 共享模型池锁验证；板子已进入 U-Boot Fastboot，decoder 与 encoder tail 的保留区 CRC 分别为 `2af64aa7`、`b5f7ae2d`。Windows 当前暴露 `USB download gadget` 错误码 28，Ubuntu 尚无 `18d1:4d00`，故 116,524,928 字节包尚未传输。O0/O2 文本配网 20 场景、DNS 9 组、HTTPS 9 组、MiMo 通用客户端 6 组已复核通过；均不替代真机 TTS/ASR/联网验收。当前状态与串口日志哈希见 `evidence/tts-spoken-gated-preflight-20260913/current-state.json`，未刷写 eMMC、未使用真实 API key、未远程提交。

2026-09-13 17:37：app/k7sound/pio.c 修复 CSR 宏三目参数优先级；O0/O2 旧代码断言失败、新代码通过，独立 ARM64/ELF 通过并 RAM 实测 TXCR=00400fff，用户确认喇叭有声。麦克风唤醒识别错误，完整配网未通过。对应 docs/喇叭播放声道回归修复_20260913.md、evidence/speaker-csr-fix-20260913/。没有刷写或远程提交。

2026-09-13 17:11：wakepartial 固件已 RAM 启动，见 `evidence/wakepartial-boot-20260913-170801.json`；OTG 恢复、两段模型与固件 CRC、无线初始化记录见 `docs/OTG恢复与语音复测_20260913.md`。本轮新增 `load_ram.py stage-tail-reset`、`prepare_voice_test.py`、`test_microphone_wake.py`，分别用于 CRC 保护的软件复位重入、无线初始化、单轮实际语音唤醒扫网。不是永久 OTG 修复或完整语音配网验收；AI 日志仍归入同仓 maomaojiang01。

2026-09-10 双核主任务：app/k7smp、K7实验SMP配置/CPU映射、公共PSCI启动错误传播已独立编译；smp-diag-20260910真机两A53固定核各10000采样、零错位、共享计数20000、定时休眠通过。第一次编译缺IPI声明、第二次沿用旧Kconfig，日志均保留。随后因最小配置漏BOARDCTL_RESET不能reboot，NSH仍响应；恢复加载一次COM8占用拒绝，无镜像数据写入。smp-reset-20260910已编译，链接cmd_reboot/psci_sys_reset/up_systemreset并通过镜像校验，尚未上板。详见 evidence/smp-diag-20260910 与 evidence/build/smp-reset-20260910。没有将双核诊断记为外设SMP或八核验收；当前等待RESET恢复无线。

2026-09-10 最新验收：`emmc-vfs-20260910` RAM镜像 SHA256 `06320f4b86326ea91d682fb6192dad5c64f7893279f9d73ed57490f80b9fe10b`，只读VFS连续64KiB和跨扇区4字节读取均返回0；GPT/16节点及块接口检查通过。BLE真配网IP10.3.0.214、网关5/5、30秒保持通过，客户端主动断开，Wi-Fi在线。证据 `evidence/emmc-vfs-20260910/device-state.json`。没有写盘或挂载文件系统，非完整模型文件读取验收。

2026-09-10 eMMC块设备：emmc-block-20260910真机注册16节点，geometry/拒写/边界/分区地址转换通过；BLE配网、5/5网关与30秒保持通过。普通dd打开返回ENXIO，SDK源码确认缺CONFIG_BCH。独立emmc-vfs-20260910仅增加BCH与BCH_DEVICE_READONLY，SDK差异审计及编译校验通过，正在RAM加载。对应evidence/emmc-block-20260910、evidence/build/emmc-vfs-20260910；未写盘、未挂载文件系统。

2026-09-10 日志写入修正：`tools/log_daily.py` 跳过内容不变的历史文件，采集进程已重启加载；`tools/test_log_daily.py` 验证跨日追加不改变旧文件和旧目录修改时间。实际刷新核对及官方校验结果见 `evidence/log-unchanged-write-verification.json`。

2026-09-10 原生 eMMC 真机：`emmc-readonly-20260910` 初始化及两扇区重复读取通过；BLE真配网/网关5/5/30秒保持回归通过。证据 `evidence/emmc-readonly-20260910/`，对应构建哈希 fd1462a94c3fad9692cbdd6e3d9fe3bbfb4f4920220eb12997b1a22a201881fe。GPT解析增量正在独立 emmc-gpt-20260910 构建，不能复用前一镜像的实测结论。没有写盘。次会话已接续 parallel-wifi-service 候选，中央日志仍由本会话统一核对。

2026-09-10 VoiceLink 交接接收：主会话核对候选 17 个交付文件和 14 个输入哈希、锁定头文件及已通过测试报告，见 `evidence/voicelink-handoff-20260910/verification.json`。未应用补丁或上板。并行主会话 01a0892e-2964-7ec3-bd76-9ab4937980cb 已显式入日志源，首次 95 条；统一官方校验通过。接口差异及接入顺序见 `docs/VoiceLink交接接收与集成顺序_20260910.md`。

2026-09-10 eMMC 固件接入：核心源码迁入 `app/k7emmc/`，新增初始化状态机、低速平台配置、复位和受限命令接口；四套主机测试通过。`emmc-readonly-20260910` 独立编译、链接与镜像范围校验通过，正在 RAM 加载，尚未声明真机读取成功。构建见 `evidence/build/emmc-readonly-20260910/verification.json`，说明见 `docs/eMMC原生只读接入_20260910.md`。本主会话日志归属 maomaojiang01；VoiceLink 并行目录未改动。

2026-09-10 eMMC 命令层增量：新增 `work-in-progress/emmc-readonly/sdhci_command.{c,h}` 和模拟控制器测试，补 CMD17、R1 检查、地址边界、共享截止时间与失败恢复门禁。`tools/test_emmc_readonly.py` 同时执行接收层和命令层测试，证据见 `evidence/emmc-source-review-20260910/host-command-test.json`。未操作硬件、未构建固件；卡初始化和平台接管仍待实现。本主会话日志统一在 logs/maomaojiang01，未修改并行 VoiceLink 工作目录。

2026-09-10 eMMC 增量：`work-in-progress/emmc-readonly/` 新增有界单块 PIO 接收核心和主机故障注入测试；`tools/test_emmc_readonly.py` 严格编译并运行通过，源码哈希/命令/结果见 `evidence/emmc-source-review-20260910/host-test.json`。仅主机测试，未集成 SDK、未构建或加载新固件。官方 SDK 四个参考 blob 的校验与静态发现见同目录 manifest.json 和源码审查.md。对应本主会话真实 AI 日志，统一归属 logs/maomaojiang01。用户已另开 VoiceLink 会话；本轮未改其暂存目录。

2026-09-10最新：model-arena-20260910已RAM上板；专用1GiB模型池创建成功，384MiB分配/98304页两轮稀疏读写/释放恢复通过。BLE真配网IP10.3.0.214、网关5/5、30秒保持通过；客户端主动断开，Wi-Fi在线。系统堆仍原范围、CPU仍单核，eMMC未写。见docs/独立DDR模型内存池_20260910.md与evidence/model-arena-20260910/。

2026-09-10：model-arena-20260910独立编译/链接通过，新增显式1GiB CPU模型池和k7mem稀疏测试入口，未上板。原系统堆/DMA范围未扩展；CPU仍单核，八核规划见docs/八核CPU接入与任务分配_20260910.md。代码/构建见docs/独立DDR模型内存池_20260910.md；当前无线固件未替换。

2026-09-10 10:03恢复完成：用户重新上电后已重载wifi-arp-20260909，实际镜像哈希9dc19c97c2d2aa5f49bc3bca36d04f026c25c096ec3894605ba7ac4d7dc4ce5d；Windows BLE真配网回IP10.3.0.214，网关5/5。客户端主动断开，Wi-Fi在线、BLE服务保留。没有重复mmc part，没有写eMMC，DDR扩展尚未启用。证据evidence/resource-recovery-20260910/。

2026-09-10 资源接入当前状态：已完成64位DDR审计工具及8项测试，实测eMMC为29.1GiB；U-Boot执行只读mmc part后无回应，Wi-Fi/BLE未恢复，等待断电重上电。未写eMMC、未启用DDR扩容。见docs/DDR与eMMC资源接入_20260910.md。

2026-09-10 最新：wifi-arp-20260909 已 RAM 上板；Windows BLE 真配网、WPA2/DHCP、真实 IP 回包通过，IP 10.3.0.214。空 ARP 缓存起步网关 5/5（含首包），BLE/Wi-Fi 60 秒保持通过。测试客户端已主动断开，Wi-Fi 保持在线。仅本轮有限验证，非长期稳定或互联网验收。见 docs/WiFi上电复验_20260910.md。

Wi-Fi 完善增量：ARP 等待 20→250 ms、ARP 收发计数、最近 PROV 请求结果；新固件 `wifi-arp-20260909` 编译和协议/桥接测试通过。前端在线扫描保护及 12 项测试通过。COM8 无回应，新固件尚未上板；见 `docs/WiFi完善与ARP等待调整_20260909.md`（统一源码入口根目录下）。

20:17 BLE 真配网已 RAM 启动并验证：`ble-connect-20260909`，connect:true；Windows BLE 请求 id 9101 完成原生 WPA2/DHCP 并回真实 IP，status 一致；网关首轮4/5仍有首包ARP问题。源码/协议/桥接/前端测试见 `evidence/build/ble-connect-20260909/`；真机与当前可扫描状态见 `evidence/ble-connect-20260909/`。同一主会话日志，未远程提交。

新增 BLE connect 接入：`prov_wifi.inc`、动态 SSID 选择、真实 status、前端 connectWifi()；协议/桥接内存检查测试、11 项前端测试及固件编译通过。新固件 ble-connect-20260909 正在 RAM 加载，尚无端到端真机通过结论。构建见 `evidence/build/ble-connect-20260909/`，对应当前主会话日志。

2026-09-09 本轮验收收尾：手机确认 10 次重连每轮收到列表和 scan_done，合并结论在 `evidence/stability-20260909/reconnect-scan-run-01/acceptance.json`，原观察器结果保留。30 分钟共存 1497/1500，零丢包未通过；随后 Wi-Fi 恢复阶段出现 ARP/网关失败，19:54 复查 5/5，同时发现 BLE reason 8 超时，用户确认 App 前台且在附近。两项后测异常在 `post-test-anomalies.json`，无已确认根因或固件修复。新增只读/有界 `tools/probe_wifi_gateway.py`；未重启、未刷写。日志归属本主会话和 `logs/maomaojiang01/`。
19:36:59 共存长测完成：1800.11 秒、1497/1500；BLE/Wi-Fi 连接保持通过，严格零丢包未通过。随后主动退出 Wi-Fi 开始独立 X 扫描/10 次手机重连采集，尚未验收。观察器新增 NSH 提示符处理，合成测试共 8 项通过；无固件修改。证据及结果见 `evidence/stability-20260909/`。

19:06:59 启动手机 BLE＋Wi-Fi 共存保持测试，证据 `evidence/stability-20260909/coexist-run-01/`；尚未完成，不计作通过。新增 `tools/observe_ble_reconnect.py` 及 7 个合成观察器测试，仅准备后续手机重连采集。`tools/summarize_wireless_stability.py` 增加独立 BLE/Wi-Fi 保持结论，严格综合零丢包标准保留。详见 `docs/蓝牙共存与重连验收_20260909.md`，对应当前主会话；无固件修改。

2026-09-09 官方赛道对齐：新增 `docs/官方硬件赛道对齐与目标_20260909.md`，核对官方 K7 待适配状态、代码交付与 Agent 定位，修正 README/目标文档中的旧无线状态。本轮无固件改动或新增真机验收；对应当前主会话。累计日志以同仓 manifest 为准，下方数量保留为历史快照。

最新长测：`tools/run_wireless_stability.py` 与 `tools/summarize_wireless_stability.py` 对应 `evidence/stability-20260909/wifi-run-01/`。1,800.11 秒，1,500 发/1,499 收，Wi-Fi 保持在线；严格零丢包未通过，手机 BLE 未连接，不能计作蓝牙长稳通过。本轮无线固件未变；Agent 仅完成部署条件核对和配置模板，未上板。日志仍归属本会话和 `logs/maomaojiang01/`。

日志持续采集修正：当前主会话长轮次不会触发 Stop，新增 `tools/watch_project_logs.py` 每 30 秒按源文件变化刷新当前会话，延续按日分文件、连续 seq 和脱敏策略。运行信息及最新官方校验在 `private/log-collector/watcher-status.json`；此变更不涉及固件或硬件。

旧主会话分日更新：`01a06679-4033-7993-b989-6269e03e0e70` 的 7,167 条记录也已按北京时间拆分到真实事件日期目录。按日期重新拼接的 SHA-256 与拆分前完全一致，序号 0–7166 连续；详见 `evidence/old-session-daily-split.json`。原版官方校验通过，后续手动刷新沿用分日规则。

最新 `wifi-amsdu-index-20260909`：首次网关 40/40、实际 A-MSDU 1 组 2 子帧交付通过，Lansee WPA2/DHCP 成功；证据 `evidence/wifi-amsdu-index-20260909/`，源码、软件测试和构建对应 `evidence/build/wifi-amsdu-index-20260909/verification.json`。当前主会话日志仍归属 `logs/maomaojiang01/`。手机 BLE 保持加密连接期间网关 50/50、无新增断开也已记录；正式前端 connect、小时级稳定性与 BLE 业务并发吞吐未验收。下方保留历史。

日志目录规则更新（2026-09-09）：当前开发主会话 `01a07ed4-3f0d-7450-8bb1-bb756849cb4e` 已按北京时间分日，9 月 9 日事件在 `logs/maomaojiang01/2026-09-09/codex__01a07ed4-3f0d-7450-8bb1-bb756849cb4e.jsonl`。9 月 8 日事件仍保留原目录；跨日 seq 连续，manifest 合并累计。手动导出和自动 hook 共用 `tools/log_daily.py`，原版官方校验通过。

2026-09-09 最新无线进展：原生 Lansee WPA2 四次握手通过，证据 `evidence/wifi-auth-20260909/lansee-auth-01.jsonl`；运行固件及源码对应 `evidence/build/wifi-auth-20260909/verification.json`。IP/DHCP 为后续独立开发，不计作联网通过。对应当前主会话，日志归属不变。详见 [认证与 IP 接入](WiFi认证与IP接入_20260909.md)。

最新修正：`phone-rpa-20260909` 针对手机重新配对 0x04，分离连接时随机地址与身份地址；实际地址选择函数回归测试和独立编译通过，正在 RAM 加载。见 [手机重复配对地址修正](手机重复配对地址修正_20260909.md) 与 `evidence/build/phone-rpa-20260909/verification.json`。对应当前主会话；首次手机连接成功不等于反复配对和稳定性验收通过。

手机业务联调补充：用户确认连接稳定，要求继续“X → 真实扫描列表 → 提交 Wi-Fi 信息 → 已收到回执”，不要求联网。当前镜像已有此协议，本轮未更改固件；新增 `frontend/手机调试器操作卡.md`、`手机调试HEX分包.txt` 及仅含测试密码的生成脚本，更新操作包。现场业务收发仍需手机开启 Notify 并发送命令，不能把资料生成当作手机收发通过。

手机配对修正 `phone-pairing-20260909`：统一配置、`port/tracked/nuttx/wireless/bluetooth/bt_keys.c`、`Kconfig` 和 `app/k7radio/skw_bt.c`；对应当前主会话。实际分配器测试及独立固件编译通过，证据 `evidence/build/phone-pairing-20260909/verification.json`。记录回收与现场排查说明见 [手机蓝牙断连排查](手机蓝牙断连排查_20260909.md)，手机连接验收仍以新镜像现场记录为准。

2026-09-09 手机联调新增：[手机蓝牙断连排查](手机蓝牙断连排查_20260909.md)。仅修改只读串口观察工具，新增手机 SMP 0x09 / 断开 0x13 现场证据；固件未变，Android 长连接尚未通过。对应当前主会话与同仓 `logs/maomaojiang01/`。

最新范围：用户要求先完成信息接收、不要求联网。`credentials-20260909` 已编译、RAM 启动和真机验证真实扫描/接收回执；证据在 `evidence/credentials-20260909/`，对应当前主会话。源码、测试和前端说明见 [本轮开发记录](配网信息接收开发_20260909.md)，下方为历史快照。

最新无线增量见 [Wi-Fi EAPOL 接收进展](WiFi-EAPOL接收进展_20260909.md)：对应当前主会话，源码/固件哈希与测试输出位于 `evidence/build/eapol-rx-20260909/verification.json`。下方首次统一版本源码清单和 80 项一致性为历史快照，不代表本轮改动后的再次验收。
统一仓库：`E:\openvela\VelaVision`；日志归属：`logs/maomaojiang01/`。

2026-09-09 日志采集配置更新：新增 `tools/auto_collect_logs.py`、`tools/install_log_hook.py` 和范围测试；对应真实会话 `01a08456-0276-7772-b214-0a86086f5974`。已刷新两个历史主会话并合并第三会话；下方数字为此前集成快照，最新数量/哈希在同仓日志 manifest。自动入口已安装、直接调用验证通过，已信任启用且 Codex CLI 自动触发验收通过；已打开的桌面任务需重新加载，详见 [自动日志采集](自动日志采集.md)。本次不改变固件或硬件验证结论。
## 日志覆盖
- 旧主会话：7167 条，2026-09-03T08:53:37.460Z 至 2026-09-08T02:21:45.985Z。
- 当前主会话：3569 条，2026-09-08T02:23:54.126Z 至 2026-09-09T04:09:14.478Z。
- 两个会话各自 seq 从 0 连续递增，不串改日期或把两个会话改成一个会话。
- 当前快照不含导出之后的新增消息；运行更新脚本可刷新。未重复合并子任务继承历史，未恢复内部推理。

## 代码到日志索引
下表是对真实日志中的路径/名称引用检索，不是补造逐次 Git 提交或宣称每条引用都代表代码修改。详细锚点在 `evidence/code-log-map.json`。
| 模块 | 源码位置 | 旧会话引用数 | 当前会话引用数 |
| --- | --- | ---: | ---: |
| BSP / USB / 平台 | `board/kickpi_k7; port/new/nuttx; port/tracked/nuttx` | 367 | 142 |
| 人脸 / 姿态 / 三视角拍照 | `app/k7host; host/vision` | 709 | 47 |
| 云台 / STM32 | `app/gimbal; mcu/stm32-v1.3` | 545 | 91 |
| NPU | `app/k7npu` | 141 | 82 |
| Wi-Fi / BLE / 前端 | `app/k7radio; frontend; work-in-progress/wifi-link` | 0 | 523 |
| 旧 PC / 仪器模型 | `legacy/pc-delivery` | 201 | 31 |
| 统一构建 / 日志 | `tools; logs/maomaojiang01` | 23 | 54 |

## 版本与验证证据
- `evidence/source-files.json`：本仓源码/模型/脚本的逐文件 SHA-256，识别当前实际代码。
- `evidence/sdk-inventory.json`：来自 VM 的文件路径、SDK Git 基线及最初取回哈希。
- `evidence/accepted-source-comparison.json`：80 项旧验收源码与统一目录全部一致。
- `evidence/build/integrated-build.json`：同一 ELF 中四个应用入口、配置和固件哈希；仅编译验证。
- `evidence/log-snapshots.json`：两个原始会话快照哈希/截止时间、事件数量、脱敏与排除项。
- 每条 JSONL 的 `metadata.source_line` 与 `source_record_sha256`：定位原始会话记录；`seq` 定位交付日志。
- `evidence/official-log-validation.json` 与 `.txt`：未修改官方校验器的实际运行结果。

历史没有逐次提交，不能证明不存在的 commit 对应。今后从此统一仓库产生代码提交，并同步同仓日志和版本清单。文件/格式核对通过，不代替联合硬件验收。

2026-09-10 VoiceLink 接入核对：外部语音目录源码只读评审，5组核心/解析主机测试重新编译执行通过；板端音频、sherpa/ONNX Runtime、共享Wi-Fi接口与内存规划待做。未修改运行固件；见 docs/VoiceLink接入评估_20260910.md、evidence/voicelink-review-20260910/review.json。归属当前主会话日志。

2026-09-10 用户确认4GB DDR/32GB eMMC：核对当前126MiB映射、DTB/固件保留区及未启用MMCSD，形成docs/DDR与eMMC资源接入_20260910.md。只读评估，未改写运行固件或存储，归属当前主会话日志。

2026-09-10 SMP增量：app/k7smp、ARM64 CPU启动返回值及RK3576 MPIDR映射、独立2/4/5/8核配置均有独立构建和真机证据。八核最终证据见 evidence/smp-eight-20260910/cpu-acceptance.json；三次主机串口参数错误和首版缺reboot均保留。不将单核无线或主机语音结果计算为八核外设验收。归属当前主会话，日志须刷新后以manifest与官方校验为准。


2026-09-10后续实测：smp-service-20260910 已RAM上板。8核诊断通过，系统/无线线程亲和性0x1已核对；BLE真实配网及60秒保持、BLE加密连接期间Wi-Fi网关5/5、本轮拒收0。当前保留此候选运行，测试客户端主动断开。不是持续计算并发、外设全量或长稳验收；k7_provision栈峰值6688/8112字节，后续共享语音服务接入前应复核余量。见 evidence/smp-service-20260910/acceptance.json。


实际增量：smp-load-20260910 RAM运行，四A72各60秒、1412766批零计算错误，ping45/45；时间重叠核对BLE回包11次、网关回复45次。A-MSDU快照2→5，不满足严格零接收异常，不能归因为计算负载或宣布长稳。首轮后台提示符采集失败未启动计算；第二轮修正主机采集后一次完成，全部记录保留。详见 evidence/smp-load-20260910/acceptance.json。


2026-09-10 原生Agent基础增量：app/k7cxx、K7异常表及首构造注册修复，经cxx-unwind独立构建/ELF检查与真机探针通过；无线真配网/BLE30秒/网关5/5。第一轮TLS_TASK缺失编译失败、第二轮异常表缺失取消加载及无线恢复失败均保留。app/k7agent/model_reader与app/k7neon为已迁入待验收候选，模型与Agent未部署。证据evidence/cxx-unwind-20260910/acceptance.json；日志归属当前主会话，子代理独立记录未宣称完整单独入库。

2026-09-10后续：neon-file64串口中断后恢复，实际off_t64、CPU4/5 NEON短时、BLE配网/IP/30秒和网关5/5通过，A-MSDU拒收1仍保留。另独立cxx-locale运行库构建及真实llama39个ARM64对象、链接/异常表门禁通过，未上板或加载模型。新增三代理第二轮候选48文件哈希已核对，宿主结果不替代设备验收。完整过程和命令见docs/原生llama编译与NEON验收_20260910.md；tools/check_llama_native_compile.py、check_llama_native_link.py、record_neon_acceptance.py对应当前主会话，同仓日志须以最新manifest/官方校验为准。


2026-09-10追加：新增app/k7eh与app/k7ehcontrol保存冷并发异常失败和独立对照；app/k7graph真实2/4线程零权重图与无线回归通过；app/k7agent/tool_api仅目标编译通过。链接v1拼错符号门禁已在v2纠正，原失败/错误记录保留。tools/observe_graph_core.py、observe_eh_control.py、record_graph_acceptance.py对应本主会话，最终日志须重新官方校验。


2026-09-10：app/k7arena经独立构建、实际TU宏/ELF检查和RAM真机通过32KiB/8192项set-get及释放；配套无线30秒、网关5/5通过。证据evidence/arena-provider-20260910/acceptance.json。USB只读MSC、FAT与模型同句柄读取仍是候选，不宣称已枚举U盘或加载权重。来源为当前主会话及候选交接哈希；官方日志须刷新后核对。


本轮真实结果：USB新节点/dev/sda，512字节扇区、60555264扇区，两次LBA0 CRC32=b1241e76且关闭成功；未挂载/未写盘。无线恢复30秒及网关5/5，拒绝1→1保留。VID/PID未输出，严格身份绑定验收尚未满足。见evidence/usb-readonly-20260910/runtime-acceptance.json；当前无持续串口进程。

2026-09-10 用户明确近期顺序为本地语音配网后再接小米云API。app/voicelink迁入6份冻结核心/解析器，增加异步扫描事务、取消/超时、64项上限，并修复连接失败返回选网与可恢复扫描失败后重新唤醒。正式源码O0/O2各14场景通过；全部音频/网络端口为模拟，未接入固件。结果与50份并行音频/扫描交付哈希见evidence/voicelink-core-integration-20260910/stage.json。tools/integrate_voicelink_core.py、test_voicelink_flow.py、record_voice_provisioning_stage.py归属当前主会话；子代理交接不代表独立完整日志已自动采集。此前FAT已编译通过的pending状态同步纠正，未RAM加载。完整待做项见docs/本地语音配网优先实施_20260910.md。


2026-09-10：audio-preflight独立构建及实际ARM64编译单元/ELF/raw校验通过，RAM上板完成8个固定CRU/IOC地址两份快照，无寄存器写、I2C事务或录放音。无线同镜像真实IP、BLE30秒/网关5/5，接收拒绝0→0。证据evidence/audio-preflight-20260910/runtime-acceptance.json；app/k7audio和配套tools归属本主会话，日志按最新manifest/官方校验为准。

2026-09-10 板载音频最高优先级：app/k7audiohw已在audio-io-b镜像真机通过固定codec双次读取与STOP/恢复；app/k7sound集成codec v2/SAI PIO v2与PMU/clock/pinmux，独立编译已通过，录放音实测待完成。CRLF脚本失败和复用构建目录未发现新Kconfig的失败均保留；未加载未通过版本。Windows BLE同版本两次DeviceNotFound，不能宣称无线恢复。所有真实结论绑定各自evidence与artifact，未刷写/云台/远程提交。


2026-09-10 18:42真实音频首轮：audio-sound镜像SHA256 58e5c5ce5a6ffc8b116c75f874ef430f23a7a0192f819ab146484e02b2cf2313已RAM启动，SAI23073576、codec初始化及停止/恢复成功。tone预填充和capture首帧后均返回-71，未取得完整录放音；四字段FIFO求和修正在独立audio-fifo版本准备。原始结果见evidence/audio-sound-20260910/first-attempt.json，失败未覆盖。


2026-09-10 18:54：audio-fifo镜像 741c9b2f8b23f6f247da865201061343cbe36b924404457f82f1d6630ce14020 真机FIFO首读00104104，四字段总16，3200帧短音传输0，用户确认三声可听到；48000帧/3秒采集也返回0但全部为零，录音未通过。数据和用户确认分别保存在evidence/audio-fifo-20260910/runtime-results.json，零样本原始WAV在zero-capture/。下一audio-normal对照只改codec正常功耗并加启动前诊断；可选原始PCM缓冲回放只由显式replay启用，未实测。


2026-09-10 19:05 audio-normal实测：正常功耗寄存器读回成功，但3200帧仍全零；0f=20只比写30少未定义bit4，mute位已清，不判静音。SDI0下拉、PATH=e4e4、GPIO有限采样全低。进一步从精确官方clk-out.c确认独立IOC26046400 bit1主时钟输出门控遗漏，audio-mic补齐该门控与官方pull_none、20ms只读引脚观察，并加入独立reset的ADC对照模式；合并平台两组O0/O2测试、ARM64编译/ELF通过，RAM加载中。见各版本evidence，未宣称麦克风成功。


2026-09-10 audio-mic 已RAM上板，外部MCLK门控从1f变1d；首次捕获真实变化数据，用户配合的48000对原始录音已保存 voice-first。尚未验收：严格每4帧只有phase0非零，其余3帧全零，不能按16k正常录音交付或简单删零。ARM64机器码/编解码器模式审查未发现相应步长或模式错误；独立audio-rxtrace仅加前32次FIFO读前/读后记录，ARM64/ELF通过，当前RAM加载。真实材料见 evidence/audio-mic-20260910/runtime-results.json 与 evidence/audio-rxtrace-20260910/。无线未启动，未写eMMC。


2026-09-10 audio-rxtrace真实3200对采样200040us，前32字证明FIFO0/1/2/3依次每62.5us各提供两字，非空读造成插零，MONO=0。新audio-reset独立镜像已编译/ELF核对，包含互斥capture-clear、capture-reset、capture-clockwait三入口及原始trace；ARM64/主机通过不代表实机修好。RAM加载中；串口恢复只读确认U-Boot，未刷写。默认capture仍保留原路径。


2026-09-10 20:07实际：audio-reset已RAM启动。CLK/FS关闭时start-clear超时并锁住；同镜像RAM重载恢复。H/M复位与20us时钟等待均执行成功但每4帧3零未消失。MIC采集48000对后由板载喇叭原样回放，两端传输/停止返回0，用户明确没有听到声音；随后同版本三次短音用户全部听到。不能验收语音回放。原始PCM及实听结果见evidence/audio-reset-20260910/runtime-results.json；无线未启动，无eMMC写入。下一版仅准备可控DAC音量档位，默认与原始PCM保持不变。


2026-09-10 最大音量测试：用户要求最大音量；app/k7sound增加预备态DAC档位及replay-max/tone-max，合法最大DAC00/OUT2 21，先关闭功放并逐项读回，失败不激活。候选O0/O2模拟检查、正式ARM64构建、ELF及raw一致性通过；image 0a68990a8b36f3e38154959a5ed33f209d0bf5a6c6a825f36ae2c06959246f9f，RAM加载中，未宣称实听成功。原始PCM/PIO不改，见evidence/audio-gain-20260910/integration.json和docs/板载录音回放操作_20260910.md。


2026-09-10 20:22实听确认：audio-gain录48000对并replay-max两次，DAC1a/1b=00、OUT2 30/31=21均真实读回，搬运与停止0。用户回复“听的清楚但是有一些杂声”，证明板载MIC录音→板载喇叭人声回放基础功能已打通；不等同纯净音质/正确16k格式验收，周期性零采样仍在。20:23同段低输出音量对照已执行，等待实听反馈。证据evidence/audio-gain-20260910/runtime-results.json与voice-max-first原始WAV。


用户后续纠正：最大音量有杂音，而且要仔细听才能听清说什么。以上基础回放确认只证明可听/可辨认，不代表清晰可用；音质验收仍未通过。下一步分开做输入PGA增益与SAI接收请求对照。


后续输入增益对照：audio-input-20260910已独立编译/ELF及raw一致性通过，RAM加载中。只新增capture-pga24（ADC09=88、+24dB）与启动前SAI槽掩码只读快照；默认capture与PIO保持不变，RDE候选未集成/未启用，DMA硬件前置状态尚未证明。参考audio-input-root候选O0/O2寄存器差分与失败检查。旧audio-gain音频回放可辨认但用户需仔细听、杂声未解决。

2026-09-10夜间：用户授权持续至9月11日09:00，无需人工确认。audio-input最终反馈为“人声更大，但杂声仍明显”。audio-filter候选冻结哈希核对后迁入app/k7sound，新增逐声道独立输出滤波，不删除或归一化原PCM。正式ARM64/ELF/raw通过，RAM镜像8c8f46b4a354be34e6ff8d3d430cb81f70d583a8a0f929aeed0047ae5f1585fc，200ms采集及一次MA4回放返回0，功放关闭；无人实听，不算音质通过。HP80仅主机通过。真实录音的主机ASR仍有偏差，不算板端语音识别。相同镜像Wi-Fi由私密串口提交认证/DHCP成功，短录音前后网关各5/5；两次Windows BLE操作均DeviceNotFound，失败保留。对应tools/integrate_audio_filter.py、check_audio_filter_host.py、check_audio_filter_asr.py、record_audio_filter_stage.py及record_audio_filter_wireless.py；evidence/audio-filter-20260910保存原始证据。日志刷新与官方校验结果仍以最新报告为准，子代理交付不表示其独立日志完整入库。
# 夜间 21:21 后增量

`audio-loopback-20260910` 静音内部回环完成，128帧/8063微秒，停止及恢复成功，但每四帧三帧为零仍复现；并非数据格式验收成功。`tools/record_audio_loopback_stage.py` 将原始输出与实际镜像哈希对应并更新当前状态。下一独立 `audio-route-20260910` 只增加显式 `loopback-rxall` 路由对照，保留原始默认路径；编译、ELF、raw 检查通过，RAM加载中。主机候选 MMU/共享语音配网后端分别接收15/82项文件哈希，未作为板端集成通过。官方日志最后一次校验通过仅覆盖当时采集内容，后续仍需刷新。

2026-09-11 MMU 诊断接入：app/k7sound 新增只读 CPU0 MMU worker 和有界页表解析，tools/observe_audio_mmu.py 绑定当前 ELF 与 SDK 哈希；audio-mmu-20260910 编译、ELF 异常表/布局/二进制核对通过。构建证据 evidence/build/audio-mmu-20260910，SDK 固定输入 evidence/audio-mmu-input-20260911。此记录时 RAM 加载中，尚无真机 MMU 结果；无线离线。首轮验证先于审计文件生成而失败，审计生成后验证通过。没有刷写、播放或启动执行机构。

2026-09-11 09:33 MMU真机：audio-mmu-20260910 RAM加载成功，CPU0三次页表读取，SAI 0x2a610000为Device-nGnRnE、identity和AF有效；不代替TLB/Stage2全验证。09:34通过私密串口认证恢复Lansee WPA2/DHCP 10.3.0.214，空ARP起步网关5/5，A-MSDU 1组2子帧/0拒收。BLE服务注册、客户端未连接，未声称GATT通过。见 evidence/audio-mmu-20260910/runtime-results.json；杂声未修复。

2026-09-11 RX2：app/k7sound/duplex及入口单变量候选通过独立编译、ELF/二进制校验及RAM实测。默认/双路取数8062/4061us、零字194/131，CSR恢复0；09:45同镜像真实WPA2/DHCP网关5/5。不是音质修复。源码、构建与实测分别见app/k7sound、evidence/build/audio-rx2-20260910、evidence/audio-rx2-20260910。日志仍归属logs/maomaojiang01。

2026-09-11主线转纯板端语音配网：audio-noise-20260911独立worktree已复制52文件带hash，不提交、不触设备。共享无线后端迁入app/k7radio，首次SDK冲突wd_init与NuttX看门狗重名，已将9文件wd_接口改为k7wd_，保留attempt2-build.log与前驱hash；voice-shared-20260910原生编译/ELF校验通过，BIN54cab65d42099a8ab0c5edc1e03eac08b6dee017d8422c0c4f469028d02c4fbe未加载。nativeWifiPort适配器及k7voice原生诊断入口正在voice-native版本编译，ASR/TTS未集成，不能称语音配网成功。

2026-09-11 voice-native：真实原生编译、应用注册、ELF及RAM启动通过；BIN 0349c64f59a8634ddea64d65598ec03190edade02a4a056c98baccf763be52a4。wifi-service-start在首次OPEN之前发送STOP，模组BSPASSERT port.c-237，VoiceLink probe native_network=0，未执行扫描。失败原始输出保留于 evidence/voice-native-20260910/native-start-20260911-102931。voice-cold独立修复冷态初始化，后续操作清理栅栏保留，尚未验收。官方日志本轮13文件/19740事件通过，仅覆盖该次采集。

2026-09-11 voice-cold：冷态服务启动和probe通过，scan接受后35秒超时；真实ps无后台pthread，host fault=0，Wi-Fi离线。独立voice-owner将pthread移入持久任务组，编译首轮仅缩进Werror失败，保存完整日志及hash后修复；现RAM加载中。原生ORT Env v2实际ARM64单源编译通过，common仍有3个源编译缺口，不能称运行库链接或ASR成功。

2026-09-11 voice-owner：BIN 048bbb796aa6a2e90c032049c7b2eac1c26954c6901359d7f58bf6b98e6bc1e8 RAM启动通过，独立k7_wifi_svc持久任务在命令退出后仍存在，三次真实VoiceLink接口扫描通过；后两次2.922/2.938秒，各64项含Lansee，host fault=0，Wi-Fi未连接，BLE仅服务启动未测客户端。ASR/TTS未集成。ORT common共21个源全部实际编译成AArch64对象，组合证据记录每个对象哈希；静态依赖链接和模型运行未完成。

2026-09-11 原生语音运行库继续：static-deps4的138步/30个静态库编译通过；link-audit3正常archive抽取，21 common对象可重定位链接通过、172外部strong符号有SDK/libm/libgcc定义，非最终固件链接。session-config9真实Iconv编译归档、算子生成及完整NuttX CPU配置通过；749步构建进行中，ASR/TTS未运行。对应tools/build_native_voice_static_deps4.py、audit_native_voice_link_closure3.py、stage_native_ort_session.py、configure_native_ort_session9.py、build_native_ort_session.py。所有失败轮次保留，没有更换板端voice-owner镜像。

2026-09-11 原生运行库增量：ORT 1.17.1完整CPU Session静态库编译通过；真实nsync及NuttX localeconv补齐后按需链接缺失强符号0。voice-ort-add-20260911正式独立固件编译成功，BIN SHA256 522047a5f8d6197ea0285227e581f3bce06e04ed23fb101af3dadc61b4f6fdd2，10,514,736字节；运行末址0x4129a000，异常表17477 FDE及首项注册静态检查通过。该版本仅显式Add诊断，尚未运行，不代表ASR/TTS。主会话已安全重启至U-Boot并开始CRC门禁RAM传输，没有刷写。
ASR配置2采用kaldifst1.7.17/OpenFST2024-06-19和Eigen实际C++11探测，通过；真实库编译开始。第一次传输因密钥权限失败、第二次归档路径未规范化被哈希门禁拒绝，均未伪造成功，修正后配置通过。
空间清理：仅删除73个历史构建目录中90,700个可再生成对象文件；893个固件/归档/配置等保护文件哈希全部不变，Ubuntu根分区可用从约1.3GiB恢复约21.2GiB。证据evidence/vm-space-cleanup-20260911；源码、SDK工具链、模型、原始日志未删。以上AI日志需后续真实导出并运行官方校验器。

2026-09-11 ASR实际编译推进：Sherpa1.12.14 C API第二轮198步全部成功，耗时248.017秒；KissFFT仅修复两个宏的命名冲突，普通日志语义六项预处理结果保留。未禁用原编译检查。真实Recognizer入口按需链接成功，396个外部强符号中SDK仅缺popen/pclose，暂未最终固件链接。该符号审计仍使用Add-only ORT，不代表ASR模型可用。28项ASR算子注册已另目录生成并配置成功，正在重新编译ORT；生成器首轮缺FlatBuffers Python导入路径，复用锁定源修正后通过，无新包安装。当前Add固件RAM传输进行中，尚未启动；不作板端ASR/TTS运行声明。
本轮真实日志导出：13文件、20,879事件、6会话，原版官方校验ALL OK；校验只覆盖该次采集时点，后续开发事件下次继续导出。


2026-09-11 14:20 接续：voice-ort-add RAM 启动成功；真实 Add 命令60秒无结果，随后ps及Ctrl+C无字节返回，不能判断具体卡点，已请求用户RESET。旧原始日志保留 evidence/voice-ort-add-20260911/。新增逐调用BEGIN/END并flush的独立探针 native-ort-add-trace，原模型和线程数不变；原生编译及按需链接缺失强符号0，voice-ort-trace独立固件编译成功，镜像静态校验进行中，未上板。
ASR完整ORT库及VITS Lexicon-only库已编译通过；ASR+VITS真实C API双入口联合链接393个外部强符号全部有SDK定义。算子注册仅一个TU改变，已单独编译替换并核对旧归档不变。上述均不代表真实ASR/TTS模型运行。新加载器候选仅在旧4KiB块CRC精确匹配时跳过相同8字节写入，仍须每块及完整镜像CRC通过；尚未用于实板。未新增子代理、未刷写、未运行执行机构。

2026-09-11 当前传输期间：正式app/voicelink/src/parsers.cpp新增反斜杠/反斜线及正斜杠/斜杠单字符规则；tests/test_flow.cpp新增解析和状态机取消清理场景，16项O0/O2通过（run-20260911T065202344226Z），native-voicelink-parsers-compile1实际ARM64编译通过。当前传输镜像不包含本次规则修改。固定提示音22条离线资产生成和16k PCM转换完成，未上板播放。模型缓冲区读取器20项O0/O2及原生编译通过，未替代文件系统/模型Session实测。

2026-09-11 ASR decoder Session 候选：新增有界 SHA256、持久模型池复制、显式顺序 Session 诊断，真实语音运行库可重定位链接与独立 voice-asr-model-20260911 固件构建通过。源码见 work-in-progress/native-asr-model-session，正式入口 app/voicelink；构建证据 evidence/build/voice-asr-model-20260911。尚未上板或运行模型，不代表语音识别通过。本轮日志刷新13文件22184事件、原版官方校验通过；该计数对应刷新时点。

2026-09-11 voice-asr-capacity-20260911：真实decoder ORT会话创建及释放真机通过；141998120字节峰值、389个同时分配记录、失败0、释放后live0。旧128记录上限不足；本版2048。证据work-in-progress/native-asr-model-session/attempt2/acceptance.json及原始串口日志。尚未运行音频推理，尚未完成语音配网。

2026-09-11 encoder1：锁定166189616字节encoder，ARM64编译、独立voice-asr-encoder-20260911固件1655步构建和ELF审计通过；分段模型包重建核对通过。尚未上板或推理。板子已进入U-Boot，等待VMware连接OTG。源码work-in-progress/native-asr-model-session/encoder1，构建evidence/build/voice-asr-encoder-20260911。

2026-09-11 encoder1真机：完整模型CRC通过，encoder Session创建与释放通过，324515048字节峰值、818条记录、失败0、释放后0。encoder与decoder尚未联合或执行真实音频推理。CRC失败与恢复过程均保留，未刷写。证据work-in-progress/native-asr-model-session/encoder1/acceptance.json。
`n2026-09-11 voice-asr-recognizer：真实完整Recognizer固件构建及ELF检查通过，BIN b98b29e8f365cdc61c07cc8bcdf704aec48758bf4db5f1a14fa7dfc027e946f1 已RAM启动；两个完整模型CRC通过。分段加载发现重启改写decoder暂存区，CRC二分定位276字节范围，受限RAM恢复后全模型CRC通过，原失败记录保留。固定WAV推理已启动，结果待采集；Wi-Fi/BLE未启动，不代表语音配网通过。主机借用Env生命周期4场景通过，仅使用假API，不替代板端。

2026-09-11 完整ASR真机：固定WAV输出中英文文本，42.021秒含加载，模型池峰值468101988字节/1264条，释放后0、失败0。报告work-in-progress/native-asr-recognizer/acceptance.json绑定原始日志哈希。麦克风快照接口及显式asr-mic开关已加入，SDK旧入口差异审查后仅登记两个准确哈希，sync_sdk核对/同步通过；voice-asr-microphone独立构建中，尚未上板。

2026-09-11 麦克风ASR：首轮实际采集48000帧后输出文字，但目标句识别错误，不算规则配网验收；第二次调用在MLAS量化矩阵首轮计算发生空地址异常并自动回到eMMC基础固件。堆栈和编译器-v/反汇编证实当前GCC为single线程模型，原__emutls_get_address使用进程共享地址，候选native-emutls-fix改用NuttX线程/任务组生命周期。主机64线程/32组/128析构及O0/O2/ASan通过，尚未真机验收；voice-tls独立构建中。

2026-09-11 TLS修复候选voice-tls独立构建/镜像静态校验通过，尚未上板，板子停在Fastboot等待OTG接入。第二轮清理仅删除8个过期构建12872个可再生成对象，回收3585662648逻辑字节；974个受保护文件哈希不变，当前TLS构建未删除，证据evidence/vm-space-cleanup-20260911-round2。

2026-09-11 voice-tls RAM实测：OTG包/固件/两个模型CRC通过；两个独立固定WAV任务42.250/42.074秒输出一致，TLS线程隔离/析构和池释放通过，证据work-in-progress/native-emutls-fix/board-repeat-acceptance.json。随后麦克风识别26.830秒完成但文字错误，未执行联网；周期性零样本仍未修复。仅有限重复验证，非长期稳定或语音配网验收。

2026-09-11 audio-pause：新增显式capture-pause250诊断，默认路径不变，诊断录音禁止用于ASR。主机O0/O2正常/溢出/冻结时钟退出和原PIO回归通过；两个错误模拟RXOI位的初始失败保留。SDK核对/同步仅3文件通过，独立构建BIN d802b88da7c984ea4e69b05d2f9a37c6bb22cd9441100aaa100ee586e7a81582及ELF校验通过；尚未上板。板子已进入Fastboot，encoder尾段和decoder保留区CRC无差异，等待OTG连接。

2026-09-11 audio-pause已RAM运行；252us无RXDR访问时四FIFO各积累2字，有限排除读动作轮转假设，音频尚未修复。诊断缓冲被ASR拒绝。SDIO/host状态/真实扫描通过；首次BLE脚本因尾部输出缺失失败，单独状态复核host_mode=1 fault=0；未验收GATT或WPA2/DHCP。报告work-in-progress/audio-pause-20260911/board-result.json与network-resume-*。
2026-09-12 VoiceLink 确认门与真实扫描：`app/voicelink` 新增密码结束后的明确确认状态，主机 O0/O2 19 场景通过并编入 ARM64 固件；OTG RAM 启动、两个模型 CRC、固定 WAV ASR 连续两轮释放归零、SDIO/BT host/共享 Wi-Fi 服务启动和正式 Controller 真实扫描通过。扫描最终提供 `Lansee_vistor`、`Lansee_auto`、`Lansee`、`REDMI K80` 四个候选并进入 WaitingNetworkChoice。源码/固件和真机证据分别见 `evidence/build/voice-flow-confirmation-20260912/verification.json`、`evidence/voice-flow-confirmation-20260912/acceptance.json`。麦克风文字尚未自动送入 Controller，TTS 仍为控制台提示，不能称完整语音配网成功。同期旧构建关键产物先归档到 `vm-archive/cmake-out-key-artifacts-20260912/`，再删除 149 个可再生成构建目录，Ubuntu 根分区从约 1.3 GiB 恢复到约 38 GiB 可用；证据 `evidence/vm-space-cleanup-20260912-round3/`。未写 eMMC/STM32，未启动云台。

2026-09-12 20:55 覆盖：麦克风 ASR 文字已由 `flow-mic-provision` 自动送入 Controller，真机一次识别 `你好 open`，多轮 TLS 检查与模型池释放通过；后续无人声轮次有界超时，不算完整配网。正式解析器新增仅针对精确 `你好open` 的兼容，O0/O2、严格 SDK 同步、ARM64 构建和 ELF 审计通过。新固件 `f96ecdb89b13af582428494c0cc903e66061ede1e3e0ec419e7512a509fe46e5` 已打成无 flash 命令的 OTG RAM 包但因 VMware USB 代理 Code 43 尚未上板；Ubuntu 已恢复，板子停在 U-Boot。证据 `evidence/voice-rule-wakepartial-20260912/acceptance.json`，不能宣称语音配网完成。

2026-09-13 云端传输基础：`app/k7agent/cloud` 已迁入 DNS 和 HTTPS/mbedTLS 正式组件；DNS 9 项、HTTPS 9 组在 O0/O2 下均通过，独立 ARM64 配置、完整构建及五个强符号保留门禁通过。修复了 mbedTLS 3.4.0 在关闭 DTLS 时未定义兼容宏触发的 `-Wundef` 错误，没有伪造配置功能。证据 `evidence/k7cloud-link-20260913/acceptance.json`，说明见 `docs/DNS与HTTPS基础接入_20260913.md`。本轮未上板、未访问网络、未使用密钥，不能称 DNS/HTTPS 或小米 API 已打通；归属当前主会话，日志仍须刷新并运行官方校验器。

2026-09-13 18:00：用户指定‘你好’前缀与‘开始联网’。app/voicelink 默认规则、解析及 20 组流程测试更新；app/k7sound 正式提示音最大档；ASR 通用日志不打印原文。新固件 55aeacf13f087db3f9e9b5cd4228206d9a3bb87b650592592c87c02d14f52fe2 已 RAM 加载，主机/编译通过，真机语音唤醒未通过。见 docs/中文前缀语音唤醒_20260913.md、evidence/voice-start-network-20260913/acceptance.json。


2026-09-13 18:38：固定 WAV 160850 帧被麦克风 48000 帧上限提前拒绝，已增加独立固定样本边界。主机 O0/O2 与 ARM64 对象编译通过，尚未链接或上板；当前实际固件不变。见 docs/固定ASR样本边界修复_20260913.md。

2026-09-13：audio-cued-capture 已 RAM 验证提示音后 32693us 开始采集，新增 Ubuntu GTK 连续语音测试窗口并启用。后续真人唤醒及三网络扫描通过；voice-ui-connect 新版本 RAM 加载后用户确认手动密码配网跑通，互联网未验收。

2026-09-13：离线 TTS、ROMFS 模型、PCM 播放与录音衔接、英文 SSID 拼读进入正式源码；ARM 链接及 ELF 检查通过，尚未上板。第一次模型传输 CRC 通过但复位后改变，已拦截，等待 OTG 补传。见 docs/离线TTS语音配网接入_20260913.md；不等于全语音配网通过。

2026-09-13 21:18 覆盖：首版 TTS 固件已 RAM 启动且三组模型 CRC 通过，但 `tts-smoke` 因独立 VITS ORT Env 仍使用约 126 MiB 系统堆而 `std::bad_alloc`；失败时 512 MiB 模型池保持空闲。新候选改为 ROMFS 模型直接字节、TTS ORT 模型池分配器、ASR 精确 512 MiB 边界，并加入 ASR/TTS 完整生命周期共享锁。最终 `nuttx.bin` SHA256 为 `e0fffeabfc4099ff0982f27fe0d07139d684e3645e399e85a0968cc56296a7af`，116,524,928 字节 RAM-only OTG 包 SHA256 为 `17d264568e8fe810da78a95b317549eb8a5ef54e236634c85b0813a517322c8e`；ELF、包布局、异常表及强符号唯一性通过，尚未上板。Ubuntu CH340 当前打开返回 I/O error，SDK 同步保护拒绝覆盖旧 ASR wrapper，因此正式 SDK 保持不变。证据见 `work-in-progress/tts-arena-fix-20260913/` 和 `docs/离线TTS语音配网接入_20260913.md`。日志已刷新为 15 文件、30,747 事件，原版官方校验 ALL OK；未写 eMMC/STM32、未启动云台、未提交远程。

2026-09-14 13:10：sharedcache2 男声 TTS 冷/热播音均成功，热合成约 3.022 秒；随后缓存 TTS 与新建 ASR 共存时在系统堆 960 字节分配处 data abort，而 512 MiB 模型池仍约有 505 MiB。正式主线转为开发机预生成 20 条男声短提示 + 缓存在线 Paraformer ASR。新增单声道 S16 直出接口，无临时立体声堆分配；O0/O2 精确 FIFO 与旧路径回归通过。第二版固件 SHA256 `e9f48b37b769aa2ee6221ec344e37c242418ef2a35444a3252140afe9eef207f`，单次 OTG RAM 包 117,122,920 字节、SHA256 `3018c2f0769faff89a7f32f50e0152f07ba372224eadb1fe6d69ebaacfbf3172`，尚未上板。白名单删除 28 个旧构建目录后 Ubuntu 可用 33 GiB。对应 `work-in-progress/prompt-asset-tts-20260914/` 与 `docs/离线TTS语音配网接入_20260913.md`；未刷 eMMC、未提交远程，当前日志需后续刷新和官方校验。
2026-09-14 17:58：配网主线改为前端加密 BLE 扫描、选择 SSID、手动输入密码；板端无线四段/固件 CRC 与 RAM 启动通过，BLE host、Wi-Fi 共享服务和 `native_network=1` 已恢复，但本轮尚未由手机重新联网，本地 ASR 保持不可用。新增 `frontend/整机联网语音适配器.mjs`、`speech-http-client.mjs`、`gimbal-cloud-client.mjs`，主机 `host/cloud_speech_bridge/` 以及板端 `cloud_speech_orchestrator`/MiMo-k7sound adapter；前端 26 项、主机 16 项、板端 O0/O2 DNS/HTTPS/MiMo/编排测试通过。业务 OpenAPI 无 ASR/TTS，设备会话凭据未提供，板端候选未上板；`10.3.3.68:3000` 当前拒绝连接。交付与边界见 `docs/蓝牙配网与云语音整机接入_20260914.md`、`evidence/integration-api-20260914/interface-package.json`。未写 eMMC/STM32、未启动云台、未提交远程。

## 2026-09-14 流式 ASR 接续覆盖

用户最新提供 `E:/openvela/语音模块/speech-service`，明确 ASR 必须流式。
该服务原为阿里云百炼批式 ASR/TTS。主仓新增 `/asr/stream` 网关，以真实
Paraformer realtime-v2 WebSocket 协议双向转发 PCM；与原 app 挂接启动，原目录和
密钥配置未改。11项隔离测试通过；真实合成句 TTS 1219 ms，3.92秒PCM流式发送，
640 ms首个非空 partial、4281 ms final，识别完整测试句。不是小米流式验收。
详见 `docs/speech-service流式ASR接入_20260914.md` 和 `evidence/speech-stream-live-20260914/result.json`。
本机服务 PID34304，监听127.0.0.1:8000；K7网络端采集/WebSocket尚未接入。

板端状态更正：旧 heartbeat 18:57把板子重启到Fastboot，17:58运行快照失效。
已停止旧加载器308144并更新定时任务，不能恢复旧语音密码固件。
新的radio-status候选ARM64已成功构建，BIN 2782928字节，SHA256
`6efed4c48eaee8c51289f9d52a704203e7207799bd3aaf7f213dc790fa891a54`；未上板。
radio bridge漏采整个断开重连区间时的旧TLS/API门禁继承已修，O0/O2各7组通过。
前端迟到探针/回报状态污染已修，Node32项通过；runtime owner O0/O2各7组通过但未集成。
用户已要求停止子代理，当前均已停止，不自动重启。

AI日志：原有writer锁覆盖导出，但取锁前read会在Windows锁争用时直接失败。
修复及13项回归见 `docs/AI日志写入锁修复_20260914.md`。
实时独立校验改用 `tools/validate_project_logs.py`，它持锁调用原版官方校验器。
旧独立无锁校验曾读取JSONL/manifest混合快照；不能笼统说原来没有写锁。

2026-09-14 前端交接：新增 frontend/前端交接_蓝牙配网与联网成功回报.md，与 prov_protocol/prov_service 和 vela-provision.mjs 核对 X 扫描、connect、wifi_connected/IP、status 恢复。12 项客户端测试通过。交付 deliveries/前端交接_蓝牙配网_20260914.zip；自动成功播报和新固件 App 闭环仍待验收。
2026-09-14 启动：capture-stream 候选通过 OTG RAM 传输及逐段 CRC 后进入 NSH；初次启动命令读取超时，后续实际查询 PROV registered=1、WIFI shared start ret=0、host fault=0。App 尚未连接，自动播报未验收。证据 evidence/ble-cloud-start-20260914/。
2026-09-14 保持配网运行推进云语音：真机 capture-stream-check 150帧/result0，蓝牙fault0。云TTS1329ms、流ASR首字578ms，新增live_client实时源及3测试，总14测试通过。见 docs/speech-service流式ASR接入_20260914.md。板端网络发送/云TTS播放未接通。
2026-09-14 板端云语音桥：新增TCP开发通道、C asr-live/tts-test、主机桥和回归。17项Python、C O0/O2边界测试、ARM64构建通过，用户批准一次RAM更新且已加载a41b79ab...镜像。蓝牙/WiFi服务启动成功，等待用户新固件配网后真机测试，未宣称完成。见 docs/板端云语音桥联调_20260914.md。
2026-09-14 云语音真机与知识库：TTS上板播放并由用户确认清楚；ASR首轮队列溢出，缓冲修复版已加载但待重新配网验证。新Swagger读取成功、暂无聊天/知识库接口，flows页面连接被拒绝。接续 docs/板端云语音桥联调_20260914.md 与 docs/知识库语音聊天对接_20260914.md。
2026-09-14 后端方向纠正：整机直接对接18085业务后端，由后端编排知识库/模型；核对设备认证/心跳合同，7项现有客户端测试通过。新增 docs/业务后端整机接入_20260914.md。设备凭据与后端聊天协议待提供，未发送伪造心跳。当前板端查询wifi0/ip0，串口本次未收到完整提示符，未重启。
2026-09-14 自由对话目标：已创建真实多轮对话活动目标；缓冲ASR真机仍失败，POSIX同C发送器150帧通过，分核候选已构建待OTG接入。project-manifest已刷新当前Fastboot状态与目标边界，禁止沿用旧在线快照。

2026-09-15 09:25：用户 App 配网后真机 radio_generation=1/wifi=1/IPv4=10.3.0.214。分核版首轮17帧发送/主机16帧，采集ENOSPC且失败，保留asr-092304.log。增加主机每帧到达时间记录（不存音频或文字），17项回归通过；仅重启电脑TCP桥为PID51872，未重启板子。第二轮asr-092528.log：48000采样、150帧、result=0/completed=1；主机首帧2438ms、末帧5407ms、completed5532ms，无partial/final，未安排人声。证明一次实时上传成功，不代表识别准确性或稳定修复。等待用户准备真实人声测试。后端认证/聊天合同仍待提供，完整自由对话未完成。

2026-09-15 人声复测：用户准备后测试150帧/result0/completed1，partial在已收87帧时返回2字，final在131帧时返回6字，整会话5047ms；文字未记录，不能认定准确性通过。新增可选K7_SPEECH_RESULT_FILE私密最新结果输出，默认关闭，当前桥51872尚未启用此增量，17项测试通过。最新18085 OpenAPI再次只见设备/业务接口，无聊天入口，已向用户请求合同及认证配置；不调用echo冒充回答。AI日志前次17文件36647事件ALL OK，后续持续刷新。

2026-09-15 云端回声：显式echo-test及19项回归通过；首次真人执行串口EIO、未发命令，无实测速率。详见docs/云端ASR回声TTS测试_20260915.md。桥49032运行，等串口重新挂接。

实测更新：串口重连后WiFi仍10.3.0.214，无重启。093448.log回声测试ASR150帧/result0，识别与测试句一致；首个非空文字距桥收到首帧1859ms，最后一帧后265ms取得final（这不是从人声起点计时）。TTS937ms合成64000字节/2秒PCM，板端result0；ASR命令5318ms、TTS命令含下载和播放5103ms，全轮10421ms。尚待用户确认实听；不能把总时长或TTS命令时长当纯云推理延迟。保存result.json，无业务聊天验收。

优化候选：board_bridge增加1024字节/5ms PCM发送节奏（原单D包合同不变），针对板端8槽RX队列的突发拥塞假设；20项测试通过，含64000字节输出一致性。桥PID10628启用--audio-interval-ms 5，metrics在evidence/cloud-echo-paced-20260915/metrics；板子未更新、WiFi仍10.3.0.214。等待同句真人对照；未宣称提速。当前云TTS DAC仍24dB衰减，不是最大音量。

优化进展：分批发送候选真人首轮ASR17帧失败，次轮150帧但无文字，TTS按预期拒绝。随后完全相同64000字节PCM四次纯传输/播放对照：原发送3987/4269ms、5ms分批4405/4170ms，均板端返回0；未证实提速，已恢复原发送。host_enqueue_ms异常均0，不据此推算交付延迟，原始证据保留。新增ASR completed即异步预合成TTS，并与板端结束录音/启动TTS请求重叠；新ASR/失败清理旧任务，过期和空结果拒绝，21项测试通过。桥PID14912已启用预合成echo模式，保持板端原固件与WiFi，等待人声速度对照。无长期稳定或提速验收；云TTS音量仍24dB衰减。

预合成实测：首轮无文字（工具启动延迟可能错过说话），增加板载tone-max提示音后识别与原句一致，150帧/result0，TTS64000字节/result0。ASR4990ms、TTS5319ms、全轮10309ms（提示音478ms单独排除）；合成875ms，其中请求等待687ms，重叠188ms。基线10421ms，只有单轮数据，不能宣称稳定显著提速。保留预合成，分批发送关闭；主要后续工作为板端下载/播放衔接及真正分块播放，尚未实现。

最大音量候选构建完成：2791336字节，SHA256 e0c66bdebb9b0dd8a8d60ec5e3f86ea20b8456484a241766ea407f7f5c6c8197，独立目录velavision_tts_max_20260915。SDK精确单文件同步check/apply/check通过，无未知改动覆盖；仅mono16内部播放改为kd_set_playback_max，DAC0dB并模拟0x30/31=0x21。尝试进入下载模式时串口EIO，重启未发送、旧固件保持，等待用户串口重新挂接；尚未加载或最大档实听。tools/load_tts_max.py固定新哈希，private/tts-volume-test-20260915.pcm已保存供本地单次重播。

2026-09-15 10:07 最大档已实播：用户配网后IP10.3.0.214，复用private/tts-volume-test-20260915.pcm，无云合成请求；日志maximum=1、DAC0dB、模拟寄存器31=0x21，64000字节result0，命令3981ms。证据evidence/tts-delivery-replays/20260915-100717/transport-0.log。板端执行通过，破音需用户实听反馈。原语音桥已恢复PID10088，--echo-test预合成开启。

2026-09-15 整机语音控制：核对现有track/photos/pose/三图客户端与最新18085合同，形成docs/语音控制整机测肤整合_20260915.md。发现旧控制器独占串口、相机每boot一次限制；未运动/拍照/上传，未冒称整机完成。

2026-09-15 已实现host/whole_device/report_speech.py与voice_intents.py：规范化报告事件校验、分句、有限队列并行合成/顺序播放、generation取消、完整识别意图去重及否定句不触发。新增8项+既有21项共29项测试通过。尚未接真实后端SSE或板端播放/停止适配器；未运动/拍照/上传，不冒称整机完成。需要后端源码目录或实际流式合同及认证配置位置；已向用户询问。

2026-09-15 用户明确后端仍在修改，优先语音命令控制。新增DeviceController，启动真实ready后播报、三视图阶段顺序、重复事件去重、停止generation取消、音频停止异常仍尝试停跟踪。7项控制器测试加原29项共36项通过，提示词补充本地保存/任务失败。尚未连接实际串口/相机/云台ports，真机语音动作未验收；不再以后端缺接口作为语音命令开发的阻塞理由。

2026-09-15 语音命令增量：CommandSession接入完整识别→控制器，拒绝旧录音会话，启动异步且可继续接收停止。SerialOwner提供单读者/写锁及取消期间写入完整性保护；停止音频与停止跟踪改为并行。新增7项，整机22项加流式语音21项共43项通过。初次discover因namespace目录不可发现、初次桥回归缺httpx失败，改用显式模块及既有streaming-dependencies后全部通过。未接生产硬件ports，未运动/拍照/上传，运行桥未替换。下一步相机常驻生命周期与板端动态TTS/停止适配。

2026-09-15 语音模式候选：板端voicecam/voicemode以及NativeMotion固定命令适配完成，最长1800秒单次采集内可hold/track/photo切换。保留相机一次启动保护、MCU校准复位前提。实际C代码O0/O2回归通过，三个C模块ARM64临时对象编译通过；Python46项通过。未改SDK、未整包链接或RAM加载、未运动拍照。动态TTS/照片事件生产接线未完成。

2026-09-15 voice-mode整包完成：四文件保护同步、独立1815步构建退出0；2791384字节，7c96242f2899a8361bbae4c91a3f9de7e3619ff74a8c0ffaf364f91c3f265672，源码哈希核对通过。48项Python通过，O0/O2实际模式回归通过。尚未RAM加载、未运动拍照。详见evidence/voice-mode-20260915/verification.json。

2026-09-15 新增host/whole_device/photo_capture.py、native_ports.py：真实协议解析、对应USB帧持久化、板端done确认、播报与保存分离。新增4项测试，总52项通过。缺帧/非法角度拒绝，慢播报不阻塞保存确认。人工测试fixture非真机照片，未采录/启动云台或RAM加载。动态TTS/停止和最终运行入口仍未接完。

2026-09-15 voice-stage候选：NativeSpeech、固定25词表、K7S1P、tts-stop/state和PIO取消清理。七个SDK文件精确前版哈希审查同步通过，独立整包构建中；ARM64模块编译与PIO三案例O0/O2通过，Python59项通过。未上板、未实播或运动。统一运行入口仍待ASR/USB接线。

2026-09-15 voice-stage整包完成：2795536字节，5cc67bd28192300f3841783ae320e6c7f393c3c089a5a7b790019baef7db1feb，11项源哈希与SDK核对，构建退出0。evidence/voice-stage-20260915/verification.json明确ram_loaded:false/hardware_tested:false。

2026-09-15 服务器部署排查：本机10.3.1.125的speech-service PID44976/8000与桥PID10088/8001仍在运行；两台10.3.3.170及10.3.3.68的SSH22返回Ubuntu OpenSSH，8000拒绝连接。尚无服务器登录账号/密钥，已请求用户提供配置位置，未尝试猜测凭据。deploy/speech与tools/package_speech_server.py生成白名单无密钥部署包，流ASR/桥22项回归通过；未实际Docker构建/部署/板端切换。整机控制仍应迁入openvela，服务器只承担语音网关。

2026-09-15 服务器部署：用户提供bool账号，SSH已登录10.3.3.170。Docker Hub超时，改独立venv/systemd。首次启动发现包遗漏session.py，已补齐并修正打包白名单。两项服务无密钥启动active、health200、端口正确，随后停服等待配置。用户先明确允许传.env，传输后又改为后端配置；已撤下服务器speech.env并验证不存在，不再传密钥。开机启动已配置，原18085及本机桥未改。真实云ASR/TTS与K7新地址联调未验收。见deploy/speech/后端配置接续.md。

2026-09-15 板端整机命令：新增device_voice_intent、k7cloud device-once、模式applied状态及音频互斥；精确final在采集和completed成功后直调原生云台模式，3秒确认超时或播报失败hold。C解析/模式O0/O2、七模块ARM64通过；独立1816步构建0，2799680字节/c3b862d2c62ced7504e1c7a4babfe317fb0c18aeee285c1c3b7f133939c5e137。未上板。串口EIO未发送命令，待重连及运动前复位确认。单轮入口不是连续自由对话，三图保存仍有主机依赖，未验收整机。心跳接续方向已修正。

2026-09-15 12:34整机候选上板：串口完整应答恢复后进入Fastboot，用户接入OTG后单包19.6MB/1.99秒，CRC和RAM启动通过。新固件c3b862d2...，蓝牙/WiFi服务恢复，等待App配网；未运动。Windows语音桥PID54580更新并关闭echo-test，22项回归通过，8000健康正常。见docs/整机板端整合接续_20260915.md。

2026-09-15 阶段TTS真机：新固件App联网IP10.3.0.214后K7S1P tracking_stopped返回51200字节PCM并播放result0，最大档及恢复通过；人工实听尚待确认。首次脚本失败未保存stdout，已改为异常也存日志、缩短只读查询超时；后续完整成功日志在device-intent目录。未运动。

2026-09-15 无人值守软件推进：native-photo与device-loop独立构建通过未上板；RAM三图缓存/并发500轮、实际门控+模拟姿态接线、取消保护、照片阶段计划器O0/O2通过，九模块ARM64和Python59项通过。新增native-photo-prompts候选构建中。用户休息期间未采录/运动/重启；相机未枚举。真实后端聊天/报告流仍缺。见docs/整机无人值守软件推进_20260915.md。

2026-09-15 13:30更新：native-photo-prompts全量构建及13项源哈希核对通过，BIN2799728字节/8cbd147ad72a3b463e734ab42320d91d80219d2b978a6bdff6b83671011965d1，六组C O0/O2综合证据已保存，未上板。A01 multipart四part编码器独立C/MIME解析通过但未接认证/网络。两个中间构建核对归档至E盘后移除VM副本，空间恢复约12GiB。K7S1P固定阶段PCM缓存候选及五项新测试完成，尚未替换运行桥。当前板端仍c3b862d2版本，阶段TTS用户实听确认，相机仍未枚举；完整闭环未完成。

2026-09-15 用户回来后：8cbd147a候选已OTG19.6MB/2秒加载、全包和五段CRC/RAM启动通过。BLE配网后实查WiFi1/IP10.3.0.214。相机Hub正常，描述符613但已断开；发现一次实例限制，新增采集前重连修复，保留采集一次/DMA边界，真实回调100轮O0/O2通过，独立camera-reconnect构建中未上板。后端新增gimbal-ai/messages流式无状态问答，已存契约和主机参考适配，4项回归通过；缺设备认证且板端未接入，不宣称聊天成功。

2026-09-15 视觉蓝牙共存：camera-reconnect 9e28d2ed已RAM启动，UVC connected1/committed1，voicecam mode0/tx0通过。随后App报通知订阅失败；实测BLE连接加密成功、subscribed1但busy1，视觉线程CPU0优先级90高于配网85，怀疑调度饥饿。新vision-ble-coexist候选将解码设为优先级80/CPU2，九模块ARM64及模式O0/O2通过，独立构建中未上板。原因仍待真机对照，见docs/视觉与蓝牙调度排查_20260915.md。

2026-09-15 空间整理：用户要求转存Windows磁盘。18个明确旧构建完整归档E盘，逐文件与归档SHA校验后再删VM副本，空闲9.2→27.682GiB；保留5个当前/回退构建、SDK及源码。中途遇未完成构建保护停止，核实后完整保留原内容，未冒充成功产物。证据vm-space-recovery-20260915.json；脚本archive_superseded_builds.py，说明docs/虚拟机构建归档到E盘_20260915.md。蓝牙共存30f6a77c候选已构建核对未上板，板子Fastboot待OTG。


2026-09-15 最新实测覆盖：vision-ble-coexist / 30f6a77c 已 RAM 加载，完整 CRC 与启动通过；实际 decode 线程 CPU2、FIFO 优先级80。用户 App 已配网成功，WiFi 1/IP10.3.0.214，40.23秒 BLE subscribed1/busy0/errors0 与视觉帧递增并存，无云台运动。仅有限共存验证，非长期稳定验收。证据 evidence/vision-ble-coexist-20260915/device-state.json。

用户提供 proof 后真实设备登录和 gimbal-ai/messages SSE 中文回答已通过；私密认证仅在 private/，不写交接明文。新增 host/streaming_asr/conversation.py 与板端 chat-loop 候选：openvela 控制提示音、3秒录音、回答播放和下一轮；网关只调用后端与云语音，不驱动串口/运动。独立 B/C 协议避免污染普通 ASR、固定提示或回声测试。空语音不提问，问题60秒单次消费，90秒总超时和等待保活，取消不回退复述。74项 Python、实际 C 停止/响应 O0/O2通过；云回答→TTS实测5047ms、199680字节PCM，板端尚未播放。独立 native-chat 构建进行中。当前为无状态轮次、固定3秒录音、整段PCM，尚非上下文记忆/VAD/边合成边播放，也尚未真人连续聊天验收。


2026-09-15 15:07 覆盖：native-chat 已全量编译/链接通过，2803824字节，SHA256 f32aadc337547b39f78cbbc845f4ce671ab29ba450e1774bc16ce49bdc8a94b2，九项源哈希核对及新增协议字符串检查通过。最初保护同步因未登记新基线而拒绝；审查两文件 diff 后将精确前版哈希登记，check/apply/check通过，未覆盖未知改动。新网关 PID56072/8001 已启用 conversation-auth，普通语音服务44976/8000健康，空聊天请求被拒绝。板端 RAM 更新尝试在串口open时报EIO，重启命令尚未发送，旧30f6固件保留；已请用户只重连QinHeng USB Serial，待恢复后 enter_device_intent_download.py → OTG接入 → load_native_chat.py --load → App重新配网 → 用户准备后 chat-loop真机测试。不得宣称已加载/已自由对话。AI日志官方校验17文件38560事件通过；覆盖范围/脱敏/部分采集声明保持。


最新接续：用户重连串口后打开正常。旧脚本首次未截获U-Boot；被动日志确认仍为旧NSH视觉帧递增、tx0。新增 enter_native_chat_download.py 先换行握手再整条发送reboot，实际截获U-Boot并确认 Enter fastboot...OK。当前板端Fastboot、WiFi/BLE服务已停，Ubuntu lsusb仅串口无OTG，已请用户接USB download gadget。下一步直接 tools/load_native_chat.py --load，不要重复重启。日志 evidence/native-chat-20260915/enter-download.log。未刷写存储。


最新加载覆盖：用户接入OTG后 native-chat f32aadc3 已RAM启动，19,581,040字节/1.932秒，整包CRC34ebc98b、四段无线和固件CRC7b96b7da通过；NSH、bt-host ret0、PROV注册connect=true、WiFi shared start ret0。当前等待App重新配网，WiFi0，未真人聊天验收；摄像头会话因重启已结束，不沿用旧运行快照。证据 evidence/native-chat-20260915/startup.log。


用户报告已配网后复查：排除旧串口缓冲，再查PROV connected1/subscribed1/busy0/errors0，commands1/last command2（STATUS），WiFi generation0/wifi0/IP0.0.0.0。未收到本次CONNECT，不把蓝牙已连接当作WiFi成功。已请求App重新选择网络提交密码，未录音/重启。证据post-provision-check.log，服务8000/8001保持监听。


后续用户重新提交后实际radio_generation1/wifi1/ipv4_ready1/IP10.3.0.214确认，覆盖上一离线状态。已请求真人准备三轮聊天；run_native_chat_test.py --start只在用户准备后执行，当前未启动采录。


真人三轮测试：三个录音均150帧、ASR completed1/result0；第一轮后端回答468480字节PCM播放result0，后两轮无文字并跳过提问，loop_done0。用户确认听到完整回答及后续提示音；实际说“你能做什么”，云端识别“说什么？”，因此不能称准确多轮聊天已验收。发现chat先独立tone再进入capture_stream，ADC尚有1000ms准备延迟，可能漏掉开头。新增capture_stream_cued复用此前已存在的sound_run连续cue→grouped_sink路径，先ADC准备再提示、同锁同codec衔接录音；普通ASR路径不变。native-chat-cue独立构建中，实际C O0/O2通过，未上板。当前板子仍f32aadc3在线，三轮已结束、没有持续录音。用户声音确认和范围见human-test-result.json。


衔接修复 native-chat-cue 构建完成：2803824字节/97dcd424366b8973bebb323db7fc2dc5e87d326f8d8129a18a2d68d96dd0fab3，12项源哈希核对通过，未上板。板子已软件重启并进入Fastboot，WiFi/BLE旧在线快照失效；Ubuntu未接OTG，已请求连接。下一步 tools/load_native_chat_cue.py --load，加载后App重新配网，再准备真人同句复测。旧f32版本第一轮后端回答实听通过，但不能作为衔接修复验收。


最新上板：native-chat-cue 97dcd424 已RAM启动，OTG19581040字节/2.073555秒、整包CRC bab717e7、固件 f5ca69b6及四无线段校验通过，bt-host ret0/WiFi shared start ret0/PROV registered1。等待App重新配网和真人准备。新修复尚未录音验收，不能沿用前版结果宣称句首准确。复测工具 tools/run_native_chat_cue_test.py --start，全量日志保留而控制台只显示关键事件。


用户重新联网后已实查 generation1/wifi1/ipv4_ready1/IP10.3.0.214，当前97dcd424修复版在线。已请求准备两轮人声复测，尚未采录；启动命令工具为 run_native_chat_cue_test.py --start --rounds 2。


最新真人复测：97dcd424完整识别“你能做什么？”，150帧/result0，句首修复单轮通过。但后端10秒DEPENDENCY_TIMEOUT，板端收到X后loop_done-22，第二轮未执行。只读服务器诊断，上游7861 HTTP200/accepted27ms但60秒无回答；详细docs/后端问答超时定位_20260915.md。已请求AI服务代码/日志位置。新增网关真实错误提示后继续下一轮，76项通过，不重发问题，尚未真机验证该提示。板子保持联网，未重复刷写。


用户表示后端同事修复上游期间继续开发：新增Ubuntu连续对话GTK面板，已启动PID445959且截图检查正常，默认不录音/不占串口。8项面板状态与串口异常清理测试通过；停止确认缺失禁止重启，不把cue完成日志冒充录音开始。用户已被邀请点击三轮测试，最后观察sessions0，尚未硬件验收超时播报后续轮次。板端仍97dcd424、网关47076，未刷写/录音/运动。详见docs/连续对话测试界面_20260915.md。


用户要求TTS18dB，随后明确短提示音保持最大。单文件分离音量设置，新增speech-volume运行时查询/设置，精确SDK同步通过，speech-volume-20260915独立构建中未上板。当前面板三轮已返回loop_done0，未占串口；未重启板子。见docs/TTS与提示音独立音量_20260915.md。


音量候选构建完成：speech-volume-20260915，2803832字节，SHA2560daae069539170459879d62e026d85e6de0499b2955fce8859ea74aa814d060b，11项源码哈希核对通过。现已软件重启至Fastboot，Ubuntu未接OTG，已请求用户接入。下一步 tools/load_speech_volume.py --load；目前不能称18dB已生效。短音最大保留，WiFi/BLE旧在线快照已失效。


最新加载验证：speech-volume-20260915 已 OTG 单包 RAM 加载，19581048字节用时1.924秒，全包CRC97e52b68、固件CRCdf21a2c1及无线分段CRC通过。蓝牙配网服务注册、Wi-Fi服务启动成功。板端实际查询 SOUND speech_gain_db=18 cue_gain_db=24 next_playback=1。仅确认配置，18dB实际播放/寄存器与听感待App重新配网后验证；未写eMMC。证据 evidence/speech-volume-20260915/startup.log、volume-query.log、device-state.json。


实机18dB播放验证：用户重新配网后，radio-status确认wifi=1/ipv4_ready=1/IP10.3.0.214。执行tts-test播放“联网成功”，日志playback_gain_db=18/dac_attenuation_db=6/maximum=0，SOUND result=0且codec_stop/platform_restore/i2c_restore均0，K7CLOUD tts result=0/pcm_bytes=35840。配置与实际播放通过，听感待用户确认；短提示音仍配置24dB，本轮未重复播放短音。无线日志有ret=-28接收丢弃，不能据单次TTS成功宣称无线长期稳定。证据evidence/speech-volume-20260915/tts-18db-playback.log。


用户听感反馈18dB仍偏大，改为16dB。新候选speech-volume16-20260915默认语音16，speech-volume与codec增益接口放宽为0到24的整数dB；DAC原公式48-2*gain保持，16对应寄存器0x10，短提示音24不变。两文件精确前版哈希审计和SDK同步通过；构建中，当前板子仍运行18dB版且已联网，未提前重启。


16dB候选已构建并核对12项源码哈希，2803832字节，SHA256 a7eecf20c424c9b3f08d8ca6f39b20fc8699842ebe5a802702ea783018a1b252。板子已进入Fastboot，WiFi/BLE旧在线状态失效，等待用户OTG接入；未加载，不宣称16dB已生效。下一步tools/load_speech_volume16.py --load。


16dB版已实际RAM加载：19581048字节OTG1.962秒，全包CRC692c2da4、固件CRC21e8a40d及无线分段CRC通过。蓝牙服务注册、Wi-Fi服务启动成功；实际查询speech_gain_db=16/cue_gain_db=24。待App重新配网及播放听感验证，未写eMMC。证据evidence/speech-volume16-20260915/startup.log、volume-query.log、device-state.json。


16dB实际播放验证通过：App重新配网后确认wifi=1/ipv4_ready=1/IP10.3.0.214；播放“联网成功”返回K7CLOUD tts result=0/35840字节。playback_gain_db=16/dac_attenuation_db=8，实际codec寄存器1a=10、1b=10，音频停止与平台/I2C恢复均0。短提示音仍配置24，本轮未重复短音，16dB听感待用户确认。证据evidence/speech-volume16-20260915/tts-16db-playback.log。


连续对话续测：后端文字请求成功，6625ms返回AISIA美容助理介绍。用户准备后两轮实机测试，第一轮ASR150帧/completed1，随后TTS超过960000字节上限，网关tts_too_long、板端loop_done=-22，第二轮未启动。不能声称连续两轮验收。新增已知长音频错误的明确口头提示并恢复下一轮，不截断答案、不重试后端问题；37项语音测试通过（最初unittest缺少-t参数导致相对导入错误，修正命令后通过）。网关已更新PID4312/8001，8000未重启；真实错误提示恢复待用户复测，完整长回答分段播放仍未实现。板端仍16dB固件，未重启/录音追加/运动。证据evidence/conversation-resume-20260915/和evidence/native-chat-cue-20260915/human-20260915T083515Z.log。


错误恢复真人复测通过：human-20260915T083907Z.log，首轮ASR150帧完成；网关chat_reply_kind=error_notice/chat_audio_error=tts_too_long，148480字节错误提示板端播放0，自动进入第二轮，第二轮150帧完成且无文本，最终loop_done0。用户确认听到提示和第二声短音，第二轮确实未说话，因此空识别符合实际。不得计为两轮正常问答成功。当前16dB语音/24dB短提示音，未重启；下一项完整长回答分段播放仍未实现。


串口重连后单轮human-20260915T084307Z.log：完整ASR“防晒有用吗？”，150帧/5000ms/completed1；再次tts_too_long，错误提示148480字节播放0，loop_done0。网关请求13719ms包含原回答失败和提示合成，并非正常答案延迟。实际问题答案未播放；下一步仍需解决长答案，不重复把error_notice计作正常聊天通过。


用户明确仅测试“启动云台”识别后模拟播报“云台已启动”，禁止实际动作。新增command_dry_run.py，仅完整匹配（忽略标点空白），拒绝否定句/其他句子，单次新鲜结果，固定PCM缓存35840字节。网关27132/8001已--command-dry-run启动，未带后端认证；缓存位置为Windows内存，不冒充板端缓存。测试入口仍chat-loop一轮，不调用device-once/device-loop。40项测试通过，待用户准备，尚未真人测试；固定3秒录音未优化。恢复正常聊天需重启网关带private/gimbal-device-auth.json。


纯提示首轮human-20260915T085156Z.log：ASR150帧/5156ms，实际final“启动原台。”，严格匹配拒绝，未播模拟提示，未运动。不能声称命令响应测试成功。当前paraformer-realtime-v2流式适配未配置热词；未擅自把错误同音识别加入真实控制规则，已请求一次复测。


纯提示第二轮human-20260915T085318Z.log：ASR150帧/5453ms，final“启动软体。”，未命中、未播报、未运动。两次错误识别不能通过放宽真实控制规则冒充命中。停止重复请求用户重说，待检查云台热词/识别效果；网关仍command-dry-run纯提示模式，不是正常聊天。


第三轮用户要求重试，human-20260915T085611Z.log：final“启动云台。”正确，ASR150帧/5204ms（会话总时长，不是说完后的延迟），command_matched=true/prompt_cache_hit=true/tts_synthesis_ms0，缓存35840字节播放result0，loop_done0；motion_enabled=false，未运动。网关缓存回复duration_ms0为毫秒精度下小于1ms，不代表声学延迟。单轮纯提示链路通过，前两次误识别保留，可靠性未验收，听感待用户确认。


用户追加“开始拍照”模拟回复“拍照已完成”；新增第二条严格映射，纯提示网关15612/8001，两提示共缓存76800字节，motion_enabled0/photo_enabled0，41项测试通过，未调用相机/运动。待用户准备真人测试。前一用户计时请求仍保留：板端计时四文件已修改并审计，speech-timing尚未构建/上板，能量端点估计与分析器尚未实现；不要把当前软件时长冒充说完到真实出声。


恢复串口后拍照纯提示human-20260915T090703Z.log通过：ASR“开始拍照。”完整，150帧/5063ms会话，缓存命中、无重新TTS合成，40960字节回复板端播放0/loop_done0。网关回复16ms不等于人声结束到喇叭出声；精细计时尚未加载。未调用相机/运动。实听反馈待用户确认。

最新主线为语音命令/缓存提示/三角度完成退出/上传，暂停自然聊天。已核对真实Swagger并添加四项通过的上传合同客户端；板端完成停止候选编译中，未上板或上传。细节和未完成项见docs/三角度姿态提示与上传接续_20260915.md。

photo-complete-20260915已全量编译通过，2803832字节，SHA256 b50b4202d7dab3860d2879caea978c7a3308c0ca9d617f1d5c493373b6220899，12项源码哈希核对通过；未加载。主机整机45项测试通过（namespace包需按显式模块运行）。上传客户端4项包括其中。未上传/未运动。实时姿态播报调度、RAM上传通道、真实consent引用和整机实测仍未完成。

用户指定开发consentEvidenceRef=dev-consent已配置并验证multipart请求体；host/whole_device/assessment-upload.dev.json、assessment_upload.py，5项测试通过。未上传照片，未运动。


## 原生上传接线增量

photo-upload-20260915：板端从同epoch RAM复制三张不可变JPEG，要求done=7且模式0已应用后，使用K7S1U传输。M=本次captureSessionId，F/L/R=三视角JPEG（各≤1MiB），E=提交确认；提交前再次核对epoch、done、停止模式及取消代次。收到网关K+任务UUID才报受理成功；W为空等待包，X/断链报失败。上传受tts停止socket取消控制，已发E后的网络异常仍可能后端已受理，不能说绝对未提交。

网关PhotoGateway只在明确配置--assessment-auth后启用，不与command-dry-run共用。三张齐全/E到达才提交multipart。private/photo-upload-spool保存同一次提交的图片摘要、metadata、幂等键及照片（此目录不进公共日志）；重传同capture必须内容一致，沿用幂等键。开发consentEvidenceRef固定用户指定dev-consent。自然语言聊天不启用。

--assessment-auth启动前预热固定阶段提示（最多8MiB/64条，一小时TTL）；缓存仍在Windows网关，不冒充板端永久缓存。请求缓存miss可能重新合成，因此仍需看真机cache_hit证据。

91项Python测试通过；板端停止/取消C O0/O2通过。未发送真实图片。固件编译中未上板；当前运行仍16dB纯提示版，网关15612尚未切换。

待完成：真机USB相机/云台初始化、真实三角度测试、阶段提示调度改善（当前仍轮询在ASR轮次后）、精细声学端点计时。用户已被询问0/0复位、无遮挡、摄像头连接确认；不用在未答复时启动真实运动。


最新覆盖：photo-upload-20260915构建/哈希核对通过，2803832字节，SHA256 0a501453e249b20a3eae270a2292f437267cd17e883d8001d760b42b6e12e08b。91项Python、C停止O0/O2通过。网关已实际切换PID7304 assessment-auth，26条提示预热2150400字节/一小时TTL成功，聊天关闭；旧dry-run不再运行。板子已软件进入Fastboot，旧WiFi/BLE在线状态失效，等待OTG，下一步tools/load_photo_upload.py --load。尚未上板、未真实照片上传或运动；用户摄像头/0/0/无遮挡确认仍待。实时阶段调度、人声端点估计继续未完成。


photo-upload固件已实际RAM上板：19581048字节OTG1.795秒，全包CRC896680e2、固件CRC c1a2094b及四无线段通过，实际SHA2560a501453e249b20a3eae270a2292f437267cd17e883d8001d760b42b6e12e08b。BLE/WiFi服务启动正常，等待App配网。USB Host启动并枚举32e6:9221相机，probe result0/committed1（camera-20260915T093418Z.log），未stream/未云台动作/未上传。网关7304整机模式26提示缓存保持，聊天关闭。真实云台0/0无遮挡确认仍待。


用户已确认0/0复位且周围无遮挡。voicecam 1800仅启动一次，VOICE SESSION ready mode=0，观察TRACK tx=0，保持停止；证据held-camera-start.log。真实device_round当前使用无短音录音入口，已如实告知用户并等待测试准备；未把模拟chat入口当成真实控制，尚未运动、未上传。


真实控制首轮：real-track-command-01.log，150帧/completed=1但text_chars=0、intent=0，未启动运动；随后显式halt且VOICE MODE applied=0。相机共存后无线仍10.3.0.214。修复真实device入口无提示音：复用既有capture_stream_cued，协议仍A、不启用聊天。停止/取消O0/O2通过，device-cue-20260915独立构建中、未加载。保护同步首次因新前驱未登记拒绝，核对实际已加载c415caa6前驱后增加单文件精确哈希登记，check/apply/check通过；未覆盖未知改动。


device-cue-20260915已实际RAM启动：SHA256 3a68fe36491c6bb1502e6b15abae73c1906d270a0a75b179a53bc642a2bf1adc，2803832字节；OTG自动接回Ubuntu，19581048字节2.123秒，整包CRC8a89baf4、固件CRCc24d335d及无线四段通过。BLE registered=1/connect=true，Wi-Fi服务ret0，等待App重新配网。相机32e6:9221重新枚举/probe0/committed1，尚未再次stream。真实命令录音现在复用cued入口，待实听/识别；未运动、未上传，不代表完整整机通过。


用户重新配网后串口确认wifi=1/ipv4_ready=1/IP10.3.0.214。新启动voicecam1800一次，VOICE SESSION ready mode=0、TRACK tx=0，尚未运动。证据evidence/device-cue-20260915/network-held-camera.log。等待人声准备后使用带短音device-once，不能重复启动本次camera会话。


2026-09-16：固定提示缓存取消一小时过期并保护固定项不被临时项淘汰，43项测试通过，网关38916预热26条成功。真实板端测试待RAM恢复；详见docs/整机恢复与提示缓存_20260916.md。


2026-09-16恢复完成：同一device-cue镜像19,581,048字节OTG1.935秒、全包/固件/无线CRC通过，NSH/BLE/Wi-Fi服务启动。摄像头32e6:9221枚举与probe0/committed1通过，尚未启动本次采集，等用户配网并准备再启动。网关38916永久固定提示缓存已部署；真实命令/三图上传未验收。当前Wi-Fi等待App重配，不沿用旧IP在线结论。详见docs/整机恢复与提示缓存_20260916.md。


新增阶段提示独立检查候选与并发停止回归，O0/O2通过，SDK单文件前驱保护同步通过，ARM64构建中未加载；详见docs/阶段提示独立调度_20260916.md。


最新：stage-watch-20260916独立ARM64构建及源码哈希核对通过，2803832字节，SHA256 5dd9bda315015ce2fd36490949aef545fc1ea3b5dbe95735b5976b44f79606f7。尚未加载，Ubuntu已恢复但无/dev/ttyUSB0，等待QinHeng接入。加载入口tools/load_stage_watch.py，当前板态未经复查。源码实现独立阶段检查不等于已实测同步延迟；详见docs/阶段提示独立调度_20260916.md。


串口重连后实查Wi-Fi在线10.3.0.214；随后为已授权stage-watch更新软件重启，拦住U-Boot并fastboot usb 1成功。当前Fastboot，先前Wi-Fi/BLE在线快照失效；Ubuntu仅见CH340，等待用户接入USB download gadget。新候选尚未加载，下一步tools/load_stage_watch.py --load。


stage-watch已RAM加载：19581048字节1.993秒，整包CRC dca5dde0、固件94615449及无线四段通过，镜像SHA256 5dd9bda315015ce2fd36490949aef545fc1ea3b5dbe95735b5976b44f79606f7。BLE registered1、Wi-Fi服务ret0，等App配网。相机32e6:9221/probe0/committed1，未启动本次采集。旧Windows服务已退出，本次恢复speech PID46364仅127.0.0.1:8000、bridge PID23784仅10.3.1.125:8001，26提示缓存2099200字节预热完成，聊天关闭。自动审批拒绝8000全网卡监听，采用仅回环的较小范围方案获准。新调度已部署但尚未真人/三图上传验收。


用户配网后已实查stage-watch固件radio_generation1/wifi1/ipv4_ready1/IP10.3.0.214；Windows语音health及设备网段网关TCP可连接。相机尚未采集，等待当前0/0无遮挡与人声准备确认，避免提前消耗1800秒单次采集窗口。证据network-confirmed.log。


真实启动云台首轮板端链路通过：real-track-cued-01.log，VOICE SESSION ready0，短音后录音起点间隔32693微秒，ASR150帧/completed1/intent1；VOICE MODE applied1，TRACK tx增长到74，94720字节提示播放result0/device0；随后显式halt且VOICE MODE applied0，tx保持74。该证据证明下发控制与软件停止，不等于物理位置ACK；等待用户动作和声音确认。ASR完成到软件TX启动2375082微秒，不是人声末端到声学出声。此轮device-once，尚未验证独立阶段worker、三角度或上传。

2026-09-16 音量覆盖：用户要求短音24 dB、语音10 dB；运行时已应用并收到板端确认，未重启。证据speech-volume-10.log，恢复脚本tools/set_speech_volume_10.py，边界见docs/播报音量设置_20260916.md。

录音回放排查与候选：docs/录音回放排查_20260916.md；app/k7sound/k7sound_main.c统一replay/TTS模拟输出路径，默认TTS10 dB；tools/replay_gain_build.py独立编译中。仅源码修正，未上板验收。

皮肤检测口令部署：device_voice_intent.c、board_voice_prompts.inc、voice_prompts.zh-CN.json、host路由及命令测试；Python4项/原生O0 O2通过；evidence/skin-command-20260916/verification.json和startup.log对应已加载版本。等待App配网与真人新口令测试，非完整闭环通过。

Ubuntu整机界面：host/whole_device/integration_panel.py、test_integration_panel.py；复用host/vision/k7_video_receiver.py；tools/launch_integration_panel.py部署，窗口PID36755。3项状态测试通过，截图evidence/integration-panel-20260916/panel.png已检查；真机视频接入和共存未验收，见docs/整机视频联调界面_20260916.md。


按钮禁用排查（2026-09-16）：窗口37910状态wifi=false、busy=false、held=true，实时视频正常。console-153455.log末尾曾有radio_generation1/wifi1/ipv4_ready1，之后命令无回包；console-154007.log持续TRACK但radio-status、device-stop、halt均无有效回应。不能把“串口可打开”视为控制通道健康，不能声称真实断网或已经停止确认。确认界面空闲后终止旧窗口，仅尝试Ctrl-C/Ctrl-U清行和radio-status，console-recovery.log仍无NSH/K7CLOUD回复；未重启板子、未重新启动相机。已部署窗口38317，保留实时视频，新增恢复控制通道按钮与禁用原因，网络解析仅接受完整字段，5项测试通过。串口驱动重新绑定需sudo而sudo -n不可用，未执行。等待用户仅断开重接QinHeng串口，不动视频OTG/供电；重接后需恢复窗口串口所有权并验证新radio-status，再允许语音测试。当前按钮故障未完成真机修复，云ASR视频并发问题仍未解决。


2026-09-16 视频结束复核：console-160041.log证明UVC requested_s=1800、elapsed_ms=1800226、53802帧、29.88fps、result0；TRACK END tx0/errors0，视频正常到期，不是新USB掉线。重连串口后radio-status及echo CONSOLE_READY均无响应，控制台仍未恢复。窗口已关闭，Ubuntu reset-watch PID38885独占串口，等待用户RESET最长600秒，看到U-Boot后拦截并fastboot usb1；没有主动发reboot或写闪存。恢复监听状态见reset-watch-state.json。UI源码增加采集结束清除camera就绪与恢复10秒超时，7项测试通过，尚未重新部署该增量。后续必须先查看watch状态，避免争用串口；成功进入Fastboot再接OTG和加载已校验固件，之后重新配网。

恢复完成：console-recovery-01独立证据目录，19581048字节OTG1.9106秒，包CRC d2370841、固件CRC9af381e8及四段无线校验通过，skin-command哈希不变。BLE registered1、wifi-service ret0、NSH恢复；panel39175已部署超时/采集结束提示。仅初始化Host/linkvideo，不启动采集、录音或运动。等待App重新配网，先单独ASR再视频并发对照；尚未修复并发根因。原第一次重载因usb.json证据保护在传输前拒绝，新增--run-label独立目录避免覆盖。

ASR无视频仍发生ENOSPC：app/k7sound/stream_ring.h、tests/k7cloud/test_stream_ring.c、board_speech_bridge.c；O0/O2通过，独立构建asr-backlog进行中，未上板。见docs/ASR发送积压修复_20260916.md。

视频并发单轮通过：panel48030、camera=true/video_fresh=true/frames2632/held=true；板端capture0 produced150 consumed150 receive0、asr0/completed1，云端准确final“皮肤检测。”，网关150帧/无错误，最大帧间隔32ms。会话起点到final4453ms、完成5156ms；无视频对照4313/5000ms，不是用户说完延迟。仅单轮通过，不等于长期稳定或三图闭环。证据video-concurrent-success.json与metrics/1789548193957643600.json，原始录音仅private。下一步用户点击真实整机测试，先启动云台再皮肤检测；当前相机已采集，不可重复启动。

用户授权放宽启动规则：含启动/openvela/openvlea触发云台，停止与否定优先，C O0/O2及Python16项通过；独立start-keyword构建中。见docs/启动关键词规则_20260916.md。

Ubuntu恢复后新启动关键词固件已RAM加载：19585144字节/1.9149秒，包CRC8ea7f21b、固件CRC1d3e3e81、无线四段校验通过，SHA1a4276bf…99ef44d。BLE registered1、Wi-Fi服务ret0。新版镜像/人脸框/33ms渲染窗口已部署PID2828，GTK/cairo及GI桥接验证可用，启动日志无异常；截图已检查布局，但无视频设备，不能算画面实测通过。Windows语音9568/网关39244正常监听。等待App配网、openvela link probe接入及用户相机待命；尚未真实新口令动作或三角度验收。

2026-09-16 17:38 相机就绪按钮修复：用户确认新版镜像预览流畅；窗口 Wi-Fi 在线、视频30fps但 camera=false。原串口日志没有一次性 VOICE SESSION ready，导致整机测试按钮灰色。新增有界 HOLD 确认：同模式 hold 不增加板端 revision，因此不能只等 applied；接受 requested=0/result=0 后的新 TRACK_HALTED 也可确认相机会话就绪。只收到命令回包、仅视频或失败回包均不能开放按钮。11项状态回归通过。窗口 PID3418 接回既有采集，无固件重启、无自动录音或云台运动。实查 camera=true、voice_enabled=true、held=true、busy=false、video_fresh=true，TRACK tx=0；证据 evidence/integration-panel-20260916/readiness-recovery.json。下一步用户点击整机测试说“启动”，真实新口令和三角度上传仍待验收。


你好前缀及三角度播报：device_voice_intent.c、board_voice_prompts.inc、host路由/prompt/UI、hello_keyword_build.py/load_hello_keyword.py。Python20项和原生O0/O2通过，候选构建通过未上板；Fastboot等待OTG，旧Wi-Fi快照失效。详见docs/你好口令与三角度播报_20260916.md。


加载完成覆盖：hello-keyword已RAM启动，固件SHA a9ddaae467447079d0426b29da81b2114be0a6fdfb4193ff3a6c4ca69540cd29；单包19585144字节1.939秒，整包CRC576b3abb、固件CRCc4f2f621、四段无线CRC通过，见startup.log。usb.json是USB传输阶段快照，其中ram_crc_verified/firmware_started=false不代表后续校验失败；后续以startup.log与deployed-state.json为准。BLE registered1、Wi-Fi shared start ret0。新版窗口PID11934，仅初始化Host与视频链路，未启动采集、录音或运动。用户回复已联网后板端新鲜回包仍wifi0/ipv4_ready0；蓝牙ATT及PROV命令有回包，但本轮联网尚未验收。Ubuntu仅QinHeng，等待App重新提交Wi-Fi及openvela link probe接回。请勿沿用旧IP在线状态。


右侧捕获阻塞实查：console-175358.log中epoch2正脸q5882和左脸q6667存储成功，q8718开始lost1，持续st6(VF_TARGET_CHANGED)/v0/done3直到q9471。不是证明右侧角度不达标；k7_photo.c目标锁定丢失后已有部分图片则不自动重置，避免混入不连续目标。触发可能为多脸、缺失超过350ms或框重叠低于0.30；本轮精确触发分支尚无区分日志，不归因于用户或背景人员。后续日志已有lost0，说明出现新测肤会话，不代表旧组三图完成。UI源码已增加角度无效不显示0°、匹配帧POSEQ lost1明确提示整组重拍；12项测试通过。当前正在实时测试，未重启/替换运行窗口，该UI修复尚未部署，板端失锁恢复策略未修改。


板端固定提示与上传修复：26条PCM2196480字节已冻结，local-prompts构建通过未加载；photo-retry入口及上传阶段诊断合并构建中。三图epoch3保存成功但上传-5/accepted0，后端未受理；串口重连仍无回应、窗口11934已退出，不能沿用旧运行快照。详见docs/板端固定提示与上传修复_20260916.md。


最新部署覆盖：photo-retry已RAM加载，固件SHA256 2cde51b7c1a228a883e1d54b3276250047b2623397abc3a22682c2653d8b055f；21784696字节OTG2.1269秒，包CRC a6e64082、固件CRC1f3c7e57、四段无线校验通过。BLE/Wi-Fi服务恢复；本地提示tracking_stopped实播result0，故意使用127.0.0.1作为云服务参数，实际日志local_prompt证实本地路径。首次脚本等NSH行尾超时，随后的tts-state为result0；修正脚本兼容配网日志交错后重测通过。重测时用户已配网，wifi1/ipv4_ready1，不能描述为断网实测；用户确认“听到了，清楚”。26条固件音频不依赖网关，动态TTS/云ASR仍需网络。窗口28435已部署新失锁提示、无效角度显示与独立重传按钮，camerafalse、视频尚未接入，等openvela link probe及用户相机待命；网关51424正常ready。三图重拍上传尚未验证，不能标记闭环通过。

最新三图复测：epoch2 front808/31180字节、left1635/28997字节、right2100/30351字节全部存储成功；photo_transport stage=left result=-11，accepted0。错误定位到左图TCP写入超时，尚未到后端受理；不能称接口没实现，也不能称上传已完成。候选网关PID4568增加可选--receive-buffer4096，监听socket实际SO_RCVBUF4096，未改系统全局网络参数。约92KB大包通过小接收缓冲的本机测试通过，但真机效果未验收。窗口保留三图、云台停止，已请用户点击“重传已拍三图”做同一组重试；不重启、不重拍。证据upload-left-timeout.json、gateway-small-rx.log。

三图上传TCP缓冲修复候选：独立velavision_photo_tcp配置预留32个IOB、总256个256B缓冲、发送积压8192B；board_speech_bridge.c保留真实connect错误。tools/photo_tcp_build.py审计/保护同步/构建/收集、tools/test_tcp_connect_vm.py O0/O2通过，photo-retry O0/O2及网关13项回归通过。固件SHA bfac0d73ce9d9d94c443e2907def6ec91b414c0c571553ce7d45e2b27094d027、5007520字节，尚未上板。旧组三图上传left -11后网络不通；原生重传connect -5；旧控制台无法恢复，已告知用户RESET会清除RAM三图并请求按键。具体见docs/三图上传TCP缓冲修复_20260916.md，不能宣称上传已修复或后端已受理。

photo-tcp修复版已RAM上板：SHA bfac0d73ce9d9d94c443e2907def6ec91b414c0c571553ce7d45e2b27094d027，单包21784736B/2.1755秒，CRC包0f53c5e4、固件938d8660、无线四段均通过。App真实配网10.3.0.214，Ping3/3后30/30。尚未拍新三图，后端受理仍待验证；当前Windows锁屏，用户需解锁后接回openvela link probe。窗口37503持有串口，未启动相机或运动。详见docs/三图上传TCP缓冲修复_20260916.md。

2026-09-17视频隔夜断开根因：k7_link_probe.c视频模式固定3600秒到时正常退出，日志LINK END result0 stop0。已修复为视频持续运行，bulk诊断600秒/1024块及fault/DMA停止保护不变。独立video-continuous构建和加载完成，SHA89881e468bb6bd290efc0a3fe302ad19839bbfd8ca6423cca20d1a9b11d39314；TCP8KiB队列/8KiB IOB预留经实际配置复验保留。窗口46465，等待重新配网、视频接入与真人重拍；三图后端受理和超过一小时连续验证未完成。代码工具与证据见docs/视频一小时退出修复_20260917.md。


## 2026-09-17 真机三图上传验收
用户完成三角度拍摄并确认提交。板端 photo_transport stage=backend_ack result=0；photo_upload epoch=2 result=0 accepted=1，任务 aa9c678f-c7b7-4e0c-a93e-05e6a071a389。网关保存正面26344B、左侧25785B、右侧26124B，后端 accepted=true/status=queued/photoVersion=1。上传后 Wi-Fi 仍在线，后续多轮 ASR produced=150/consumed=150/result=0，视频仍持续接收。本轮上传闭环通过；报告未完成，重复大图及长期稳定性未验收。脱敏证据 evidence/photo-upload-accepted-20260917/verification.json；原图保留 private，不复制到公开证据。


2026-09-17：报告任务查询/brief 实取与独立服务，见 docs/测肤任务报告接续_20260917.md。8项测试通过；板端播报和正式SSE未完成。

2026-09-17：板端报告K7S1R及mock SSE逐句TTS实现、37项测试通过、独立固件已构建未加载；网关51104启用mock-sse，等待OTG。见docs/板端报告流式播报_20260917.md。

2026-09-17：report-speech固件已RAM上板，真实后端mock SSE→TTS→K7三段播放result0，用户实听全部完整、间隔可以。证据evidence/report-speech-20260917/board-report-verification.json；完整新一轮拍照自动报告衔接尚未复测。

2026-09-17：真实原生拍摄→受理→报告衔接O0/O2测试通过，evidence/report-speech-20260917/native-photo-report-chain.txt。等待用户接回视频及现场实拍。

2026-09-17：左右侧脸独立偏航容差13°、正脸7°；fusion_core与原始角度门一致，边界O0/O2及原生照片流程测试通过，独立固件编译中。见docs/侧脸容差调整_20260917.md。

2026-09-17：命令语音静音400ms提前收尾、阶段提示抢先采集轮次候选；PIO清理/端点/ring原生O0/O2测试通过，固件已构建尚未加载，等待OTG。见docs/语音延迟优化_20260917.md。

2026-09-17 报告播报已切正式brief来源，停止mock文案和测试声明；见docs/真实报告内容播报切换_20260917.md。网关18860，窗口74371，19项测试通过，喇叭待验收。

2026-09-17 官方报告审阅稿：按官方DOCX模板完成3.1–3.7及信息表，10页Word/PDF，原节布局、页眉页脚、编号保留；系统框图、流程图及有限实测数据已加入，团队信息、部分型号及未统计项明确待补。交付deliveries/contest-report-20260917/，生成tools/build_contest_report.py，校验qa-result.json。未提交或推送。报告43295事件为编写时快照，不是Token量；后续日志刷新以manifest为准。
2026-09-17 14:44周期只读复查：虚拟机state.json显示相机采集已结束、本次启动不可重复采集，camera=false/held=true/busy=false/voice_enabled=false、Wi-Fi缓存状态true。未向板端主动查询，因此不据此重新验收实时网络。界面保留“后端已受理·报告已播完”是历史状态，不覆盖brief新模式喇叭待验收。未自动重启、录音或运动；后续现场测试需先恢复相机会话。

2026-09-17 两小时/10轮/异常恢复验收启动准备：独立stability-session构建成功、未加载，现处Fastboot等待OTG。只延长voicecam至最大10800秒，保留单次DMA保护；UI增加显式长测模式和单串口遥测，默认1800秒。只读soak_acceptance等待新鲜视频/网络/BLE加密/内存基线，7200秒后仍需人工内存趋势审查。不将等待时间或历史结果计入本轮。详见docs/整机稳定性验收_20260917.md。

2026-09-17 第四轮失锁：epoch4正脸q9763成功，q9938起lost1持续；首次触发原因未知。失锁后重复photo请求不重置的缺陷已改源，正常重复保护保留；O0/O2测试通过，独立候选构建中未部署。共存采样40秒因busy期间遥测停发退出，属于观测失败，不能断言网络断开。见docs/第四轮目标失锁排查_20260917.md。

2026-09-17 用户要求报告结束恢复真实跟随：photo_prompts加入同组三图/取消/hold revision检查及恢复applied确认，显式停止优先。实际模式与报告链O0/O2测试通过；report-follow独立合并构建中未部署，见docs/报告结束恢复跟随_20260917.md。

最新覆盖：合并固件report-follow构建/哈希校验通过，5011616B，SHA3642d9155f8456d216ddb14cc4cacfb9139a6f19bc5997f507638a804306260b。首次构建接口声明缺失失败日志保留，第二次增量通过。更新前新鲜状态held=true/busy=false，已释放UI82847、软件重启并进入Fastboot；Ubuntu未接到OTG，已请求用户连接。新固件未加载，旧Wi-Fi/视频快照失效，后续需重新配网。当前不运行长测，soak-run-01观测失败记录保留。

部署覆盖：report-follow已RAM启动，21788832B/2.199秒，整包CRC64af471d/固件CRC13147139/无线四段CRC通过。BLE服务registered1、Wi-Fi服务ret0，新界面启用忙时单串口遥测；未自动录音或运动。等待App重新配网及openvela link probe相机待命，报告结束真实恢复跟随仍待实机验证。

2026-09-17 再次失锁已实锤reason1/faces2/has_face1/gap0；候选区分明显小且分离的背景脸，仍保留多人歧义保护。O0/O2边界与模式测试通过，photo-foreground构建中未部署；见docs/背景人脸触发失锁修复_20260917.md。

最新覆盖：photo-foreground独立构建/哈希校验通过，5011616B/SHA189c3e5cbf0953586cdcd7b9af2ee164c1ebb7e57d5f30cfa240c3ef8913ce00；报告结束恢复与失锁重试保留。已确认held且busyfalse后释放UI99153，软件重启到Fastboot。Ubuntu无OTG，已请求用户接USB download gadget。未加载新固件，旧Wi-Fi/视频快照失效，验收暂停，不重复催促。

部署覆盖：photo-foreground已RAM加载启动；单包21788832B/2.280秒，包CRC85119bdd/固件CRCf2aaadf9及无线四段校验通过。蓝牙App已连接并订阅，加载末尾正在扫网；不据此宣称DHCP完成。新联调界面已恢复，无自动录音或运动。背景小脸过滤与报告后恢复实体跟随仍待真人验证，两小时长测未重新开始。

2026-09-17 16:42只读观察：photo-foreground运行期间epoch2/3两组完成三图，报告各3段result0，随后report_resume_tracking result0且VOICE MODE applied1。仅证明软件恢复执行，实体云台跟随仍待用户确认。epoch3同task重复上传/播报不计作额外轮次，存在重复消费旧完成组三图的行为待排查。当前用户已停止，Wi-Fi/视频新鲜、heldtrue/busyfalse。证据observed-two-rounds.json；两小时长测未启动，不能算10轮通过。

2026-09-17 17:47只读复查：窗口状态持续更新、Wi-Fi新鲜回包、视频135133帧且fresh，串口无错误，云台held且busyfalse。BLE connected=0、connections=2/disconnections=2、last_reason=8；两小时共存基线不满足，未启动新计时，不自动重连或重启。当前“报告播放未完成”对应同一epoch3旧任务再次播放，report_done result=-125 segments=2，与已停止状态一致，不能据此判定后端故障；旧任务重复不计新增闭环。证据evidence/photo-foreground-20260917/heartbeat-1746.json。下一步需要恢复手机BLE持续连接并核对剩余相机会话时长，避免相机10800秒上限打断新的7200秒观察。

2026-09-17 新后端切换：上传与私有设备认证配置统一至用户指定HTTPS根地址。OpenAPI及设备登录200，刚签发Bearer查询当前任务/测肤任务均401 AUTH_REQUIRED；待后端鉴权链路排查，未上传新照片或验收。见docs/新后端部署联调_20260917.md。

2026-09-18 09:13复测：新后端设备登录HTTP200，使用新token查询current-assessment HTTP200、currentAssessment=null，昨天该接口401问题本次已恢复。旧环境taskId只读探测返回404 RESOURCE_NOT_VISIBLE，不能宣称旧任务迁移或新报告已验收。尚无新环境当前任务，需下一轮实际拍摄提交后验证任务→brief/播报。未重传旧照片，未启动录音或云台。证据evidence/backend-redeploy-recheck-20260918.json。

2026-09-18 板端复测准备：恢复流式语音服务8000及新后端网关8001，26提示预热完成；Ubuntu尚无CH340/K7 USB，未发板端命令。见docs/板端新后端复测准备_20260918.md。

2026-09-18 交接文档：汇总当前旧固件阻塞、新后端鉴权复测、RAM加载入口、语音网关、整机闭环和稳定性验收顺序；已排除所有凭据。见docs/交接文档_整机测试与新后端_20260918.md。

2026-09-19 云端后端配置适配：新增backend_profile统一解析后端地址；DeviceBackend、三视图上传、任务轮询、报告brief和讲解SSE共用同一地址。默认仍为局域网开发配置，新增assessment-upload.cloud.json及board_bridge.py的--backend-base-url/--assessment-config参数；上传前增加认证地址与上传地址一致性检查。主机核心23项回归通过，Python compileall通过。见docs/云端后端切换说明_20260919.md。
