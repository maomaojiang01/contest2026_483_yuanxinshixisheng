# 录音回放与真人空识别排查

用户确认12轮命令测试中说了“开始测肤”，但云端每轮150帧、completed、无文字，未进入拍摄。固定合成语音经过同网关返回“开始侧敷。”，证明当时服务可以返回文字，但未通过精确命令验证。

单次capture-cued和replay均返回result=0。用户听到录音前短音，但诊断回放6 dB、12 dB均未听到。不能将软件成功等同可听性成功，也不能据此判断麦克风损坏。

代码确认旧replay只设置DAC衰减，OUT2保持初始化0x14；正常TTS先调用kd_set_playback_max，把OUT2设为0x21，再设置DAC衰减。同一数字档位并非相同输出配置。

候选replay-gain-20260916统一replay与TTS模拟输出配置，允许replay使用10 dB；默认TTS由16改10，短音24不变。构建入口tools/replay_gain_build.py，受保护前驱49fd1130155a5db147a7ca1c7ac8b353f5c7fbd30d0a6eb8028cc11d6c451f42。尚在编译，未加载、未实听验证。此修正解决诊断配置差异，并非已证明解决ASR空文字。

导出PCM串口日志完整可解析53856/96000字，并发输出造成内容不完整，不能作为完整录音送云或评估时长。原日志保存在evidence/stage-watch-20260916/mic-replay-diagnostic.log，后续应采用带完整性校验的导出方式。

当前板端保持原stage-watch固件，最后软件停止确认applied0；没有拍摄、上传、重启或刷写。Windows语音与网关服务未重启。真人再次测试需要明确提示开始；不得在用户未准备时持续录音。

最新覆盖：独立ARM64编译及源码核对通过，SHA256 a033dd854775e23fe99519399f8b7063ac68cc4a9497b62b0f3a33b07f54be8e，2803832字节。OTG自动接入并RAM加载成功，19581048字节/1.819秒，整包CRC9b7c3835、固件CRC d3b8b19c与无线四段校验通过。NSH启动、PROV registered1、Wi-Fi服务ret0；重启后Wi-Fi未配网，camera未启动。待用户准备后用tools/diagnose_mic_replay_fixed.py做本地录音与10 dB回放，无需先联网。尚未实听验收。

用户复测：新回放10 dB仍无声，模拟增益差异不能解释全部问题。部分可解析左右样本相关系数约-0.989，仅作为反相线索；串口导出存在丢字及跨次冲突，未得到可信完整WAV，不可据此声称ASR根因。新增单声道回放候选及dump逐行限速，尚未编译/加载。另针对真实合成识别到的开始侧敷，仅增加精确同音别名（不做模糊包含），原生O0/O2命令测试通过；否定、询问、冲突命令仍不触发，尚未部署。当前运行replay-gain固件，Wi-Fi重启后待配网，云台未启动。下一步将命令别名与回放诊断合入同一候选，一次更新，并用启动云台与开始测肤作同条件对照。

后台接续：replay-mono-20260916已合并单声道回放、串口dump限速及开始侧敷精确命令别名，两个SDK前驱哈希已审计并通过check/apply/check保护同步。独立构建已启动PID20312，未收集成品、未部署、未启动录音或运动。下一步tools/replay_mono_build.py collect核对实际编译结果。

最新：replay-mono独立ARM64编译通过，2803832字节，SHA256 3d292eddcb605601ea72f9dea45dbc0782c8bbe27f6899b547359673d07de7a3；全部核对源码哈希通过，加载脚本本地校验通过。尝试进入下载模式时/dev/ttyUSB0打开即I/O错误，尚未发送重启命令。lsusb仍列CH340，sudo -n不可用；已请求用户重新连接QinHeng串口。新候选尚未加载，不能沿用旧联网或相机状态。

串口重连后已恢复并成功软件重启、拦住U-Boot，fastboot usb 1返回OK；Ubuntu仅见CH340，未见18d1:4d00。当前板子停下载模式，等待USB download gadget接入；replay-mono尚未加载，旧无线在线状态失效。

最新加载：replay-mono-20260916已RAM启动，OTG19581048字节2.043秒，整包CRC c543f934、固件CRC8d87709d及无线四段校验通过。固件SHA256 3d292eddcb605601ea72f9dea45dbc0782c8bbe27f6899b547359673d07de7a3。蓝牙registered1、Wi-Fi服务ret0，重启后未配网；相机未启动。单声道回放/限速dump/同音别名均已部署，实听、真人识别与拍摄上传尚待验证。下一轮tools/diagnose_mic_replay_mono.py，需用户准备确认。

replay-mono真人验收：用户确认10 dB单声道回放完整清楚；录音保留数据经限速重复导出合并96000/96000字、无冲突，得到48000帧16kHz单声道WAV。证据mic-replay-mono.log、mic-mono-coverage.json、mic-mono-channel-0.wav。录音回放分项通过，ASR/三图闭环未验收。尝试将同一录音经已有云服务做识别对照被自动审批拒绝，要求明确授权该段录音外发；已向用户解释并请求授权，未发送。已另请用户App重新配网。

用户明确授权同一3秒录音送现有云ASR后，对照成功：150帧发送/确认，final sentence_id760返回开始测幅。证据retained-mic-asr.json。这证明本次保留录音左声道可被云端识别，不等于板端实时上行空文字已解决。已源码增加开始测幅精确别名，并补否定/询问/冲突保护测试，O0/O2通过；当前replay-mono固件尚不含此新增别名，需下次合并更新。单声道回放真人完整清楚分项通过，完整闭环仍未验收。

用户改口令为皮肤检测：新增精确皮肤检测/开始皮肤检测，保留原正确测肤词；移除试验同音别名。同步两条引导语及host参考路由，Python4项/原生O0 O2测试通过。skin-command-20260916独立编译中，尚未部署；Windows网关身份核对后重启PID58428，沿用LAN8001、回环speech8000，重新预热26条新提示语，预热结果待核查。

skin-command候选已编译并核对SHA256 9325afe85b768c84b9ec5e8f538c41ba156aca3b2f256a3c2592b3fa1e4603bf，2803832字节。网关58428预热完成26条/2124800字节。板子已软件重启到Fastboot，Ubuntu尚未接到OTG，已请求用户接入；尚未加载新口令。入口tools/load_skin_command.py --load。

skin-command已RAM加载：19581048字节1.980秒，整包CRC d2370841、固件CRC9af381e8及四段无线校验通过；镜像SHA2569325afe85b768c84b9ec5e8f538c41ba156aca3b2f256a3c2592b3fa1e4603bf。NSH启动、BLE registered1/Wi-Fi服务ret0，当前尚未配网，相机未启动。新口令皮肤检测及两条语音引导已部署，网关26提示预热完成，真人新口令未验收。已请求App配网。

用户配网后已实查skin-command板端radio_generation1/wifi1/ipv4_ready1，IP10.3.0.214。等待准备确认，下一轮使用asr-live仅验证皮肤检测实时识别，不调用device-once、不启动视觉运动。

新口令真人实时ASR通过：第二轮最终文字皮肤检测。（sentence_id860），150帧/completed/errorfalse。网关final在4453ms（128帧时），completed5015ms；计时从网关连接开始，不等于说完后的等待时间。证据recognition-only-final.json及skin-command metrics。首轮仅记录非空5字，缺原文不可冒认正确；已恢复K7_SPEECH_RESULT_FILE，网关PID62844，26提示预热2142720字节。两轮均asr-live，只识别，未启动云台/相机或拍照。
