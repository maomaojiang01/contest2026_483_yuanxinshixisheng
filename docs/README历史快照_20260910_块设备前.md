# VelaVision

2026-09-10 最新：原生 eMMC 低速初始化和 LBA0/LBA1 重复读取通过，识别 MBR/GPT 签名；读取后 BLE 真配网、网关 5/5、30秒保持通过。当前 emmc-readonly-20260910 从 RAM 运行，未写盘；完整 GPT 表与文件系统仍待接入。见 docs/eMMC原生只读接入_20260910.md。VoiceLink 主机候选及日志已接收，次会话继续共享 Wi-Fi 事务服务候选。

2026-09-10最新：model-arena-20260910已RAM上板；专用1GiB模型池创建成功，384MiB分配/98304页两轮稀疏读写/释放恢复通过。BLE真配网IP10.3.0.214、网关5/5、30秒保持通过；客户端主动断开，Wi-Fi在线。系统堆仍原范围、CPU仍单核，eMMC未写。见docs/独立DDR模型内存池_20260910.md与evidence/model-arena-20260910/。

2026-09-10：model-arena-20260910独立编译/链接通过，新增显式1GiB CPU模型池和k7mem稀疏测试入口，未上板。原系统堆/DMA范围未扩展；CPU仍单核，八核规划见docs/八核CPU接入与任务分配_20260910.md。代码/构建见docs/独立DDR模型内存池_20260910.md；当前无线固件未替换。

2026-09-10 10:03恢复完成：用户重新上电后已重载wifi-arp-20260909，实际镜像哈希9dc19c97c2d2aa5f49bc3bca36d04f026c25c096ec3894605ba7ac4d7dc4ce5d；Windows BLE真配网回IP10.3.0.214，网关5/5。客户端主动断开，Wi-Fi在线、BLE服务保留。没有重复mmc part，没有写eMMC，DDR扩展尚未启用。证据evidence/resource-recovery-20260910/。

2026-09-10 资源接入当前状态：已完成64位DDR审计工具及8项测试，实测eMMC为29.1GiB；U-Boot执行只读mmc part后无回应，Wi-Fi/BLE未恢复，等待断电重上电。未写eMMC、未启用DDR扩容。见docs/DDR与eMMC资源接入_20260910.md。

2026-09-10 最新：wifi-arp-20260909 已 RAM 上板；Windows BLE 真配网、WPA2/DHCP、真实 IP 回包通过，IP 10.3.0.214。空 ARP 缓存起步网关 5/5（含首包），BLE/Wi-Fi 60 秒保持通过。测试客户端已主动断开，Wi-Fi 保持在线。仅本轮有限验证，非长期稳定或互联网验收。见 docs/WiFi上电复验_20260910.md。

Wi-Fi 完善增量：ARP 等待 20→250 ms、ARP 收发计数、最近 PROV 请求结果；新固件 `wifi-arp-20260909` 编译和协议/桥接测试通过。前端在线扫描保护及 12 项测试通过。COM8 无回应，新固件尚未上板；见 `docs/WiFi完善与ARP等待调整_20260909.md`（统一源码入口根目录下）。

日志查看：当前开发会话「接续VelaVision运行时接入」已改为按北京时间分日保存，9 月 9 日记录位于 `logs/maomaojiang01/2026-09-09/`，正常回复结束后自动更新；9 月 8 日记录保留在原目录。详见 [自动日志采集](docs/自动日志采集.md)。

**统一开发入口：`E:\openvela\VelaVision`。源码、前端、STM32 配套、构建证据和 AI 日志均在本仓库内。**

目标是在 KICKPI K7 / RK3576 原生 openvela 上完成视觉感知、双轴人脸跟随、引导三视角拍照、仪器目标检测，以及 BLE 配网和 Wi-Fi 通信。Ubuntu 用于编译与调试；旧 PC 推理程序保存在 `legacy/`，不作为板端原生功能的证明。

**参赛主线：新硬件适配赛道。** 官方待适配清单明确包含 KICKPI K7；以原生 BSP、关键驱动、可复现构建和机器人演示形成交付。机器人运行时 Agent 是后续应用目标，区别于辅助驱动开发的 Agent。见 [官方赛道对齐与目标](docs/官方硬件赛道对齐与目标_20260909.md)。

## 当前统一版本

**最新 BLE 真配网已接通：`ble-connect-20260909` 从 RAM 运行，Windows BLE 加密提交真实凭据后获得 WPA2/DHCP 成功回包和 IP，status 一致，connect:true。** 网关首次 4/5，首包 ARP 相关失败仍待排查；不是长期稳定验收。当前已主动退出 Wi-Fi、保留蓝牙服务供前端从扫描开始。见 [新版前端操作说明](frontend/真实联网联调说明_20260909.md) 和 [真机证据](evidence/ble-connect-20260909/device-state.json)。以下此前状态按对应版本保留。

**2026-09-09 实测结论：30 分钟 BLE/Wi-Fi 连接保持通过；10 次手机重连、加密与列表回传通过，用户确认每轮收到列表和 scan_done。** 共存网关 1497/1500，严格零丢包未通过。测试结束后的 Wi-Fi 恢复曾拿到 IP 但网关不通，19:54 复查恢复 5/5；另发现 BLE 超时断开（reason 8），用户确认仍在附近且 App 前台，原因待查。不能宣称正式稳定交付。前端仍 `connect:false`。见 [前端实测与边界](frontend/无线稳定性与重连实测_20260909.md)。

**此前 Wi-Fi 独立长测（18:13–18:43）：Wi-Fi 保持在线 30 分钟，1,500 发/1,499 收，丢包约 0.067%，内存稳定；按零丢包标准未完全通过。** 手机在计时前断开，蓝牙长连接尚未验收。该次测试结束时 Wi-Fi 在线、手机 BLE 未连接；本轮同固件重新测试共存保持。见 [完整测试结果](evidence/stability-20260909/wifi-run-01/测试结果.md)。Agent 尚未部署，模型服务待确定，见 [部署接续](docs/Agent部署接续_20260909.md)。

**此前短时真机结果：原生 Wi-Fi WPA2、DHCP、首次网关通信和 A-MSDU 接收通过。** 当前 RAM 固件 `wifi-amsdu-index-20260909`，Lansee IP 为 `10.3.0.214`，首次网关 Ping 40/40、0% 丢包，实际交付 1 组 2 子帧聚合数据；手机已重新建立加密连接，并保持连接完成网关 50/50 测试，无新增断开。前端仍 `receive_credentials:true/connect:false`，正式配网事件、长期运行/重密钥、DNS 与互联网验证待完成。见 [聚合帧修复与真机记录](docs/WiFi聚合帧接收修复_20260909.md) 和 `evidence/wifi-amsdu-index-20260909/device-state.json`。

以下为历史阶段记录，以顶部最新状态为准。

**认证阶段记录：** `wifi-auth-20260909` 在 RAM 运行，Lansee 四次握手及 PTK/GTK 安装返回成功；测试随后主动断开，尚未获取 IP。手机 X → 真实扫描列表已由用户确认。原生数据通道和 DHCP 正在独立 `wifi-ip-20260909` 中开发，前端仍为 `connect:false`。见 [认证与 IP 接入记录](docs/WiFi认证与IP接入_20260909.md)。

**历史板端：`phone-rpa-20260909` 已从 RAM 启动，配网服务及广播启动成功。** 修复身份解析后错误使用身份地址进行配对确认的问题；实际地址选择函数回归测试和独立固件编译通过，手机重新配对与 X 业务收发待现场验证。上一版首次加密/100 秒保持通过，但随后重新配对复现 0x04，因此不能宣称手机稳定性通过。详见 [重复配对地址修正](docs/手机重复配对地址修正_20260909.md) 和 `evidence/phone-rpa-20260909/device-state.json`。下方为此前阶段验收记录。

**历史信息接收版本：`credentials-20260909` 已从 RAM 启动，BLE 真实扫描和 Wi-Fi 信息接收回执已做真机验证。** 64 条扫描记录含 Lansee，校验后回复已收到并擦除密码；没有尝试联网。前端使用 `receive_credentials:true` 与 `submitCredentials()`，`connect:false` 保留。见 [最新联调说明](frontend/信息接收联调说明.md) 和 [本轮开发记录](docs/配网信息接收开发_20260909.md)。旧集成镜像、EAPOL 阶段和 id36 长测结论如下保留，本轮不等于视觉/NPU/云台联合验收。

**历史 EAPOL 开发增量：** [Wi-Fi EAPOL 接收链路](docs/WiFi-EAPOL接收进展_20260909.md) 已接入统一应用并编译通过，队列与编解码的 ASan/UBSan 测试通过；新镜像在 `artifacts/eapol-rx-20260909/`，尚未上板、尚未完成 WPA/IP。下方固件哈希和 80 项一致性为首次统一快照。本轮源码与验证见 `evidence/build/eapol-rx-20260909/verification.json`。

`VelaVision-integrated-20260909.1` 已完成同一固件的编译与链接：`k7host`、`gimbal`、`k7npu`、`k7radio` 都在一个 `nuttx.bin` 内。固件 1,911,800 字节，SHA-256：

```text
ea9e90b0fbe23201114a8cb692d1c0db5180ee42d47907752489f6b4195d8580
```

本次未上板运行集成固件、未刷 eMMC、未启动云台。编译成功不等于联合真机验收通过。80 项已验收视觉/云台/NPU/无线源码比对全部一致，见 [源码保留核对](evidence/accepted-source-comparison.json)。旧镜像保存在 `baselines/`，不被新构建覆盖。

## 从这些位置继续开发

| 内容 | 项目位置 | 当前边界 |
| --- | --- | --- |
| K7 板级与合并配置 | `board/kickpi_k7/` | `configs/velavision_integrated_local/defconfig` |
| RK3576 芯片/公共层 | `port/new/nuttx/`、`port/tracked/nuttx/` | 公共修改另存 `patches/`，尚未上游合入 |
| 人脸/姿态/三视角拍照 | `app/k7host/` | 原生 CPU 已做阶段真机验证；照片由 Ubuntu 保存 |
| 云台 UART 唯一出口 | `app/gimbal/` | 保留已确认标定；无 MCU 位置反馈 |
| NPU | `app/k7npu/` | 固定 INT8 实验已验证；完整模型运行时未完成 |
| BLE 与板载无线 | `app/k7radio/` | 真实扫描、30 分钟保持/10 次重连、原生 WPA2/DHCP 已实测；测试后异常断连与正式配网待处理 |
| Wi-Fi 数据与认证 | `app/k7radio/` | 网关通信与 A-MSDU 已实测；旧实验保留于 `work-in-progress/wifi-link/` |
| 前端接入 | `frontend/` | uni-app 适配器、示例、协议和实测证据 |
| 视觉预览/照片落盘 | `host/vision/` | Ubuntu 工具，不代替 K7 人脸/姿态推理 |
| STM32F103RCT6 v1.3 | `mcu/stm32-v1.3/` | 硬件 PWM 与串口固件源码 |
| 原 PC / RK3568 交付 | `legacy/pc-delivery/` | 完整保留，作为后续原生移植参考 |
| AI 开发日志 | **`logs/maomaojiang01/`** | 两个真实主会话、一个 manifest，官方格式校验 |

以前的阶段目录保留为历史证据；此后以本仓库为入口。VM `/home/swl/openvela` 是外部 SDK/编译工作区，通过 `tools/sync_sdk.py` 同步本仓库源文件，不把临时 candidate 目录当作独立产品。

## 编译入口

最新无线 IPv4 配置使用 `bash tools/build_wifi_ip.sh /home/swl/openvela`，见 `board/kickpi_k7/configs/velavision_wifi_ip_local/defconfig`。下方 `tools/build.sh` 是初始合并配置入口，不能代替最新无线版本的构建记录。

将本仓库同步到 Ubuntu，然后运行：

```bash
bash tools/build.sh /home/swl/openvela
```

脚本先核对 SDK 基线和文件哈希，发现未知改动即停止；再同步源码并编译到独立目录 `cmake_out/velavision_integrated_20260909`。它不连接串口、不启动设备、不写 eMMC。见 [构建与集成说明](docs/构建与集成说明.md)。从空机器获取全部依赖的干净构建尚未验收。

## 日志与代码对应

GitHub 归属 **maomaojiang01**，队伍 **contest2026_483_yuanxinshixisheng**。日志直接在源码仓库内：

2026-09-09 已补 Windows Codex Stop hook，配置已安装、真实会话直接调用与官方校验通过，**已信任启用，Codex CLI 真实自动触发验收通过；已打开的桌面任务需重新打开以加载配置**。步骤见 [自动日志采集](docs/自动日志采集.md)。下方两会话说明为原集成快照；新增日志配置会话与以后自动采集会话以 `logs/maomaojiang01/manifest.json` 为准。

`logs/maomaojiang01/manifest.json` 是最新数量、哈希和文件列表的唯一入口。两个项目主会话按真实北京时间分日，跨日 seq 连续；另保留真实日志配置/验收会话。旧主会话覆盖 BSP、摄像头、跟随、三视角拍照与 NPU；当前主会话覆盖无线、前端、集成与后续开发。历史快照数量不作为当前累计数。

查看 [代码、日志与验证对应表](docs/代码日志对应表.md)。源码哈希、SDK 基线、构建结果、原始会话行哈希和官方校验输出均有记录。历史开发尚未形成逐次代码提交，因此不能补造“每条日志对应一个 Git commit”；本次用冻结源码清单和实际证据建立对应关系。

导出范围是两个明确选定的项目主会话，不另行重复导出子任务继承的历史，不补造工具结果、内部推理或工时。旧会话属于之前的 Windows 工作区，本次依用户要求合并旧开发资料而显式纳入。密码等内容已脱敏，`health=degraded` 如实说明快照范围有限。

官方格式复核命令：

```bash
python3 tools/official-validator/tools/validate-log.py logs/
```

见 [官方校验原始输出](evidence/official-log-validation.txt)。校验器和 schema 是官方提交 `10743591d1034480ecee7c8ffffe9bb251d4474d` 原文件；自动采集验收范围见上文，格式通过不代表无遗漏或已上传。

## 接下来完成什么

1. 补 BLE 长连接、重连和业务并发验收；将已实测的原生 WPA2/DHCP 接入前端正式配网事件。
2. 补齐全新环境复现、芯片/公共层补丁清单和赛事交付材料；当前工作仍未提交远程。
3. 部署最小文字 Agent，先读取真实设备状态，再接单次拍照；模型方案待定，不启动云台。
4. 接完整 NPU 模型及仪器检测，随后扩展语音与整机协同，保留跟随/拍照基线。

本目录来自用户指定的 GitHub fork，分支 `dev-ai-contest-2026`。本次都是本地未提交改动，没有 push、PR 或合入官方仓库。AI 协作覆盖方案、驱动、调试、测试与文档，记录见同仓 `logs/`，自建 Skill 在 `skills/`。
