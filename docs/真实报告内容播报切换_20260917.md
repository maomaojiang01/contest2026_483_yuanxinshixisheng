# 真实报告内容播报切换（2026-09-17）
用户要求取消联调测试声明、直接播皮肤检测结果。最新服务器Swagger仍明确report-narration-stream为固定MOCK，因此不删除声明后继续冒充真实报告，而将网关K7_REPORT_NARRATION_MODE切为brief。使用设备认证查询任务，再根据reportId读取brief，按后端conclusion/description分句TTS；保留两段预合成队列、逐段ACK、任务替换保护。未新增诊断或分数。
网关18860/8001已监听，替换空闲旧51104；界面74371已恢复，报告按钮和状态去除固定mock标签，无自动录音/运动。最新任务report_ready，brief可读，但description为空，因此只能播后端现有结论并提示未提供详细说明，不能宣称已提供完整AI讲解。报告相关和界面19项测试通过。未在本轮自动播放，真实喇叭验收待用户点击“播放皮肤检测报告”。Wi-Fi与板端固件保持，无刷写。
历史mock-sse代码保留为显式联调模式，当前部署不启用。未来切real-sse需先确认后端已替换为实际报告生成。
