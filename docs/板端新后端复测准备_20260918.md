# 板端新后端复测准备（2026-09-18）

新后端设备鉴权09:13已恢复，当前任务为空，尚无新环境三图闭环。

恢复电脑服务：旧speech-service/.venv指向不存在的C:/Users/Admin/anaconda3/python.exe，启动失败。沿用已安装private/streaming-dependencies及Codex Python，启动host.streaming_asr.serve（8000/PID18924，health ok），board_bridge（8001/PID38660，ASSESSMENT_READY prompts=26/cache_bytes=2152960/chat_enabled=0）。网关沿用brief真实报告模式，新后端认证/上传配置一致。WLAN仍10.3.1.125。

Ubuntu SSH已恢复，但两次lsusb均无CH340及K7 USB设备，/dev/ttyUSB0不存在。Windows CH340 COM8状态OK。用户回复接入后仍未在Ubuntu出现，已请求通过VMware“连接（断开与主机的连接）”接入；未启动第二串口所有者，未发送录音/云台/重启命令。旧UI state.json超过15小时，不作为当前网络/相机状态证据。

日志：private/speech-stream-20260918.*.log、private/gateway-ready-20260918.*.log。测试服务已运行，真机测试仍待USB接入。
