# 整机板端整合接续

## 当前目标与运行位置

用户要求持续推进直到真实整机整合完成。近期继续使用 Windows 10.3.1.125:8000 的阿里云 speech-service 和 :8001 K7 语音桥；服务器 10.3.3.170 已准备运行环境，密钥由后端配置，尚未切换。
最终命令、云台、相机、测肤流程、播放控制在 openvela 上运行。host/whole_device 是测试和参考实现，不能当作最终产品必需的电脑控制器。

## 本轮候选

- device_voice_intent.c 直接解析云端 JSON 事件，明确 final 命令在 completed 且采集成功后才可执行。partial 不触发；否定句、混合句、重复 JSON 键、非法句子编号、NUL、超长内容拒绝。没有把识别文字拼成 shell 命令。
- k7cloud device-once IP：一轮 3 秒真实采集，云端流式结果由板端解析；精确启动云台/测肤命令调用板端模式 API。必须事先启动已校准的 held voicecam 会话，不自动猜测 MCU 位置或重复打开相机。
- pipeline 增加实际 applied 状态查询；接受请求不等于成功。最多等待 3 秒确认，失败/播报失败即停止未来跟踪目标。停止云台是软件 hold，不代表电机断电或物理位置 ACK。
- 录音/TTS 共用音频所有权，拒绝并发占用。
- 当前是单轮入口，连续循环/停止竞争仍需完成；拍照仍有主机 JPEG 保存确认依赖，板端持久化、上传、真实聊天和报告流尚未接通，不能声称完整闭环。

## 验证与硬件状态

实际 C 解析与模式转换 O0/O2 通过，相关七个 ARM64 模块编译通过。device-intent-20260915 七文件 SDK 前版差异审查及 check/apply/check 通过，独立全量构建中；尚未加载。
此前运行的镜像仍以 tts-max 的实际加载记录为准，不把新候选当作已运行。
本轮只读查询发现 /dev/ttyUSB0 可见但打开返回 EIO，命令未发送；已请求用户断开并重连 QinHeng 串口，保持供电。云台开始前还需确认 v1.3-xcenter MCU 0/0 复位及周围无遮挡。

AI 日志必须导出并使用原版校验器；保留未完成和硬件失败事实。不得为了重建目标而把既有未完成目标标记完成。

全量构建完成：1816步退出0，2799680字节，SHA256 c3b862d2c62ced7504e1c7a4babfe317fb0c18aeee285c1c3b7f133939c5e137。证据 evidence/device-intent-20260915/verification.json，ram_loaded=false/hardware_tested=false。

12:29附近定时只读复查：串口已可打开，收到radio_generation=1/wifi=1/ipv4_ready=1/IP10.3.0.214回包，但窗口内没有完整NSH提示符；本机speech-service health=ok。未更新固件、未运动；云台复位和无遮挡确认仍待用户。证据heartbeat-radio-recheck.json。

12:34最新覆盖：device-intent固件已通过OTG单包19576896字节/1.99秒传输，全包及五段CRC通过并在RAM启动，BLE注册与Wi-Fi服务正常，尚待用户重新App配网。Windows语音桥已更新PID54580，echo-test关闭，speech-service PID44976保持正常。未启动相机或云台，复位确认仍待用户。证据startup.log及device-state.json。

最新阶段播报：用户重新配网后radio返回IP10.3.0.214。首轮测试未返回通过且旧脚本未保存异常stdout，不能称首轮通过；已修复失败证据保存和超时边界。复测K7S1P tracking_stopped通过，PCM51200字节，maximum=1，SOUND/tts/prompt_done均0，音频恢复0。用户实听待确认，未运动。

用户休息40分钟阶段：真实TTS已人工确认清楚，MCU复位已确认，但相机start后connected=0，未启动采集/跟踪。已完成RAM三图缓存、原生门控接线、连续轮次/取消、照片阶段播报候选及软件验证；详细构建/边界见docs/整机无人值守软件推进_20260915.md。期间未重启或运动。

最新主线为语音命令/缓存提示/三角度完成退出/上传，暂停自然聊天。已核对真实Swagger并添加四项通过的上传合同客户端；板端完成停止候选编译中，未上板或上传。细节和未完成项见docs/三角度姿态提示与上传接续_20260915.md。


最新覆盖：photo-upload-20260915构建/哈希核对通过，2803832字节，SHA256 0a501453e249b20a3eae270a2292f437267cd17e883d8001d760b42b6e12e08b。91项Python、C停止O0/O2通过。网关已实际切换PID7304 assessment-auth，26条提示预热2150400字节/一小时TTL成功，聊天关闭；旧dry-run不再运行。板子已软件进入Fastboot，旧WiFi/BLE在线状态失效，等待OTG，下一步tools/load_photo_upload.py --load。尚未上板、未真实照片上传或运动；用户摄像头/0/0/无遮挡确认仍待。实时阶段调度、人声端点估计继续未完成。


photo-upload固件已实际RAM上板：19581048字节OTG1.795秒，全包CRC896680e2、固件CRC c1a2094b及四无线段通过，实际SHA2560a501453e249b20a3eae270a2292f437267cd17e883d8001d760b42b6e12e08b。BLE/WiFi服务启动正常，等待App配网。USB Host启动并枚举32e6:9221相机，probe result0/committed1（camera-20260915T093418Z.log），未stream/未云台动作/未上传。网关7304整机模式26提示缓存保持，聊天关闭。真实云台0/0无遮挡确认仍待。
