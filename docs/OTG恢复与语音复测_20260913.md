# OTG 恢复与语音复测（2026-09-13）

17:03 板端软件复位并拦住自动启动后，Fastboot 报告 high-speed，Windows OTG 代理由 Code 43 恢复为 Code 0。随后 Ubuntu 同时识别 18d1:4d00 和 CH340 /dev/ttyUSB0。此前重启 Windows、仅拔插 OTG 未恢复；这些现象支持进一步检查板端 gadget 重入状态，但不证明已确定全部根因或永久修复。

尾段包 82,303,536 字节通过 OTG download，耗时 7.970 秒；源包 CRC 1696436b、尾段目标 CRC b5f7ae2d、decoder CRC 2af64aa7 均通过。新增 load_ram.py 的 stage-tail-reset 模式在搬运后软件复位，重新验证两个暂存模型后进入 Fastboot；本轮 Ubuntu 自动重新识别 OTG，无需再次人工连接。软件复位不能保证保留所有 RAM，必须每次重新校验，不得复用未经验证的数据。

最终包 115,987,824 字节耗时 11.703 秒，SHA256 12426051aa50132bcb2a8c4ed13cf9d1611aa01ca221c7eada09b1cce123a70d、CRC 2bbc24df。完整 encoder CRC b20f998f、decoder CRC 2af64aa7、固件 CRC a62378c4 均通过；固件 SHA256 f96ecdb89b13af582428494c0cc903e66061ede1e3e0ec419e7512a509fe46e5 已对应锁定包。RAM 启动到 NSH，见 evidence/wakepartial-boot-20260913-170801.json。

无线按 sdio-boot、bt-host、wifi-service-start 顺序启动，分别报告 boot_ret=0、bt-host ret=0、shared start ret=0；k7voice probe 返回 native_network=1。此诊断命令固定输出的 asr=0/tts=0 不是模型加载验收结果。当前尚未连接 Wi-Fi，BLE 客户端及完整语音配网未验收。

单轮实说测试使用 test_microphone_wake.py：提示音、capture-pga24-grouped 48000、flow-mic-wake。该入口只检验真实录音到唤醒和扫描，不提交密码。录音得到 48,000 帧，返回 result=0，ASR 开始；结果另行追加。

后续云端播报按用户指定采用 mimo-v2.5-tts；先完成规则语音配网，再验证 DNS/HTTPS，接口参数、鉴权和音频格式仍需核对官方文档，尚未调用云端 API。

边界：本轮发送的是 RAM download/cp.b/crc32/booti 和软件 reset，无 flash、erase、saveenv、eMMC 写入、STM32 写入、云台或远程提交。恢复措施仅有限真机通过，U-Boot 清理候选尚未编译上板，不能宣称稳定修复。
