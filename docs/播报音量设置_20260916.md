# 当前用户音量设置

用户最新要求：短音保持最大24 dB；TTS语音播报降低到10 dB，覆盖之前16 dB设置。

已在当前运行固件执行 `k7sound speech-volume 10`，板端返回 `SOUND speech_gain_db=10 cue_gain_db=24 next_playback=1`。下一次语音播放生效；没有重启、重新配网或重新合成TTS。

这是运行时设置，当前固件编译默认仍为16 dB。后续每次RAM更新或重启后，在播放前重新执行上述命令。可用 tools/set_speech_volume_10.py 应用并核对回执；不得仅凭本文声称新启动已应用。

证据：evidence/stage-watch-20260916/speech-volume-10.log。

此前用户确认真实云台跟随及启动播报均成功。后续测肤轮未说话，空ASR结果不作为识别故障。该轮已停止，日志记录 device stop_requested=1、loop_done=-125、VOICE MODE applied=0，控制发送计数保持74。尚未完成三角度拍摄与上传验收。

后续real-photo-volume10.log：运行12轮，均intent0，结束loop_done0且applied0。用户确认已说开始测肤；不能归因为用户未说话。云端每轮150帧/completed但没有文字。固定合成语音通过同网关得到开始侧敷，说明服务可以识别，但词汇误识别存在；真人空结果原因仍待录音回放定位。未进入拍摄、未上传。

录音回放排查：用户听到短音但未听到6 dB诊断回放；已将同一录音用回放命令12 dB重放（独立于TTS10 dB），板端result0，等待实听反馈。mic-replay-diagnostic.log的PCM导出受并发串口输出影响，仅完整解析53856/96000个字，不能当作完整录音送云验收；不能据此断言麦克风损坏。未启动云台、未拍摄。
