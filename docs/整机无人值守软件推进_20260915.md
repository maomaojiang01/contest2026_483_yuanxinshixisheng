# 用户休息期间的软件推进

用户于约12:53要求休息40分钟，期间继续软件任务，不安排真人录音、云台动作或重启板子。

## 板端实际状态

正在RAM运行的仍为 device-intent-20260915，SHA256 `c3b862d2c62ced7504e1c7a4babfe317fb0c18aeee285c1c3b7f133939c5e137`。
用户App配网成功，最后查询Wi-Fi在线、IP10.3.0.214。
K7S1P阶段TTS已真实播放51200字节，用户确认清楚；Windows桥PID54580，echo-test关闭。
用户确认MCU已复位0/0、无遮挡；随后相机准备仅k7host start成功，camera connected=0/committed=0，未进入probe/voicecam。用户休息期间不运动。

## 新增代码与验证

1. `app/k7host/k7_photo_store.*`：单轮三图RAM保留，最大三张各1MiB，严格front/left/right顺序、同epoch、递增序号，拒绝不一致重复与过期写入。复制返回调用者拥有的数据，避免上传引用被重拍释放。500轮并发重拍/复制测试O0/O2通过。缓存不是eMMC持久化，断电丢失。
2. `k7_photo.c/k7_pipeline.c`：voicecam使用原生缓存；真实门控选中的JPEG先复制，再确认vf_saved；该模式忽略外部host ACK。旧photos调试模式保留原host保存协议。真实C门控+模拟解码/姿态的端到端存储接线O0/O2通过；这些是合成夹具，不是真机照片。
3. `board_speech_bridge.c`：增加device-loop IP ROUNDS（1..300）和device-stop。连续执行3秒采集轮次，停止使generation失效、立即请求软件hold并取消在途TTS；迟到final与TTS不能撤销停止。单个采集仍等待结束/网络超时后回收资源，不代表全双工或毫秒级语音打断。停止保护实际C O0/O2通过。
4. `device_photo_prompts.*`：按原生RAM完成位产生拍照完成/下一个角度提示，拒绝旧epoch、回退和非法顺序，不重复播报。三图完成只说暂存在设备内存，不宣称上传。状态在本批成功播放后提交。计划器O0/O2和ARM64模块编译通过。
5. 固定词表26条；现有Python59项回归通过。新核心和接线九个ARM64模块临时编译通过。

## 独立构建

- native-photo-20260915：2799728字节，SHA256 `96069cb7568ee4a24735473193f412fce2a2c0c4694af84b8cc2c0011c90959a`，全量构建通过，未上板。
- device-loop-20260915：2799728字节，SHA256 `382a926f007168fbfc582809a2f1f29e059d10957895926a426a286e1e0037e0`，全量构建通过，未上板。
- native-photo-prompts-20260915：精确九文件SDK保护同步、全量构建和13项源哈希核对通过；2799728字节，SHA256 `8cbd147ad72a3b463e734ab42320d91d80219d2b978a6bdff6b83671011965d1`，未上板。

## 未完成事项

相机枚举/采集和真人云台命令未验收；连续流程需要用户回来后测试。
阶段提示目前在识别轮次之间处理，可能落后于相机事件，不是实时全双工提示。
RAM照片还没有原生后端上传、设备认证、报告流和自由聊天接线；不等于完整整机闭环。
后端18085最新OpenAPI仍无聊天/报告讲解流；服务器语音两个systemd服务仍inactive、未发现后端配置文件。不能擅自补密钥。

保持现有固件和服务，用户回来后先核对摄像头连接再做联合测试。最新候选的构建、源哈希与软件回归已有独立证据。

最终阶段播报候选全量构建已通过：2799728字节，SHA256 8cbd147ad72a3b463e734ab42320d91d80219d2b978a6bdff6b83671011965d1，未上板。六组实际C O0/O2综合回归原始日志在evidence/native-photo-prompts-20260915/tests-20260915T051330Z/。新增assessment_multipart编码器按真实A01合同生成四part，独立MIME解析和分块读取O0/O2通过；尚未编入固件或接网络/认证，不代表上传成功。
只读复查camera仍connected=0。VM剩余9.3GiB，经E盘校验归档两个中间构建的ELF/BIN/map/config等后删除对应可再生成目录，恢复约12GiB；最新候选与上板版保留。归档见evidence/native-intermediate-archives-20260915/。

阶段提示缓存候选：host/streaming_asr/board_bridge.py 对显式 K7S1P 请求提供进程内PCM缓存，最多16条/2MiB、5分钟绝对过期，按服务地址及原文区分；普通K7S1T和ASR不缓存。缓存只保存合成成功的偶数字节PCM，LRU淘汰，重启清空；更换云音色/采样配置后应重启桥以立即清空。新增过期、预算、内容复制、协议隔离、失败重试测试；没有向运行中的PID54580热更新，也没有宣称真机时延改善。

最终Python综合回归64项通过。ELF符号审计确认device_loop/device_stop/photo_status/photo_prompt_plan已链接；首次检查也要求photo_native_copy存在而失败，核实该上传复制API尚无调用方后改为单独记录upload_copy_linked=false，未隐藏上传缺口。证据symbol-audit.json；接实际上传时必须将调用方编入再复验。AI日志原版校验本次已通过17文件/37918事件，后续自动刷新数量以manifest为准。

接续顺序：先只读检查相机及供电/连接，用户回来后再安排运动；保留当前联网版，候选RAM加载后重新App配网。联合验证启动/停止、三面角度门控、每阶段播报和RAM三图完整性。上传接线须取得真实设备会话与consentEvidenceRef，用同一epoch复制三张JPEG到上传拥有的内存，在请求结束前不得释放；202只表示受理，应继续查询任务，不能提前播报分析完成。报告讲解流和自由聊天以后台实际提供的接口为准。最新日志刷新为17文件/37934事件ALL OK；缓存回归证据见prompt-cache-tests.json。

用户回来后首轮检查：Windows speech-service /health=ok；Ubuntu /dev/ttyUSB0打开即I/O错误，camera查询未发送。已请用户只重连QinHeng串口，不重启板子。新增tools/load_native_photo_prompts.py，默认仅本地候选SHA/大小与加载脚本语法核对，通过；必须显式--load才访问硬件。目前仍未加载新候选。

后续最新覆盖：用户重连串口及OTG后，native-photo-prompts 8cbd147a候选已RAM启动。OTG19576944字节/2.000秒，整包及5段CRC通过，BLE和Wi-Fi服务恢复。旧运行哈希不再代表当前板子。用户报告已连，但查询确认BLE connected=1，最近PROV command=2，Wi-Fi仍0/IP0.0.0.0，已请求实际选择网络并提交密码。相机Host/Hub初始化成功，拔插后descriptor_bytes=613但connected=0；源码create_camera的g_created每启动一次限制会拒绝后续枚举，尚未修改该生命周期逻辑，不应反复要求拔插。未启动采集/云台。

后端新契约：/api/v1/gimbal-ai/messages已出现，严格请求仅text，云台Bearer设备session，SSE response.accepted/delta/completed/failed；无上下文、无报告追问。已归档gimbal-ai-contract.json，新增host/whole_device/gimbal_ai.py与4项契约测试通过，明确仅主机参考，不是板端接入。无真实token，不发送虚构身份请求，不将测试替身作为实测。后续认证与板端HTTP/SSE/TTS接线仍需完成。

相机修复后续：camera-reconnect-20260915全量1818步构建通过，2803824字节，SHA256 9e28d2edb4118734814e86247248a6e194d879f732c29572602f5558b4002b95；单文件SDK前版哈希审核同步，8项源哈希核对。回调释放实例并允许采集前重连，控制锁被占用时不接纳新实例；保留采集后一次尝试限制。当前已reboot进入Fastboot等待用户接OTG，Wi-Fi此时已离线，不能沿用之前10.3.0.214在线快照。待运行tools/load_camera_reconnect.py --load。Windows桥已在无活动连接时按原参数重启为PID61068，缓存生效，stderr空。用户提供credential/version，已写git忽略的private/gimbal-device-auth.json并加入AI日志脱敏；尚缺proof规则，未尝试认证。

最新上板覆盖：camera-reconnect 9e28d2ed已OTG19581040字节/1.933秒传入，全包/各段CRC及RAM启动通过。Host识别UVC 32e6:9221，connected=1/config613，probe result0/committed1。启动voicecam1800秒，ready mode0，已观察TRACK q320 x0/y0/tx0；没有启动跟踪运动。只证明本次启动枚举/协商/保持会话成功，尚未做真机重复拔插寿命测试。当前Wi-Fi0/IP0.0.0.0，已请求本轮App重新配网。摄像头会话最多30分钟，续跑必须先检查实际会话状态，不重复打开。证据在camera-reconnect目录。


2026-09-15 最新实测覆盖：vision-ble-coexist / 30f6a77c 已 RAM 加载，完整 CRC 与启动通过；实际 decode 线程 CPU2、FIFO 优先级80。用户 App 已配网成功，WiFi 1/IP10.3.0.214，40.23秒 BLE subscribed1/busy0/errors0 与视觉帧递增并存，无云台运动。仅有限共存验证，非长期稳定验收。证据 evidence/vision-ble-coexist-20260915/device-state.json。

用户提供 proof 后真实设备登录和 gimbal-ai/messages SSE 中文回答已通过；私密认证仅在 private/，不写交接明文。新增 host/streaming_asr/conversation.py 与板端 chat-loop 候选：openvela 控制提示音、3秒录音、回答播放和下一轮；网关只调用后端与云语音，不驱动串口/运动。独立 B/C 协议避免污染普通 ASR、固定提示或回声测试。空语音不提问，问题60秒单次消费，90秒总超时和等待保活，取消不回退复述。74项 Python、实际 C 停止/响应 O0/O2通过；云回答→TTS实测5047ms、199680字节PCM，板端尚未播放。独立 native-chat 构建进行中。当前为无状态轮次、固定3秒录音、整段PCM，尚非上下文记忆/VAD/边合成边播放，也尚未真人连续聊天验收。


2026-09-15 15:07 覆盖：native-chat 已全量编译/链接通过，2803824字节，SHA256 f32aadc337547b39f78cbbc845f4ce671ab29ba450e1774bc16ce49bdc8a94b2，九项源哈希核对及新增协议字符串检查通过。最初保护同步因未登记新基线而拒绝；审查两文件 diff 后将精确前版哈希登记，check/apply/check通过，未覆盖未知改动。新网关 PID56072/8001 已启用 conversation-auth，普通语音服务44976/8000健康，空聊天请求被拒绝。板端 RAM 更新尝试在串口open时报EIO，重启命令尚未发送，旧30f6固件保留；已请用户只重连QinHeng USB Serial，待恢复后 enter_device_intent_download.py → OTG接入 → load_native_chat.py --load → App重新配网 → 用户准备后 chat-loop真机测试。不得宣称已加载/已自由对话。AI日志官方校验17文件38560事件通过；覆盖范围/脱敏/部分采集声明保持。
