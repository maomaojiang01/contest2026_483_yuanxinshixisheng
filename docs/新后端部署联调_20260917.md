# 新后端部署联调（2026-09-17）

用户指定根地址：https://eveaisia.com/openvela。已下载该地址 /v3/api-docs，HTTP200，servers与根地址一致；设备会话、三图提交、任务查询、brief报告、讲解流路径存在。

已更新 host/whole_device/assessment-upload.dev.json 的 baseUrl 和 private/gimbal-device-auth.json 的 base_url。凭据未改动、未输出或写入公开证据。上传与认证/报告必须保持同一环境，不能只改其中一个。

实际请求：POST /api/v1/gimbal-sessions 使用既有设备凭据，HTTP200，返回gimbalId/sessionToken/expiresAt/serverTime。紧接着使用新sessionToken（Authorization: Bearer）请求 GET /api/v1/gimbals/{gimbalId}/current-assessment 和 GET /api/v1/skin-assessment-tasks/{原任务ID}，两者均HTTP401，error.code=AUTH_REQUIRED，message=authenticated principal required。最终URL仍为所请求HTTPS地址。旧任务不一定迁移，因此不将其存在性视为前提；current-assessment无需旧taskId也同样401。

请后端检查新部署反向代理Authorization透传、/openvela路径下的设备会话鉴权过滤器及设备token校验配置。尚不能确定具体根因。未上传新照片，未触发运动/录音/刷写，不宣称新环境整机通过。检查时Windows8000/8001未发现监听，故修改磁盘配置不代表网关正在运行。

证据：evidence/backend-redeploy-openapi-20260917.json、evidence/backend-redeploy-check-20260917.json。保留原长测失败；更换后端后的结果需独立记录。

2026-09-18 09:13复测：新后端设备登录HTTP200，使用新token查询current-assessment HTTP200、currentAssessment=null，昨天该接口401问题本次已恢复。旧环境taskId只读探测返回404 RESOURCE_NOT_VISIBLE，不能宣称旧任务迁移或新报告已验收。尚无新环境当前任务，需下一轮实际拍摄提交后验证任务→brief/播报。未重传旧照片，未启动录音或云台。证据evidence/backend-redeploy-recheck-20260918.json。
