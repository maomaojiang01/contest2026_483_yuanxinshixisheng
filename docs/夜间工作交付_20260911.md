# 夜间工作交付：截至 2026-09-11 09:08

**收尾后的新授权：**用户随后明确要求“现在接着推进”。因此恢复当前交互任务的音频与无线开发；旧夜间定时任务保持暂停，不把新的开发算作昨晚连续运行。下一独立版本 `audio-mmu-20260911` 尚未建立；当前已准备的工具使用 `audio-mmu-20260910` 作为版本标识，实际执行日期应以证据时间戳为准。

本次是限时无人值守开发的收尾，不是音频或语音配网最终验收。最后有据可查的真机实验为9月10日21:52:39；随后子代理报告使用额度耗尽。9月11日09:06恢复处理时已过截止，未再启动新实验；不能声称整夜连续运行成功。定时接续任务 `velavision-9-11-09` 已暂停。

## 实际完成

- MA4及MA4+80Hz高通回放组件已接入，保留原始PCM，逐声道独立处理。主机测试及独立ARM64/ELF检查通过；MA4板端200毫秒回放路径通过。无人实听，不能当成音质通过；主机ASR输出仍有明显偏差。
- 内部静音回环复现每四帧三帧零；128帧停止及恢复通过。改变RX各路径至SDI0后，原始分布没有改善。
- 编号回环已RAM上板。128帧共256字，194字零、62个不同有效发送序号、0个非法非零字，TX跟踪与编码一致。有效数据随周期更新，排除了这段观察中持续重复同一旧marker的情况；还未定位零样本根因，不得删零宣称修好。
- `audio-route-20260910` 镜像上，私密串口提交完成真实WPA2/DHCP，IP 10.3.0.214，空ARP起步网关5/5，A-MSDU两子帧接收、拒绝0。Windows BLE配对失败；软件入队不等于ACL已发送，已形成独立只读分析，诊断计数候选尚未完成。
- 共享BLE/语音扫描与联网后端候选已形成，修正编译警告、join错误路径数据竞争和终态发布竞态，补齐当前CMake入口。主机O0/O2验证通过。整合暂存于 `private/radio-reviewed-stage-20260910`，正式无线源码、目标SDK及板端尚未集成该后端。
- MMU只读探针候选、编号反解器及串口换行兼容测试已交付。MMU探针未上板。

## 当前设备与证据

最后加载镜像 `audio-marker-20260910`，BIN SHA256 `06fd9f72fe31ad2b52a2bb27d64e37bb75bb9be1d6627c490a85e6f090218cb7`。ELF SHA256 `7ef24933ae9d5a58769b18bb1c85b685191df78ef6c2749d8c718c2b2ad40ac6`。RAM加载和八核任务亲和性快照通过，音频实验结束时功放关闭。

09:08只读状态确认NSH有响应、Wi-Fi未连接、配网服务未注册。未做晨间录放音或联网测试，未重新测量功放引脚。该镜像无线未启动，不能沿用前一镜像Wi-Fi在线结论。进程核对未发现本任务的加载、音频观察、构建或BLE检查进程仍运行。未刷eMMC/STM32、未启动云台、未提交或推送。

- `evidence/audio-loopback-20260910/runtime-results.json`
- `evidence/audio-route-20260910/runtime-results.json`、`wireless-recovery.json`
- `evidence/audio-marker-20260910/numbered-analysis.json`、`morning-status-20260911.json`
- `evidence/build/audio-marker-20260910/`：源码、镜像及ELF核对
- `logs/maomaojiang01/`：显式项目会话日志；原版官方格式校验结果见 `evidence/official-log-validation.json`。子代理候选交付不代表其独立会话完整日志全部入库。

## 下一步尚待完成

1. 检查SAI实际内存属性、FIFO/有效通道配置，定位周期性零值；获取正确连续采样后再调整降噪和人声增益。
2. 补齐BLE入队、实际发送/ACK及端口5接收计数，先恢复可靠GATT通信，不放宽安全配对。
3. 独立构建共享语音配网后端，验证真实扫描、凭据提交、认证/DHCP事件、取消与重连。
4. 将实际ASR/TTS和音频接口接入板端，完成唤醒→扫描→选择→密码→真实联网。当前不能用主机模拟或文本流程代替板端语音验收。

恢复开发应从这些未完成项继续，不重复已完成的原始硬件基线测试。
