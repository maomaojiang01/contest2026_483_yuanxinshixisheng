# Wi-Fi 聚合帧接收修复记录

**真机修复验证通过：首次网关 Ping 40/40，0% 丢包。** `wifi-amsdu-index-20260909` 已从 RAM 运行，Lansee 的 WPA2、DHCP 成功，IP `10.3.0.214`。状态记录实际交付 1 组 A-MSDU、2 个子帧；2 次非聚合数据被严格 PN 检查拒绝，没有造成这轮 Ping 丢失。证据为 `evidence/wifi-amsdu-index-20260909/lansee-01.jsonl`、`ping-first-40.bin`、`wifi-status.bin`、`device-state.json`。

本记录对应统一源码 `app/k7radio/skw_wifi_data.h`、`skw_wifi_amsdu.h`、`wifi_ip_service.inc`，保留失败证据，不将构建成功写成真机通过。

## 手机与 Wi-Fi 同时工作的实测

手机重新配对后建立加密连接。随后在原生 `wlan0` 上连续 Ping 网关 50 次，**50 发 50 收，0% 丢包**，耗时 50.050 秒。前后蓝牙均为 handle 17、encrypted=1、连接数 2/断开数 1，测试中没有新增断开；其中已有的一次断开是 RAM 重启后手机尝试恢复旧配对，后续重新配对成功。证据 `ble-status.bin`、`phone-coexist-50.bin`、`ble-after-coexist.bin`、`wifi-after-coexist.bin`。

这是手机 BLE 连接保持与 Wi-Fi 网关流量的短时并发测试；本轮未发送 BLE 业务数据压测，也没有小时级保持、iOS 验收或互联网证明。当前固件和连接保留运行，COM8 没有持续观察进程。

装载工具复用了 298 个经过前后 CRC 验证的 RAM 数据块，总装载阶段约 228.2 秒，完整镜像、模组固件及 DTB 校验后才启动；记录见 `ramload-summary.json`。

## 定位过程

1. `wifi-ip-rx-20260909` 已完成 WPA2、DHCP，早期网关 Ping 为 30/30。随后手机保持加密连接的测试为 40 发 30 收；前 10 次因地址解析未完成而发送失败。原始记录是 `evidence/wifi-ip-rx-20260909/phone-coexist-ping.bin`。前后 BLE 状态均为同一连接、相同断开计数，证明这段测试中蓝牙保持连接，但不能据此宣称网络稳定。
2. 首个 A-MSDU 实现 `wifi-amsdu-20260909` 软件测试和原生构建通过，真机仍为 40/30，拒收标志包括 `0x3e/0x5e`。证据 `evidence/wifi-amsdu-20260909/ping-first-40.bin`。
3. 为保留手机连接，通过 NSH `xd` 只读采样已验证镜像的接收描述符。只输出频道 7 的长度、标志、序号和子帧编号，不保存原始内存、载荷、PN 或密钥。第三轮采样记录首标志 `0x3e`、序号 7、子帧编号 **1**；见 `rx-descriptor-samples-03.jsonl`。异步采样可能与接收竞争，因此它是定位线索，修复必须另做真机验收。
4. 编号修复 `wifi-amsdu-index-20260909` 按明确的 first 标志建立起点，使用相对编号收齐整组。不得用编号 0 的假设拒绝合法首帧。最多缓存 8 个子帧，缺帧、重复、混合序号/PN、超时、长度越界仍拒绝；收齐后才提交 PN 并交付 IPv4/ARP。

## 软件验证与构建

- `tests/wifi/test_amsdu.c`：零起点和实际一开始编号、完整后交付、顺序交付、重复首标志拒绝、乱序子帧、缺帧、混合 PN/序号、超时、八子帧上限及不支持协议过滤。
- `tests/wifi/test_wifi_data.c`：描述符边界、目的地址、单播/组播每 TID 重放保护、RSC 溢出及 TX 布局。
- 两者在虚拟机中以 AddressSanitizer/UndefinedBehaviorSanitizer 运行；原版 WPA 状态机测试也运行。
- 逐文件源码哈希、构建退出码与固件摘要在 `evidence/build/wifi-amsdu-index-20260909/verification.json`。
- 原生固件 SHA-256：`85fb84d34a7dbc74469b91c501b3e289791a1b99636829690d3cc6210e688dfe`。
- `wifi-amsdu-diag-20260909` 只是编译过的中间诊断镜像，未上板；实际使用只读采样后直接测试编号修复。

## 仍须区分的边界

原生控制台真实联网与前端正式配网接口是两个验收项。当前 BLE 能力仍为 `receive_credentials:true/connect:false`；`submit_credentials` 只校验并回复收件确认，不能显示成已联网。正式 connect 事件、任意 SSID、安全模式兼容、长期运行、完整重密钥、DNS 和互联网可达性仍需逐项完成。

修改未刷写 eMMC/STM32，未启动云台，未提交或推送远程。AI 日志归属同仓 `logs/maomaojiang01/`。
