# Ubuntu 历史构建中间文件清理

用户授权清理不影响最终项目目标的无用文件。实测根分区清理前可用1379409920字节，主要占用为历史`cmake_out`约50GiB，SDK预编译工具约14GiB、Git元数据约16GiB。

本轮仅删除`/home/swl/openvela/cmake_out`内73个闲置K7历史构建的90700个`.o/.obj`文件：限定实际`CMakeFiles`路径，必须出现在对应`build.ninja`规则中，是无符号链接的单硬链接普通文件。删除前核验清单SHA256、inode、大小、修改时间及无活动进程使用该树。没有整目录删除。

保留固件、静态库、配置、编译规则、编译命令、日志、SDK源码及仓库元数据、模型、Windows正式项目和当前`/dev/shm`构建。893项受保护文件删除前后SHA256一致。若今后重新构建这些历史目录，缺少的对象会重新编译，不能称其增量构建缓存完整保留。

可用空间增至22739542016字节，约21.2GiB。删除文件逻辑大小共21186611464字节，约19.7GiB；实际释放量还包括文件系统分配差异。

执行工具：`tools/cleanup_vm_old_objects.py`。完整清单、计划hash、逐文件删除记录和验证结果：`evidence/vm-space-cleanup-20260911/`。本清理不代表ASR/TTS验收，没有操作开发板、刷写或远程提交。
