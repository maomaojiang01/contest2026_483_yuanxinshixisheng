# 原生语音运行库编译推进

目标仍为纯板端 ASR/TTS 与 VoiceLink 配网。以下是运行库移植的编译证据，不是语音识别或联网验收。

- ONNX Runtime 固定提交 `8f5c79cb63f09ef1302e85081093a3fe4da1bc7d`；sherpa-onnx 固定提交 `26aa2fa93210376a89de3a65a1a4dd320c37f5e9`。源码在 `work-in-progress/native-voice-sources`，逐文件提取清单及 16 份独立官方源交叉核对已保存。sherpa 归档中的符号链接未创建，遗漏清单保留。
- Eigen 原 ZIP 与上游锁定 SHA1 不符，拒用记录保留。另通过官方 Git 固定提交和根树、1784 个 blob 核验获得源码，采用独立 Git 来源路径，不修改原 ZIP 锁。
- `Env` v2 已使用本项目真实 NuttX ARM64 编译器、生成头、libc++ 和配置编译成功。对象 SHA256 `bd96c2a71d5b291064ed95bc0e005f0ffb2bde5d684376c1b7c255b05baa8b2e`，ELF64/AArch64/REL。证据为 `evidence/native-ort-env-compile3-20260911`。生成的 ORT 配置头仅保守取消未探测警告特性，不代表上游完整 CMake 配置通过。
- common/platform 的21个源文件已全部真实编译通过：第一轮原始成败保留在 `evidence/native-ort-common-compile1-20260911`；第二轮只重编3个失败单元，NuttX logging 改用 gettid/getpid，补齐 date 和 profiler 头路径，均通过。`evidence/native-ort-common-compile2-20260911/combined-source-gate.json` 汇总21个对象及哈希，逐个确认为 ELF64/AArch64/REL，未声称链接通过。

尚未完成 common 链接、nsync/Abseil 静态链接闭包、ORT Session/微型图、真实 ASR 模型加载和 TTS。单个对象编译通过不能证明未定义符号可解析、任务终止安全、内存充足或推理正确。Env 候选的 join 失败 terminate 策略仍须评审 NuttX 任务组作用范围，不能把 fail-stop 写成安全回收。

正式固件中的 `k7voice probe|scan` 目前仅验证真实共享无线接口。麦克风物理采样率、声道布局与连续性仍需验收；保留原始零样本，不能删零或改采样率标签冒充合格 ASR 输入。

## 静态依赖的实际阻塞

`native-static-deps2-20260911` 使用独立 CMake 3.28.3 配置成功，在 Abseil `time_zone_format.cc` 编译处失败：`strptime` 未声明。实际 SDK 的声明和 `lib_strptime.c` 均受 `CONFIG_ALLOW_MIT_COMPONENTS` 控制，当前正式配置关闭此项。原文和哈希冻结于 `evidence/native-strptime-input-20260911`。这不是已经链接成功后的运行问题。

下一轮 `tools/build_native_voice_static_deps3.py` 仅在隔离编译环境中打开声明，并同时编译 SDK 的真实 MIT 许可 `lib_strptime.c`，不用空实现；正式 SDK 配置和当前 RAM 镜像保持原状态。此轮仍是编译验证，最终必须在一致配置下重建 libc、核对新增源范围并完成 ELF 链接与运行。准备阶段因远端目录仍指向上一轮而被不可覆盖检查拒绝，修正独立目录名称后才开始构建，旧结果未覆盖。

第三轮实际结果：configure=0、build=1；`time_zone_format.cc` 已编译通过，后续 `elf_mem_image.cc` / `vdso_support.cc` 因 NuttX 缺少 Linux 动态链接头 `link.h` 失败。完整原始日志在 `evidence/native-static-deps3-20260911`。不能由前一个错误消失推断整个依赖库成功；下一步审查 Abseil 的 ELF/VDSO 平台选择与非 Linux 回退，不引入 Linux ABI 兼容层。

## 原生依赖闭包与完整会话配置

`native-static-deps4-20260911` 已完成全部138步，生成30个ARM64静态库；独立Abseil副本只修正NuttX的ELF-memory/VDSO功能选择，沿用上游不支持路径。真实MIT `strptime` 源也编译归档，但正式固件的相应配置尚未变更。

链接审查分三轮保留：第一轮强制whole-archive暴露23个缺口；第二轮补上真实POSIX EnvTime编译与当前配置已有的工具链libm，剩13个Abseil线程/allocator符号；第三轮使用正常archive group抽取，21个common对象与30个库可完成可重定位链接，172个外部强符号均可在实际SDK/libm/libgcc库中找到。证据 `evidence/native-voice-link-audit3-20260911`，合并对象SHA256 `4cf188d8dc25296c4cc28310d8893d7549e865073a20252f4df7a061644e0fa4`。这不是最终固件链接通过。未为无关的强制纳入对象修改NuttX匿名映射或TLS析构；审查发现其页对齐和析构语义不能直接满足Abseil假设。

完整上游ORT会话源码及13份依赖归档已在独立tmpfs准备，4232项输入核对，官方host protoc为3.21.12；此工具只生成代码，不能作为板端运行库。NuttX CMake平台差异、FlatBuffers/Protobuf/ONNX上游补丁及已审查Env/logging候选均只应用到该独立源码。Eigen继续使用已验证固定Git树。

`native-ort-session-config91-20260911`：真实Iconv源码编译/归档、Add-only算子注册生成、完整CPU Session CMake配置均为0。Iconv来自SDK `lib_iconv.c`，没有伪目标或空实现；正式locale配置尚未开启，后续须正式集成。NuttX静态CPU范围排除动态provider共享库，不冒充支持动态加载。先前各轮补丁换行、生成脚本模块/输入缺失、构建工具环境及Iconv发现失败分别保留原始结果。

当前 `tools/build_native_ort_session.py` 正在构建749步范围的真实Session、MLAS、框架、计算提供者和模型解析静态目标。ASR/TTS实际模型尚未加载运行；Add-only注册不能承载完整语音模型。编译器支持FP16/BF16不证明A53/A72具备对应指令，运行分派仍需独立核对。

## 后续编译与模型内存核对

上述首轮构建最终失败，保留原始日志。第一项是上游代码的shadow/undef警告被当前平台Werror提升；仅在隔离ORT目标中保留警告而取消这两项的error级别。第二轮出现MLAS扩展指令汇编失败：全局`-mcpu=cortex-a53`与专用源文件的`-march`选择冲突。第三轮将C/C++/ASM的调优参数改为`-mtune=cortex-a53`，仍保留全局`-march=armv8-a`，专用扩展源的架构参数由上游保留，并显式关闭CPUINFO。没有把编译器支持伪装成硬件支持。变更前后哈希与配置证据见`evidence/native-ort-session-warnings-20260911`及`evidence/native-ort-cpu-flags-20260911`。

第三轮`tools/build_native_ort_session3.py`使用4个编译任务，独立日志为`evidence/native-ort-session-build31-20260911`（目录末尾31为实际名称），当前仍在编译。其743步范围包含真实Session和CPU提供者，但算子注册仍仅Add。`tools/audit_native_session_link.py`已准备好，必须等待该轮成功才会读取产物，检查真实C API调用所引入的按需链接闭包；本记录时尚未执行。

最小Add模型109字节，SHA256 `f0ab8a49f72351be2010f51c35e10bd69d82c1dd0e8e8d95fd0db4cd42a4453f`。真实C API探针已通过当前NuttX ARM64编译器的`-Werror`编译（`evidence/native-add-probe-compile-20260911`），尚未链接运行。主机ONNX工具只核对模型结构；没有将主机推理结果当作板端结果。

真实ASR编码器165462184字节，超过约126MiB普通系统堆。仅注册专用张量allocator不能消除ONNX protobuf解析时的大权重复制。后续优先验证固定ORT格式模型、池内持久模型字节、`session.use_ort_model_bytes_directly=1`与`session.use_ort_model_bytes_for_initializers=1`，并配合专用张量allocator；图元数据、预打包和临时分配仍需实测峰值。未扩展普通堆、未全局替换malloc，也未改变DMA范围。

独立模型清单已核对实际编码器、解码器与VITS图，生成语音算子注册输入；Add构建不能冒充覆盖这些算子。sherpa仍需要真实kaldi-native-fbank和文本处理依赖，TTS的`rule.far`不因ONNX图没有StringNormalizer而变成可省略。模型文件读取、音频16k连续性、ASR/TTS真机运行仍待验收。

后续实际结果：第三轮1133.73秒后因`string_normalizer.cc`调用当前mini-localization不提供的`std::locale(name)`失败，未超时。原始日志保存在`native-ort-session-build31-20260911`。真实生成表中其注册已被上游工具注释，仅剩一个前向类声明；首个保守检查因此拒绝，保留失败。修订检查仅允许这一条精确的未用声明，其他任何活跃引用仍拒绝。`native-ort-reduced-locale2-20260911`配置通过且实际compile_commands不再含该源，第四轮正在剩余194步构建；没有伪造locale实现。TTS的FST文本处理不在此次裁剪范围。

固定ORT1.17.1主机工具在项目独立Python目录通过逐包SHA256锁定安装，原始模型转换为单独ORT候选成功，输入哈希前后相同（`native-speech-ort-conversion1-20260911`）。同时用该版本官方工具从转换结果生成实际ASR/语音算子清单，未开启类型裁剪。Windows Python双direct加载首次未返回，精确终止后受控复测得到访问违规；path、copy和direct-only均成功，结果见`native-ort-bytes-modes1-20260911`。

固定源码审查发现Python绑定把模型字节传入按值`std::string`，Load后该临时副本被销毁，后续另一步Initialize可能访问借用字节。独立真实C API实验使用官方1.17.1 C运行库、持久64字节对齐缓冲，direct-only及双direct均加载编码器成功、退出0。此结果支持生命周期风险解释，但C发行库与Python扩展不是同一个二进制，不能声称唯一崩溃根因。证据位于audio-noise独立工作树的`work-in-progress/native-ort-persistent-capi-v1`；未Run，不算数值一致性或板端内存验收。板端候选将直接用C API并保证缓冲寿命。

最小Sherpa ASR链实际锁定fbank1.22.1、kaldi-decoder0.2.10→kaldifst1.7.16→OpenFST固定tag及KissFFT等，共8项依赖逐归档校验通过。其Eigen3.4.0原锁可精确取得，采用独立Sherpa build/cache，避免覆盖ORT Eigen。`native-sherpa-asr-stage-20260911`已核对3757项输入并应用仅NuttX的真实ORT库导入候选，尚未配置/编译。接口不传播Eigen类型，但最终静态模板符号/ODR仍需审查。

本轮官方日志刷新：13文件、20592事件、6个显式会话，原版校验器ALL OK；只对应当次采集范围，后续仍须刷新。当前板端仍voice-owner，没有加载新推理固件。

2026-09-11 原生运行库增量：ORT 1.17.1完整CPU Session静态库编译通过；真实nsync及NuttX localeconv补齐后按需链接缺失强符号0。voice-ort-add-20260911正式独立固件编译成功，BIN SHA256 522047a5f8d6197ea0285227e581f3bce06e04ed23fb101af3dadc61b4f6fdd2，10,514,736字节；运行末址0x4129a000，异常表17477 FDE及首项注册静态检查通过。该版本仅显式Add诊断，尚未运行，不代表ASR/TTS。主会话已安全重启至U-Boot并开始CRC门禁RAM传输，没有刷写。
ASR配置2采用kaldifst1.7.17/OpenFST2024-06-19和Eigen实际C++11探测，通过；真实库编译开始。第一次传输因密钥权限失败、第二次归档路径未规范化被哈希门禁拒绝，均未伪造成功，修正后配置通过。
空间清理：仅删除73个历史构建目录中90,700个可再生成对象文件；893个固件/归档/配置等保护文件哈希全部不变，Ubuntu根分区可用从约1.3GiB恢复约21.2GiB。证据evidence/vm-space-cleanup-20260911；源码、SDK工具链、模型、原始日志未删。以上AI日志需后续真实导出并运行官方校验器。

2026-09-11 ASR实际编译推进：Sherpa1.12.14 C API第二轮198步全部成功，耗时248.017秒；KissFFT仅修复两个宏的命名冲突，普通日志语义六项预处理结果保留。未禁用原编译检查。真实Recognizer入口按需链接成功，396个外部强符号中SDK仅缺popen/pclose，暂未最终固件链接。该符号审计仍使用Add-only ORT，不代表ASR模型可用。28项ASR算子注册已另目录生成并配置成功，正在重新编译ORT；生成器首轮缺FlatBuffers Python导入路径，复用锁定源修正后通过，无新包安装。当前Add固件RAM传输进行中，尚未启动；不作板端ASR/TTS运行声明。
本轮真实日志导出：13文件、20,879事件、6会话，原版官方校验ALL OK；校验只覆盖该次采集时点，后续开发事件下次继续导出。


2026-09-11 14:20 接续：voice-ort-add RAM 启动成功；真实 Add 命令60秒无结果，随后ps及Ctrl+C无字节返回，不能判断具体卡点，已请求用户RESET。旧原始日志保留 evidence/voice-ort-add-20260911/。新增逐调用BEGIN/END并flush的独立探针 native-ort-add-trace，原模型和线程数不变；原生编译及按需链接缺失强符号0，voice-ort-trace独立固件编译成功，镜像静态校验进行中，未上板。
ASR完整ORT库及VITS Lexicon-only库已编译通过；ASR+VITS真实C API双入口联合链接393个外部强符号全部有SDK定义。算子注册仅一个TU改变，已单独编译替换并核对旧归档不变。上述均不代表真实ASR/TTS模型运行。新加载器候选仅在旧4KiB块CRC精确匹配时跳过相同8字节写入，仍须每块及完整镜像CRC通过；尚未用于实板。未新增子代理、未刷写、未运行执行机构。

用户RESET后已确认Sep4基础固件NSH恢复，随后软件重启截停U-Boot，voice-ort-trace开始逐块CRC RAM传输。自动后续脚本只在加载成功标记后执行一次60秒Add测试。独立模型池适配接口用真实NuttX ARM64工具链编译通过（native-shared-runtime-target-compile1），尚未链接或运行真实分配/Session。
