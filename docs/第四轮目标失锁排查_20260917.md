# 第四轮目标失锁排查

用户反馈第四轮角度检测失效。已保存当前会话状态、日志尾部和完整事件/首次失锁上下文到 evidence/stability-session-20260917/round4-investigation/，没有重启板子。

## 证据
当前日志明确记录两组完整三图并经backend_ack accepted=1；随后epoch4正脸q9763存储成功，q9938开始lost1/st6/v0并持续。用户第四轮编号与固件epoch不是同一口径，不能自动宣称前三轮全部通过。失锁前相邻人脸框接近，现有日志没有faces数量/明确失锁原因，不能确定背景第二张脸、短暂漏检或IoU跳变中的哪一种触发。

## 已定位恢复缺陷
拍照模式重复请求原本保持幂等，保护已拍照片；但失锁后也不触发reset，导致界面建议的再次说检测命令无法恢复。修复候选增加原子retry_required：仅失锁后重复mode2触发新revision和整组reset；正常拍摄重复命令仍不丢照片。增加首次LOCK_LOST原因日志（1多脸、2缺失超时、3框重叠不足），不放宽同一目标要求，不混入新旧照片。真实模式代码O0/O2测试已通过，候选photo-lock-retry独立构建中，尚未上板或真人验收。旧固件可先切停止模式，再重新检测；这属于手动恢复，第四轮失败记录不能改为通过。

## 长测采样问题
soak-run-01启动40.045秒后因network_reply_stale退出。原因至少包括UI在busy期间不发送周期radio-status，不能据此证明Wi-Fi断链。用户已实际进行语音拍照，本轮也不是纯空闲共存。原失败记录保留，归类观测不足；不得把它计为2小时通过。后续需修复忙时有界遥测或限定不同测试阶段，不简单忽略新鲜度门限。
