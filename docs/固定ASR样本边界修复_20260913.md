# 固定 ASR 样本边界修复

当前板端仍运行 voice-start-network-20260913，未替换固件。18:22 人声试验录取 48000 帧，ASR 返回 -61，无识别文本，模型池释放后为 0，未触发扫网；是否按提示说话仍待用户确认。原始记录见 evidence/voice-start-network-20260913/mic-trial-182239.json。

无人值守诊断发现固定 WAV 为 160850 帧，但固定样本入口复用了上限 48000 帧的麦克风接口，在推理前即被拒绝。正式 native_asr_runtime.cpp 已为不可变固定样本增加独立的 15 秒编译期上限和 PCM 长度检查，直接使用内部有界识别流程；公开麦克风接口仍保留 3 秒上限。

work-in-progress/asr-fixed-fixture-20260913 保存修改前后源码、复现脚本、原始结果。主机 O0/O2 均复现旧入口拒绝，并验证新入口完整馈入 160850 帧加 4800 帧尾部、正常清理及原有边界/并发检查。主机采用假推理接口，只验证控制逻辑，不能证明识别准确率。

独立 ARM64 对象编译通过，使用真实固定音频头文件；源码 SHA256 为 4a976bb90e562ac034d81fc7c0a27c01d618086b491808082272b4fc894df93b。未同步替换 SDK 正式源码，未完整链接、未上板。下一步将候选独立构建、审计并用 OTG RAM 加载后进行真实固定样本测试，再区分录音时序与模型识别问题。

复现：主机运行该目录 test_runtime.py；Ubuntu 运行 compile_arm64.py。编译记录含完整命令与输入、对象哈希。没有刷写、云台或远程推送操作。

18:48 更新：SDK 原始哈希核对后仅同步运行库文件；独立固件完整编译及 ELF/异常展开审计通过，BIN SHA256 51e0c1e1f9a56514dd4d62734783c008b17c7e5fd1093573bf07ebd5387c2f3c。115987824 字节单包已生成。串口打开返回 I/O 错误，重启命令未发送，尚未加载。当前等待恢复虚拟机串口连接。构建结果 JSON BOM 导致的首次审计读取失败已修正，不涉及固件内容。

18:50：用户恢复串口后已进入 Fastboot，decoder 与 encoder 尾段 CRC 通过；Ubuntu 尚未匹配到 OTG USB 序列号，未传输。当前原 RAM 固件已经停止，无线服务不在线，等待用户将 USB download gadget 接入 Ubuntu。

18:56 更新：单包 OTG 传输 11.166 秒，全模型/固件 CRC 通过并在 RAM 启动。固定样本 160850 帧、17 次解码，42.267 秒返回 75 字节文字，result=0；模型池峰值 468101988 字节、live=0、failures=0。该证据证明推理完成，不证明文字准确率。蓝牙 host 注册及 Wi-Fi shared start=0，未连接 Wi-Fi、未验收手机 BLE。首次初始化日志缺失 NSH 提示符，后续换行恢复后只执行剩余步骤。等待真人唤醒测试。

18:59：真人复测识别成‘互联网’（26.911 秒，9 字节），没有命中唤醒规则；模型池 live=0。已最大音量回放同一录音，等待用户确认开头是否完整，不能认为语音配网通过。原始 mic-wake-scan-20260913-185737.log 保存在本候选 evidence 下，脚本路径沿用旧目录不代表旧固件。
