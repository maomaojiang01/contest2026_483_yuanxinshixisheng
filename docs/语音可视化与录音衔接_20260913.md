# 语音可视化与录音衔接

用户确认上一轮录音开头被截断。新增 `k7sound capture-cued 48000`：同一音频所有权内先初始化 ADC、等待稳定，再播放最大档短音并切换到分组采集，避免原来两个命令间重新初始化的等待。保留既有无提示采集入口；完整语音业务循环尚未改用新入口，可视化测试已使用它。

独立固件 audio-cued-capture-20260913 完整 ARM64 构建、ELF/异常表审计通过。BIN SHA256 `5781770cebc1ba3ee875b6705a03916fdf21b3923bc8ab3ee5a07d6ee10bb46d`。单包 OTG 传输约 11.24 秒，模型、固件 CRC 和 RAM 启动通过。

真机提示音 3200 帧完成，停止正常；提示音结束到 RX 开始 32693 微秒，采集 48000 帧，分组 mismatch=0，codec/platform/I2C 清理均返回 0。该验证不等于人声准确率通过。蓝牙 host 初始化与 Wi-Fi 共享服务启动通过，Wi-Fi 未连接，手机 BLE 连接未验收。

新增正式工具 `host/voice/wake_panel.py`，在 Ubuntu 原生 GTK 窗口显示准备、录音、识别和真实扫网状态。用户点击开始后持续重试直到板端返回等待网络选择及非空列表；停止等待本轮安全退出。窗口已启动并启用按钮，三组主机判定测试在 Windows/Ubuntu 通过，实际窗口截图已检查。没有把主机注入文字或模型响应当作真实配网成功。

构建及原始设备记录见 `evidence/audio-cued-capture-20260913/`，界面记录见 `evidence/voice-panel-20260913/`。当前仍未完成真实语音配网；后续验收人声唤醒、选择网络、密码规则确认与真实 IP。未刷 eMMC/STM32、未启动云台、未推送仓库。

19:10 用户窗口实测通过：ASR 你好联网你好联网，板端 mic_state=2 grammar=1 count=3；真实扫描提供 Lansee、Lansee_vistor、Lansee_auto。当前一次性 wake 命令返回后 Controller 销毁，GUI 未继续选择网络/密码步骤，因此不能自动连接。后续必须使用持续的板端 Controller 会话接续，不能把这次扫网标记当作 WPA2/DHCP 成功。原始证据 evidence/voice-panel-20260913/wake-panel-20260913-190956-374411.log。
