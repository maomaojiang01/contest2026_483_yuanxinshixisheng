# Windows Codex 自动日志采集

2026-09-14 安全刷新：最终手动刷新三个显式主会话后，原版校验器检查 16 个 JSONL、35,571 个事件并返回 `ALL OK`。秘密复查发现 9 月 13 日启动的常驻 watcher 没有加载后来加入的 `tp-` Token 脱敏规则；先核验 PID 29776 的命令行确为本项目脚本，再停止并以同一 Codex Python 3.12 `pythonw.exe` 隐藏重启为 PID 43616。新进程已重新导出当前主会话，用户曾提供的 MiMo key 和含 key 的历史工具命令均被替换。新增 `tools/test_log_export_core.py`，与 hook 范围、分日写入测试合计 10 项通过，覆盖合成 MiMo/OpenAI Token、Bearer、私钥、已知密码 JSON 转义形态和媒体块。后续修改脱敏代码时必须重启常驻 watcher，不能只刷新文件。实时数量继续以 manifest 和 `private/log-collector/watcher-status.json` 为准。

2026-09-10 修正：`log_daily.write_session` 在写入前比较内容，完全相同则跳过，不创建临时文件或替换历史文件，避免刷新历史日期目录的修改时间。已有文件系统修改时间不回填成消息日期。跨日新增与当日追加测试通过；历史内容如因明确补录或脱敏修正而变化，仍按真实写入时间更新。

## 长任务持续更新（2026-09-09）

此前 Stop hook 仅在整轮回复结束时触发，不能保证持续数小时的活动轮次中更新。现补充 `tools/watch_project_logs.py`，每 30 秒检查指定当前开发主会话 `01a07ed4-3f0d-7450-8bb1-bb756849cb4e` 的原始文件，有变化才重新脱敏导出并运行官方校验。它不调用模型、不联网、不操作 Git 或硬件；与 hook、手动导出共用写锁，并通过单实例锁避免重复运行。

已启动隐藏 Python 进程，并添加当前 Windows 用户登录启动项 `VelaVision Log Collector.lnk`。不依赖当前桌面任务是否重新加载 hook。只采集已落盘记录，正常更新延迟约 30 秒加导出时间，不是逐字实时同步。跨日按北京时间分文件。

监测状态：`private/log-collector/watcher-status.json`（会话、最近成功时间、数量、校验结果）；进程信息：`watcher.json`；错误时间：`watcher-error.json`。状态未变化可能表示原始文件无新记录，不能仅凭文件时间判断故障。Stop hook 仍保留，但当前长任务持续采集以 watcher 状态为准。

停用此补充监测器：先核对 `watcher.json` 中 PID 的进程命令确实指向本项目 `watch_project_logs.py`，再结束该进程，并删除当前用户启动目录中的 `VelaVision Log Collector.lnk`。仅在 `/hooks` 关闭 Stop 不会停止此独立监测器。

旧主会话 `01a06679-4033-7993-b989-6269e03e0e70` 已按用户要求采用同一北京时间分日规则，历史记录已实际拆分，后续手动刷新不会再合回 9 月 3 日。拆分后重新拼接与原日志哈希完全一致，证据见 `evidence/old-session-daily-split.json`。该旧会话工作目录在项目之外，自动采集范围仍不扩大到其他目录。

## 2026-09-09：当前开发会话按日保存

用户指定「接续VelaVision运行时接入」（`01a07ed4-3f0d-7450-8bb1-bb756849cb4e`）按北京时间的原始事件日期分文件。9 月 8 日记录保存在 `logs/maomaojiang01/2026-09-08/`，9 月 9 日记录持续写入 `2026-09-09/`；未来跨日自动创建对应日期目录。回复正常结束时由 Stop hook 更新，不是逐字实时写入。手动补录也使用相同规则。

各文件保持同一 session_id，seq 跨日期连续，不重复复制整段会话；manifest 只有一个会话条目，event_count 为累计数，file_path 指向最新日期，files 列出各日数量/哈希，顶层会话 sha256 为完整会话流哈希。已通过现有原版官方校验器的跨日期聚合校验。其他历史会话保持原目录规则。

2026-09-09：已安装用户级 Stop hook，每轮正常回复结束后运行 `tools/auto_collect_logs.py`。这是一层本地 Windows 兼容适配，不是官方安装器。SDK 获取方式无需改变，也不需要为此重下源码或伪造 `.repo` 目录。

## 已验证与待验证

- 已用本次真实 Codex 原始会话直接调用采集入口，生成第三个项目会话；原版官方校验器通过。
- 四项范围测试通过：主项目、子目录和工作区根允许，其他项目、相似前缀路径和 SubagentStop 不采集，非法会话 ID 拒绝。
- 已安装 `C:\Users\pc2025\.codex\hooks.json`；经用户要求，已通过 Codex 正常 /hooks 界面信任并启用，显示 Stop Installed=1 / Active=1。
- **真实自动触发验收通过**：Codex CLI 0.153.4 会话 `01a084bd-4cd6-7910-a51e-2354fa1eee74`，测试标记 `VV-HOOK-20260909`，自动写入输入、LOG_TEST_COMPLETE 回复及用量，共 3 条。官方校验 4 文件、10,809 条事件全部通过。证据见 `evidence/log-hook-verification.json`。
- 当前已打开的桌面任务仍需重新打开或重启应用以加载新增 hook；本次未把 CLI 验收冒充当前桌面任务已触发。

## 使用与以后重装

本机已完成下面第 1 步及 CLI 的第 3、4 步，用户无需再次信任。当前桌面应用只需重新打开任务，必要时重启。

1. 使用同一 Windows 用户、同一 CODEX_HOME 的 Codex CLI，在 `E:\openvela\VelaVision` 打开 `/hooks`，审阅并信任指向 `tools/auto_collect_logs.py` 的 Stop hook。
2. 重新打开桌面任务，必要时重启 Codex。工作目录选择 `E:\openvela\VelaVision`，也支持 `E:\openvela` 根目录。
3. 发送：“这是日志采集测试，请只回复‘日志测试完成’，不要修改文件。”等待回复结束。
4. 检查 `private/log-collector/status.json` 的会话 ID、采集时间；确认对应 JSONL 同时包含这次输入和回复，且 `private/log-collector/official-validation.txt` 显示 ALL OK。不要用本次直接调用测试的旧状态当作新验收证据。

Codex 要求信任具体 hook 定义，新增或修改定义后需要重新审阅。见 [OpenAI 官方 Hooks 文档](https://learn.chatgpt.com/docs/hooks)。若 CLI 不支持 `/hooks` 或与桌面使用不同 CODEX_HOME，需要先核对客户端运行环境。

## 范围和维护

- 归属 `maomaojiang01`，队伍 `contest2026_483_yuanxinshixisheng`，写入同仓 `logs/maomaojiang01/`，按原会话开始日期分类。
- 仅采集工作区根或 VelaVision 内的本机主会话；其他目录、远程 Ubuntu、WSL、Codex 外部 worktree、子任务和带 fork 标记会话不自动纳入。
- 继续复用 `log_export_core.py` 的脱敏规则和本地私密配置；缺失已知密码脱敏配置则停止。排除内部推理、系统/开发者提示和媒体，保留 degraded 范围声明。脱敏规则不能保证识别任何形式的未知秘密，提交前仍需审阅。
- 每次按 tool + session_id 合并 manifest，保留其他会话；Windows 导出写入共用文件锁。不执行提交、推送或硬件操作。
- 手动更新两个历史主会话仍使用 `tools/export_project_logs.py`。历史 evidence 和 project-manifest 的日志哈希属于快照；实时最新清单在 logs 内的 manifest。
- 本机依赖复制到被 Git 忽略的 `private/log-dependencies/`，Python 使用 Codex 捆绑的 3.12。重装机器时需按 `tools/requirements-log.txt` 安装依赖并重新运行 `tools/install_log_hook.py`。
- 失败摘要写入 `private/log-collector/error.json`；成功状态有独立时间，旧错误文件不代表最新运行失败。不要提交 private 目录。
- 在 `/hooks` 停用该 Stop hook 即可停止自动导出。Codex 升级若改变原始 JSONL 格式，需要重新验收适配器。

本次只为实际使用中的 Windows Codex 配置。指南中的 Linux 安装、SSH、推送流程不属于本次变更。
