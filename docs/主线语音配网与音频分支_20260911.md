# 主线语音配网与音频分支

用户明确要求：优先纯板端语音配网，不采用电脑辅助识别；杂声修复在分支进行。

## 主线

- 分支：`dev-ai-contest-2026`。
- 工作区：`E:/openvela/VelaVision`。
- 目标：板载录音 → 本地 ASR → VoiceLink 状态机 → 真实网络扫描/选择/密码提交 → WPA2/DHCP → 本地 TTS 播报。
- 共享扫描/连接候选已迁入 `app/k7radio`，`voice-shared-20260910` 已独立编译并通过 ELF 检查，未单独上板。
- `app/voicelink` 控制核心、真实无线适配器和 `k7voice probe|scan` 已在 `voice-native-20260910` 原生编译链接。ASR/TTS 运行库仍待移植；该命令仅是网络集成诊断。
- 本地查到的可执行语音库是 Windows x64，不能用于 NuttX。ASR/TTS 模型存在，但不能仅复制模型就运行；完整原生源码依赖、OS 接口、分配器和实际文件路径须先接通。

编译首轮同步拒绝了音频 profile 旧版本。只读核对 SDK 配置 SHA256 与 `audio-guard` 已完成构建记录均为 `aa7f9e17eb9c1cd08450c876e38bd51e8a468856f9451b9cd021fed9eed38be7` 后，才允许推进该确切前驱。保留首轮 staging，不接受未知 SDK 修改。

## 音频分支

- 分支：`audio-noise-20260911`。
- 独立 Git worktree：`E:/openvela/worktrees/VelaVision-audio-noise`。
- 当前输入为主线未提交源码的 52 文件快照，逐项哈希见该目录 `audio-input-manifest.json`。Git HEAD 仍为脚手架提交，不能声称全部开发源码已提交到分支。
- 已接入同源路由候选，分支源码 O0/O2 主机测试通过；没有上板验证。
- 不操作板子、COM8、蓝牙、SDK、U盘、云台，不修改主线，不提交推送。需要真机实验时由主线统一安排。
- 主线保留当前录放音基线，不再将音量、滤波或杂声实验作为语音流程主线的持续变更。

## 当前设备与验收边界

最新覆盖：`voice-owner-20260910` 已 RAM 启动，BIN `048bbb796aa6a2e90c032049c7b2eac1c26954c6901359d7f58bf6b98e6bc1e8`。冷启动初始化及持久 `k7_wifi_svc` 任务组修复后，三次独立真实扫描均通过；后两次分别2.922秒、2.938秒，均返回64项并包含Lansee。任务在启动命令退出后保持，host fault=0。当前Wi-Fi未连接，BLE仅服务启动，未测客户端。见 `evidence/voice-owner-20260910/device-state.json`。

以下保留两次失败定位历史，不能当作当前在线状态：cold版本消除了固件断言，但启动任务退出时pthread随任务组消失，导致扫描35秒超时；owner版本将工作线程迁入独立持久任务后才通过上述扫描。尚未在owner版本验证密码认证/DHCP，也没有ASR/TTS或语音成功结论。

`voice-native-20260910` BIN SHA256 `0349c64f59a8634ddea64d65598ec03190edade02a4a056c98baccf763be52a4`，ELF SHA256 `57a07361973c55fec21ed509fa513f0076886d70fe58679dcfd8745c355b3c1b`。配置启用、builtin 应用注册、异常表、8 MiB 镜像范围和二进制核对通过，已 RAM 启动并看到八个 idle 任务。首次 wifi-service-start 在首次 OPEN 之前发送 STOP，触发 `BSPASSERT:port.c-237`；命令6/4超时，probe 返回 native_network=0。扫描没有开始。证据见 `evidence/voice-native-20260910/device-state.json`，旧版本在线状态不能沿用。

首次 native 构建返回 0，但严格配置门禁发现没有 K7VOICE。原因是 CMake 的 `nuttx_generate_kconfig` 在已存在生成的 `apps/Kconfig` 时直接返回，遗漏新加入的应用。保留 attempt1 构建日志、配置和菜单后，仅移开生成的顶层菜单缓存，重新配置编译；没有绕过门禁，也没有改 SDK 生成器。重新构建已确认 `CONFIG_EXAMPLES_K7VOICE=y` 和 builtin_list 中的 k7voice_main 注册。

纯板端语音配网尚未完成。不得用文字模拟、电脑识别、固定密码串口测试或 BLE 配网替代其验收。密码不写日志、不交云端模型。

代码和日志均以主线为统一入口，AI 日志归属 `logs/maomaojiang01/`。本次没有提交、推送或刷写。
