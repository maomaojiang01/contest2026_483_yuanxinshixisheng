# Agent 部署接续

2026-09-10最新决策增量：主线推荐原生openvela CPU llama.cpp＋规则兜底＋可信设备工具，Qwen2.5-0.5B-Instruct Q4_K_M为首个候选，不投入双系统或Linux ABI兼容。详见 `离线Agent选型与B0落地方案_20260910.md`。当前四A72计算与无线短时并发分项已通过，但接收异常与长稳仍待查；当前C++、模型文件、allocator及真实板端ASR/TTS仍有缺口。下文“需选择模型服务”为旧阶段记录，不能覆盖本条推荐或表示Agent已部署。

按用户顺序，先完成无线测试，再核对 Agent 部署条件。2026-09-09 18:43，Wi-Fi 30 分钟实测结束：1,500 发、1,499 收，约 0.067% 丢包，连接全程保持，空闲堆没有下降；预设零丢包标准未完全通过。手机在长测开始前断开，因此蓝牙保持仍未验收。

## 当前核对结果

- 原生入口仍为 `E:\openvela\VelaVision`，RK3576 应用层规划为 `app/k7agent/`，该应用尚未实现或上板部署。
- 对 `app/`、`host/`、`legacy/` 的模型服务/Agent 相关源码核对，没有找到已实现的 Agent 运行时或现成模型 API 配置；本地私密配置目录当前仅有无线配置。
- NPU 已有的固定矩阵测试不能替代语言模型推理。不能将关键词匹配或配置模板称为已经部署的任务理解模型。
- Wi-Fi 已有网关通信证据，DNS、TLS、互联网模型接口及错误恢复尚未验证。
- `config/agent-model.example.json` 是待选择模型的配置模板；无密钥，未启用服务。

## 首个部署验收范围

第一条任务为“报告机器人当前状态”：文字输入 → 模型产生明确的状态查询调用 → RK3576 应用层读取真实设备状态 → 返回模型与用户。返回结构应含任务 ID、步骤、状态、错误码和实际结果，不用模型描述代替设备执行证据。

状态查询适配完成后，再接入已有单张拍照等能力。云台动作在本轮部署测试中保持禁用；不启动跟随、不刷写 eMMC/STM32。STM32 继续负责实时控制，不承担语言模型任务调度。

## 部署仍缺的信息

需要确定联网模型服务（服务名称、模型和调用地址），或具体离线模型方案。这会决定适配器、内存预算及构建依赖，不能从已有 Wi-Fi 密码或 Codex 登录状态推导出可用模型服务。

若使用联网服务，密钥保存到 `E:\openvela\本地私密配置\agent-model.json`，不放入源码、普通串口日志或 AI 日志；本模板不自动创建、调用或收费。真实模型接入、板端执行器、失败/超时/取消和 RAM 上板验证完成后，才可将部署状态改为已运行。

无线长测原始结果与逐分钟数据见 `evidence/stability-20260909/wifi-run-01/`；本轮没有变更已运行的无线固件。
