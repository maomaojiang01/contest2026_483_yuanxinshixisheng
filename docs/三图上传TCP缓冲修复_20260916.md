# 三图上传 TCP 缓冲耗尽修复候选（2026-09-16）

## 已有证据与边界

旧 photo-retry 固件真实三图为 31180、28997、30351 字节，epoch=2；首次 `photo_transport stage=left result=-11`，后端未受理。后续原生重传在 connect 阶段返回 -5。Windows 10.3.1.125:8001 正常监听，板端保留 10.3.0.214/22，但主机邻居解析失败、Ping 2/2 超时。板端执行网络探测后控制台未再返回命令结果，视频仍继续输出。不能仅根据 wifi=1 判断 IP 数据通道健康。

实际旧 SDK 构建配置：IOB_NBUFFERS=128、IOB_BUFSIZE=256、IOB_THROTTLE=0、NET_SEND_BUFSIZE=0、TCP_WRITE_BUFFERS=y。总 IOB 数据容量约32KiB，TCP发送积压无配置上限且无保留池。SDK tcp_wrbuffer.c 对发送使用 net_iobtimedalloc(true)，网卡和发送克隆仍需额外 IOB；因此存在发送占尽缓冲、使收发无进展的配置风险。故障发生于约31KB正脸图之后与此一致，但真机修复验收前不能宣称唯一根因已证实。

## 已完成候选

- 独立配置 `board/kickpi_k7/configs/velavision_photo_tcp/defconfig`：256×256字节总池（64KiB），IOB_THROTTLE=32预留8KiB，NET_SEND_BUFSIZE=8192限制TCP排队。单次应用写入最大1024字节；SDK队列检查不是严格逐字节硬上限，可能包含本次写入的短暂超额。
- `connect_server`保留真实超时/网络不可达/拒绝连接错误，避免统一返回-EIO；失败关闭socket。
- SDK同步前保留哈希和差异，仅同步上述配置和board_speech_bridge.c，保护未知改动。
- 独立ARM64构建成功，固件5,007,520字节，SHA256 bfac0d73ce9d9d94c443e2907def6ec91b414c0c571553ce7d45e2b27094d027；RAM未加载。
- 已核对实际`.config`值，证据 `evidence/photo-tcp-20260916/network-config-watch.json`。
- 真实C连接函数O0/O2错误路径回归通过；原生photo-retry O0/O2通过；网关/上传13项回归通过。这些不是板端上传实测。

## 待执行真机验收

旧固件控制台不响应，Ctrl-C未恢复；用户已收到物理RESET请求，并明确告知RAM三图会丢失，需要重新配网重拍。复位监听只接管U-Boot后进入fastboot usb1，不自行断电。新固件通过OTG RAM一次加载，不写eMMC/STM32。

上板后：App配网；检查真实网关TCP通信；保留短音24dB/播报10dB/板端本地提示；相机待命，用户真实三角度拍摄；检查K7S1U网关三个JPEG字节数、spool文件、后端受理taskId以及板端accepted=1；再用同组原生重传验证幂等性和后续网络可用。后端受理不等于报告分析完成。

目标仍未完成，必须以真机后端受理证据结案。

### 修复版真机加载与配网

用户按RESET后接管成功，Fastboot由用户接入Ubuntu；photo-tcp固件已RAM加载。21,784,736字节OTG耗时2.1755秒，包CRC0f53c5e4、固件CRC938d8660、四段无线CRC通过，BLE服务registered=1、Wi-Fi服务ret=0。见startup.log和verification.json。

App本次重新配网后，新鲜板端日志确认DHCP10.3.0.214，radio_generation=1、wifi=1、ipv4_ready=1。Windows到K7 Ping3/3，88/91/114ms；只是上板后基础连通检查，尚未证明大图传输修复。联调窗口PID37503，等待openvela link probe接入及用户相机待命、真实重拍。

等待视频接入期间，Windows到K7 Ping30/30、0丢包，25–215ms平均67ms（pre-photo-ping.txt）；不等于照片发送负载验收。Windows当前锁屏，Computer Use只读截图确认后停止UI输入并请求用户解锁。需解锁后将openvela link probe接入Ubuntu，再进行真人三角度拍摄。当前串口由联调窗口37503持有，禁止另起串口消费者。

### 验收暂停点

连续三轮续跑均未收到用户开始真机拍摄的操作。最新核验：Ubuntu联调进程37503实际存活，Wi-Fi=true，视频USB已接入但frames=0/camera=false/done=0；本地photo-upload-spool中accepted.json数量0。修复版已部署且基础网络通过，三图负载和后端受理仍未证明。此时无法用模拟照片或Ping代替用户真机三角度验收，也不需要再次重启。等待用户解锁电脑、确认0/0与无遮挡并进入相机待命后恢复目标。

### 2026-09-17 09:04 隔夜状态覆盖

联调进程37503仍实际运行，状态文件持续更新。最新串口radio-status仍回应wifi=1/ipv4_ready=1，控制台有新鲜nsh提示符。USB视频从此前已接入变为Errno19，Ubuntu lsusb不再列出1209:0001；原因未确定，不归因于板子重启或用户拔线。相机未待命、frames=0/done=0，后端accepted记录仍0。需要在后续现场测试前恢复openvela link probe接入；无需因此直接重启或重新烧录。未启动录音、相机或云台运动。


## 2026-09-17 真机三图上传验收
用户完成三角度拍摄并确认提交。板端 photo_transport stage=backend_ack result=0；photo_upload epoch=2 result=0 accepted=1，任务 aa9c678f-c7b7-4e0c-a93e-05e6a071a389。网关保存正面26344B、左侧25785B、右侧26124B，后端 accepted=true/status=queued/photoVersion=1。上传后 Wi-Fi 仍在线，后续多轮 ASR produced=150/consumed=150/result=0，视频仍持续接收。本轮上传闭环通过；报告未完成，重复大图及长期稳定性未验收。脱敏证据 evidence/photo-upload-accepted-20260917/verification.json；原图保留 private，不复制到公开证据。
