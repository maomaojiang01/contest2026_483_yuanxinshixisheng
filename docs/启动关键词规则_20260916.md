# 启动关键词规则（2026-09-16）

用户明确要求：识别到“启动”即可启动云台，同时支持“启动openvela”“openvela”和“openvlea”。板端只根据final、完成的识别会话匹配，仍不执行partial。对标点/空白归一化，并将ASCII大小写转为小写；含上述任一关键词触发tracking_start，保留旧“开启云台”。同句停止云台/停止测肤/停止播报优先；“不要”“别启动”“取消启动”不启动。多final歧义处理保留。

与此前严格整句匹配不同：现在带“启动”的较长句子也可命中，这是本轮用户要求的放宽。真实动作仍要求界面确认、相机待命，匹配由板端执行。

修改：device_voice_intent.c、主机voice_intents.py、相关C/Python测试、窗口口令提示。16项Python测试和原生C -O0/-O2通过。工具start_keyword_build.py通过精确SDK前驱审计同步，仅修改板端意图文件；保留ASR150帧缓冲及诊断。构建进行中，尚未上板。

已构建完成并collect校验：2807928字节，SHA256 1a4276bfa861b56159ebd439f32504dab6a128d286d5eb383dbb35d9e99ef44d。确认窗口空闲/held后关闭，软件重启进入Fastboot；Ubuntu未接到18d1:4d00，等用户连接USB download gadget。旧Wi-Fi/视频状态失效，尚未RAM加载；tools/load_start_keyword.py为加载入口。

Ubuntu恢复后新启动关键词固件已RAM加载：19585144字节/1.9149秒，包CRC8ea7f21b、固件CRC1d3e3e81、无线四段校验通过，SHA1a4276bf…99ef44d。BLE registered1、Wi-Fi服务ret0。新版镜像/人脸框/33ms渲染窗口已部署PID2828，GTK/cairo及GI桥接验证可用，启动日志无异常；截图已检查布局，但无视频设备，不能算画面实测通过。Windows语音9568/网关39244正常监听。等待App配网、openvela link probe接入及用户相机待命；尚未真实新口令动作或三角度验收。
