# 并行开发责任与验收矩阵

用户已授权主、次会话各组织三个 GPT-6 Astra / Medium 子代理。并行工作交付候选，正式代码和硬件始终由主会话统一集成；不表示六个代理必须一直占用运行资源。

| 协调者 | 代理工作 | 唯一候选目录 | 对接边界 |
|---|---|---|---|
| 主会话 | C++异常展开与线程风险审查 | parallel-cxx-unwind-medium | 两文件补丁、ELF门禁、线程风险；不操作SDK |
| 主会话 | 模型只读文件接口 | parallel-model-reader-medium | 有界普通文件读取；POSIX默认拒绝，待实际挂载验证 |
| 主会话 | A72/NEON诊断 | parallel-neon-probe-medium | CPU4/5短时诊断；不进行全面性能评测 |
| 次会话 | 视觉检测结果适配 | parallel-vision-contract | 沿用实际视觉接口，新检测能力缺失时明确不支持 |
| 次会话 | RK3576↔STM32通信契约 | parallel-mcu-contract | 现有UART线格式兼容、主机编解码；无CAN或PID实机宣称 |
| 次会话 | 机器人Tool调度 | parallel-robot-tools | 类型化请求、真实事件完成、事务与取消；默认禁用执行机构 |

所有目录均在 `work-in-progress/` 下。原次会话已交付 `parallel-llama-pool-safety`，新六项不重复线程池、已有VoiceLink/Wi-Fi状态机或音频候选。

正式源码、共享配置、Ubuntu SDK、COM8、蓝牙、摄像头、USB、STM32、云台和日志仅由主会话处理；不刷eMMC/STM32，不提交推送。代理间只传说明/交付，不直接应用对方代码，不继续嵌套代理。输入快照哈希与当前源码漂移分开记录，主会话正常集成造成的漂移不回滚。

## 已完成与接下来的门禁

- 主会话三份候选及线程池交付共99份输出哈希核对通过，见 `evidence/agent-prerequisites-20260910/delivery-review.json`。哈希核对不替代真实测试。
- `cxx-unwind-20260910` 已RAM真机通过基础C++构造、单主线程异常RAII、对齐分配、线程与条件变量；无线真配网、30秒保持和网关5/5通过。并发异常尚未验收，实际GCC libgcc线程模型single是后续门禁。
- 当前探针测得off_t32；`neon-file64-20260910` 已编译，启用FS_LARGEFILE并加入NEON诊断，真机结果以对应evidence为准，不能凭构建结果宣称通过。
- 模型读取器源码已进入 `app/k7agent/model_reader`，实际普通文件挂载、文件哈希和llama内存分配域仍未接通。没有部署Qwen或语言理解服务。
- 次会话三个候选已交付；主会话核对81份输出哈希及60项输入，核对时均无漂移，见 `evidence/robot-contract-review-20260910/review.json`。视觉主机2236项检查、MCU离线22项测试、Tool两优化级各6073项检查属于候选原始证据，本次没有重复测试或板端验收。
- MCU现有UART没有动作完成ACK，CAN未接通，PID数学代码不代表当前控制闭环。Tool候选仅提供能力/状态查询描述，执行机构禁用、真实后端未接；不能把发送成功或本地Accepted升级为设备完成。
- 最新串口中断记录 `evidence/neon-file64-20260910/interruption.json`：RAM新镜像未完成加载，COM8未枚举，当前无线运行状态未知。最后Cxx镜像通过记录仍保留，不能代替新镜像验收。

## 最终目标对齐

原生BSP、八核/DDR/eMMC基础 → C++与计算/模型读取 → 真实llama CPU核心 → 文本到经校验Tool → 设备事件反馈 → 语音入口与播报。视觉与STM32候选在旁路同步准备，最后通过同一事务和状态接口连接。

人脸跟随、三视角拍照与固定NPU INT8矩阵基线保留；人体/仪器模型、完整NPU运行时、CAN、板端ASR/TTS及整机感知—决策—执行闭环尚未完成，不因主机模拟或并行交付提前标记成功。

## 第二轮并行候选

用户再次要求提速后，复用同三个GPT-6 Astra/Medium代理，各自在原目录下的新子目录工作，不修改冻结交付：

|候选|独立范围|已交付边界|
|---|---|---|
|concurrent-probe-v1|冷/热并发异常、RAII计数和创建失败清理|8种主机情形；未编译或运行目标探针|
|integrity-v1|同一文件句柄流式SHA256、失败撤销READY|43条主机命令；不能排除校验后同长度修改，须不可变存储|
|affinity-v1|创建前绑核、实际核号门禁、错误传播|70项模拟检查；尚未接入实际池或交叉编译|

主会话已核对这轮48份输出哈希，见 `evidence/agent-prerequisites-20260910/followup-delivery-review.json`。三项现已交付；不把完成后空闲说成仍在运行。

同期主线独立完成NEON/file64真机和无线恢复，以及39个llama ARM64对象和完整链接门禁。llama ELF异常表检查通过，含2个CIE、2323个FDE；尚未上板。详见 `docs/原生llama编译与NEON验收_20260910.md`。当前主线瓶颈转为正式集成、加载内存边界与真实图计算；串口/SDK操作仍只能有一个所有者。


2026-09-10追加：第三轮layout/graph/tool-json已交付，graph经主会话目标编译和真机通过，tool-json仅目标编译。后续A负责冷失败静态分析、控制ELF审查及threaded-unwind计划；B负责model-arena-plan/buft；C负责接收PN拒绝审计/诊断候选。全部隔离目录；只有主会话接入SDK和COM8，候选通过不等于目标集成。


## 模型存储接入轮次（2026-09-10）

三个代理均为 GPT-6 Astra / Medium，使用已有代理接续，最多三项同时运行。主会话独占正式源码、SDK、串口、BLE及RAM加载。所有候选从冻结输入生成，目录不交叉；不触碰其他任务的工作区。

| 代理 | 已交付 | 接续任务 | 写入边界 |
|---|---|---|---|
| A | checked MSC v2，31组mock传输 | USB诊断输出验收器 | parallel-cxx-unwind-medium |
| B | FAT只读候选，38份文件哈希核对 | FAT与模型reader身份/64位契约 | parallel-model-reader-medium |
| C | 同句柄模型接入审查 | k7storage真实源码mock测试与错误清理 | parallel-neon-probe-medium |

主会话已验收arena-provider真实模型池缓冲区与无线回归，正在准备MSC-only独立固件；此轮FAT不启用。U盘已插板但尚无枚举结果。模型权重、完整推理与Agent业务尚未完成。各代理主机结果不代替目标编译或真机结果。

音频接续轮次：用户确认MIC接麦克风后，A→audio-i2c3-adapter-v1，B→audio-codec-plan-v1，C→audio-sai1-adapter-v1，均在各自原目录下。未知寄存器和未核定初始化继续拒绝硬件操作；不重复旧217项交付测试。根任务继续独占USB RAM加载、只读验证和无线恢复。
