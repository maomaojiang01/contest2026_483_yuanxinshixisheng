# 命令延迟与拍摄提示优先（2026-09-17）

用户反馈本地角度提示滞后、你好OpenVela响应慢并要求优化。实查当前150帧=3秒固定采样，录音开始至ASR完成多轮约3.15秒（不是说完后耗时）；本地提示日志local_prompt确认不调用云TTS。photo_watch原来等待device_busy清除后才播放，造成拍照事件排在整轮ASR之后。

本次候选：command_endpoint.h在每20ms完整PCM帧上判定；平均绝对幅度>=350记语音帧，>=200清除静音计数；至少8个语音帧、至少录800ms、连续400ms安静则提前结束，最晚仍3秒。未检测到足够语音或弱声保持3秒，保留所有句首样本、不改变短音→ADC衔接，不用于自然聊天。不是训练VAD模型，阈值需要真机验证，未承诺绝对首声延迟。

record回调通过PIO正返回值提前正常结束，负值仍异常取消，SAI/功放原清理流程保留；ASR接受完整变长帧序列，只在完整completed后执行命令。photo_watch设置全局提示优先标志：在正在采集的命令轮次，下一个20ms边界结束录音并丢弃该轮识别，释放音频给拍摄提示；不把被截断的轮次执行成命令。尚不打断正在播放的前一句提示；也没有把云端网络等待变成零延迟。新增endpoint reason/captured_ms/last_active_ms和prompt_started时间日志。

验证：真实command_sample回调与ring O0/O2证明1秒语音+400ms静音=70帧、句首/帧序列完整、提示优先20ms边界、chat仍150帧；静音/弱声回退、300ms句内停顿、INT16_MIN幅度无溢出通过；PIO提前成功/错误取消/清理失败共6场景O0/O2通过。模拟测试不等于真实人声通过。

独立固件command-latency-20260917编译完成，5007520B/SHA fe469a6a3701ed4ff641b1a207b8eb43f15d6cfc968af4e0f3afae024142a8fe；只审计同步board_speech_bridge.c、command_endpoint.h、pio.c、pio.h。保留侧脸±13°、固定提示语10dB/短音24dB、三图上传和mock报告流。tools/command_latency_build.py、load_command_latency.py、test_command_latency_vm.py。

旧GUI64308因UART前缀交错未识别loop_done=-125而busy滞留，日志实际已停止。UI仅修正对完整loop_done字段的解析。进入更新前先确认云台held、三图done7已播报告，再发送device-stop/tts-stop并读tts-state busy0；软件reboot接管Fastboot。当前新固件尚未加载，等待USB download gadget接入Ubuntu。旧Wi-Fi在线状态已失效。最新已受理任务c80aff50-1aba-45c1-8c40-12b675e89edb，后端三图已保存，无需抢救RAM。

下一步接USB→加载校验→恢复GUI/App配网→先单次语音检查说你好OpenVela，核对endpoint和完整识别；再真人三角度测试比较事件→本地提示起播，不在用户未准备时自动录音/运动。保留失败与回退证据，无法确认时不得声称已提升实际响应速度。当前网关51104/8001、speech服务保持，mock-sse不会作为真实报告。

更新：11:17 RAM 加载完成，21784736B/2.141秒，包CRC f992e21c、固件CRC 654ca198 校验通过；蓝牙配网与Wi-Fi服务已启动。GUI PID72640 已恢复（不自动录音/运动），等待本次App重新配网。真实人声延迟优化仍待验证。

用户报告启动提示后云台不跟随。该轮日志VOICE MODE applied=1 result=0，跟踪段UART写入计数和坐标持续变化；这不是电机执行回执，不能否定用户观察。已保留tracking-investigation.log/json。当前云台held；未盲目重启跟踪或扩大限位。等待用户确认全程不动/曾动后停止和独立供电，再区分UART输出与STM32/舵机执行。新一轮三图后台受理和mock报告3段完成，但不代表云台物理跟随验收。

11:53周期实查：重启GUI后的相机采集已结束，camera=false、voice_enabled=false、held=true、Wi-Fi在线；程序重启未重置板端voicecam的1800秒会话。相机不能在本次启动重复初始化，未自动重启板子或录音/运动。需要恢复相机会话后才能再测实体跟随；此前的三图和mock报告结果不可误记为本轮新拍。证据camera-session-ended.log。

13:52用户要求重启的续接完成：OTG自动检查已接入，restart-1352加载21784736B/2.302秒，包CRC f992e21c、固件CRC654ca198通过，BLE和Wi-Fi服务启动。窗口重新启动，不自动录音/运动。旧Wi-Fi状态失效，需App重新配网；云台不跟随问题尚未验收。

2026-09-17 报告播报已切正式brief来源，停止mock文案和测试声明；见docs/真实报告内容播报切换_20260917.md。网关18860，窗口74371，19项测试通过，喇叭待验收。

## 2026-09-17 用户验收覆盖
用户明确反馈“跟随已经修复了”，云台实际跟随更新为用户实机确认恢复，取消“跟随未修复”待办。此结论来自用户观察，不冒充MCU回执或自动测量；不据此推断长期稳定、双轴精度或全部整机流程完成。此前不跟随日志保留。
提交收尾仍需：官方模板3.1–3.7技术报告、最终演示与证据索引、审阅后的源码/Skill/AI日志提交推送及远端核验。当前AI日志最近校验为19文件43295事件ALL OK；源码仍有未提交内容。本次不执行Git提交、推送或官网提交。
