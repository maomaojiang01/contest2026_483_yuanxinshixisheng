# 夜间持续任务

用户授权持续推进到北京时间 **2026-09-11 09:00**，期间无需确认。
当前任务目标已创建；接续自动任务 ID：`velavision-9-11-09`，每15分钟检查。

优先完成音频滤波候选审查、独立固件构建、ELF门禁和有界RAM验证；随后推进本地语音配网。真实采样周期性零值尚未修复，滤波仅用于回放对照，不能宣称恢复了正确16k采样。最新PGA24实听反馈为“人声更大，但杂声仍明显”。

不安排用户说话或听音测试，不无人值守反复最大音量播放。需要人耳确认、重新供电或缺少硬件时记录待办，继续其他可完成工作。仅主任务操作SDK、COM8、BLE及正式源码；候选代理只能写各自目录。保持不刷eMMC/STM32、不启动云台、不提交或推送的约束。

每步保留输入哈希、源码、原始输出和失败记录，主机结果与板端验收分开。不要因自动接续重复启动串口或构建进程。截止时停止启动新测试，收尾自有进程、保存当前设备状态与未完成项，刷新官方AI日志并校验，然后暂停自动任务。电脑/虚拟机休眠或断电会使设备验证中断。
