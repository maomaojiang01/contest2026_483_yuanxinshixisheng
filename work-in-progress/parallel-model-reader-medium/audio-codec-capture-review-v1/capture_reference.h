#ifndef CAPTURE_REFERENCE_H
#define CAPTURE_REFERENCE_H
#include "codec_txn.h"
/* Incomplete, non-executable review artifact. kc_start MUST reject it. */
void k7_capture16_reference(struct kc_plan *plan);
#endif
