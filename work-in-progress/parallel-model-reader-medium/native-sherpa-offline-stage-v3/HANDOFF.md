# Corrected sources and Eigen probe-standard fix

sources/kaldifst is actual recipe1.7.17; sources/openfst is actual recipe sherpa-onnx-2024-06-19. Both v2 archives were rehashed against expected recipe SHA256, then safely extracted:763 regular files, omitted0. All regular source/license bytes retained; source-manifest.json has per-file hashes and archive modes. No existing source stage changed. Root replaces only these two overrides in a NEW independent stage, retaining the other six verified sources from v1. Safely preserve recorded executable modes when transferring Windows files.

Actual root CMakeConfigureLog.yaml now establishes the error stage: both standard_math_library probes fail while compiling <cmath>/libc++ with trailing -std=c++03. The libc++ __config diagnostic requires GCC C++11; the test never reaches a meaningful libm link check. This supersedes v2's unresolved root-cause note. No missing-libm conclusion is justified by that failure.

Minimal new-cache fragment eigen-cache.cmake sets only EIGEN_TEST_CXX11=ON. Eigen3.4.0 CMakeLists.txt:79–89 then chooses its C++11 branch, sets its child-directory CMAKE_CXX_STANDARD11 and appends -std=c++11 instead of03. The sherpa cmake/eigen.cmake helper adds Eigen with add_subdirectory(... EXCLUDE_FROM_ALL), creating a child directory scope. Those normal set(CMAKE_CXX_STANDARD ...) and set(CMAKE_CXX_FLAGS ...) calls have neither CACHE nor PARENT_SCOPE, so they do not downshift the parent Sherpa directory's17 setting or sibling targets. The fragment intentionally does not reset parent flags or target standard. Eigen probe/child targets may use11; this is the supported header baseline indicated by the actual libc++ diagnostic. Root should confirm generated probe compile command and that later sherpa/kaldi commands remain their expected17.

Run a NEW configure build/cache, apply the existing ASR initial cache then eigen-cache.cmake (or pass -DEIGEN_TEST_CXX11=ON). Do not reuse the old failed standard_math cache results. No STANDARD_MATH_LIBRARY_FOUND or standard_math_library_linked_to_* result is preset/FORCEd. This candidate corrects the demonstrated compilation-language mismatch only. If target try_compile is STATIC_LIBRARY, the later result still does not prove sin/log resolution; final real target executable/firmware link must use root's actual libm/runtime closure. No library search workaround is needed to explain the recorded03 compile error.

No configure/build/device action performed here. Script extraction and per-file disk readback hashing are actual host work; the next native probe outcome remains for root. Input log/source hashes frozen in inputs.json.
