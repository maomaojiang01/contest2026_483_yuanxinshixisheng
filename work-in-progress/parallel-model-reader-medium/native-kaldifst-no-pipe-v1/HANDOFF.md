# NuttX explicitly unsupported process pipes

Apply candidate.patch at the corrected kaldifst1.7.17 root. Only kaldi-io.cc changes. Under __NuttX__, PipeInputImpl/PipeOutputImpl definitions are excluded and the existing classified pipe branches return false with a generic warning before allocation. There are no popen/pclose stubs, successful fake handles, or attempts to execute a command. Other platforms retain exactly the original pipe classes/branches. File/offset/stdin/stdout code is unchanged; no global filename classification is altered.

Source evidence: kaldi-io.cc:256–340 and528–610 contain the actual popen/pclose references. Output::Open:403–404 and Input::OpenInternal:788–789 select those implementations. A failed pipe request now leaves impl_ null and IsOpen false. Existing open objects are closed by the original Open prelude before the unsupported new request fails. Direct Open returns false. Filename constructors retain their existing KALDIFST_ERR failure path; log.h:50 throws std::runtime_error. Input::Close on an already closed object still returns0 according to its existing contract; that is not a fabricated successful pipe close. No claim that every higher C API catches such exceptions is made.

Actual ASR model loading: frozen online-paraformer-model.cc:36/41 calls Sherpa ReadFile for encoder/decoder. file-utils.cc:27–30 uses std::ifstream and a byte vector, not kaldifst::Input or a command pipeline. Thus ordinary encoder/decoder paths do not require this unsupported pipe feature. Current ReadFile itself does not explicitly report open/read failure; an empty vector may reach subsequent ORT session construction and trigger an ORT error. This patch does not improve that separate error contract or model-memory allocation behavior. Other recognizer/optional FST paths can pull kaldifst I/O into the archive closure even when Paraformer model bytes are read elsewhere.

Bounded actual Windows host tests compile the changed translation unit plus upstream kaldi-table.cc, parse-options.cc, text-utils.cc at O0 and O2 with __NuttX__ selecting the candidate branch. No fake I/O classes or fake popen functions are linked. Tests cover input/output pipe Open false, closed state, constructor runtime_error, real small ordinary file write/read/close, missing file failure, and existing open file cleanup before rejected pipe reopen. Each build is limited60s, run10s. These are host libstdc++ file tests, not NuttX libc++ or SDK execution. Full translation-unit compilation also succeeded independently. Raw commands/logs and hashes are retained.

Root must recompile the real kaldifst object/archive and repeat actual target archive demand-link audit. Do not alter SDK global feature defines or enable process APIs. Formal source/SDK/device remain untouched here.
