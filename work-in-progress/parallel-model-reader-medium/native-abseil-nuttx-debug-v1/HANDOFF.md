# NuttX ELF-memory / VDSO feature guard

Minimal one-line patch: append !defined(__NuttX__) to absl/debugging/internal/elf_mem_image.h:37's platform eligibility condition. The actual failing command already defines __NuttX__; no new project-wide macro is required. All non-NuttX conditions retain their previous value. Do not define ABSL_HAVE_ELF_MEM_IMAGE=0: the header forbids direct definition, and downstream uses ifdef. Do not undefine __ELF__: the target really uses ELF objects, but that does not promise Linux/glibc in-memory dynamic-loader APIs.

Observed failure: static-deps3 build.log elf_mem_image.cc/vdso_support.cc both reach elf_mem_image.h:43 <link.h>. Header currently treats generic __ELF__ as sufficient after excluding several platforms. NuttX is missing from that exclusion. This candidate does not add a fake link.h or invent dynamic ELF success.

Real upstream feature-disabled paths: elf_mem_image.cc:20–386 implementation is guarded by ABSL_HAVE_ELF_MEM_IMAGE, with no fabricated replacement class. vdso_support.h:47–156 only declares VDSO and defines ABSL_HAVE_VDSO_SUPPORT when ELF image support exists; vdso_support.cc:21–205 similarly omits that implementation when unavailable. ARM64 stacktrace_aarch64-inl.inc:40 initializes signal-trampoline address null; its42+ VDSO lookup block is skipped, retaining the existing no-VDSO path. Thus Linux __kernel_rt_sigreturn lookup is unavailable; no claim of target signal-frame unwinding correctness follows.

Symbolization is independently controlled: internal/symbolize.h:31 requires __GLIBC__ alongside __ELF__. The target must not pretend glibc/Linux. symbolize.cc:42–43 otherwise includes the upstream symbolize_unimplemented.inc: Symbolize returns false, decorator installation returns -1, mapping/decorator queries return false. That is explicit unavailable diagnostics, not empty success supplied by this patch. InitializeSymbolizer's existing no-op is upstream behavior. This one-line patch does not disable all stack unwinding or fix its other target assumptions.

Integration: root applies candidate.patch to its independent Abseil stage, or replaces only absl/debugging/internal/elf_mem_image.h with candidate elf_mem_image.h after checking the input hash. Recompile all consumers of this feature header (at least both failed units, ARM64 stacktrace, and affected dependency targets); do not reuse stale objects with differing feature macros. Final native link and runtime remain root gates. Verify preprocessed target header leaves both ABSL_HAVE_ELF_MEM_IMAGE and ABSL_HAVE_VDSO_SUPPORT undefined, rebuild, then inspect any next errors normally. Other platforms need no build option change.

Official complete relevant files frozen in input/ with hashes, original provenance is parent's verified Abseil20240116 source. No native compile, host fake build, SDK/device operation, or previous candidate modification performed here. Candidate is a narrow platform capability correction, not ASR/TTS readiness.
