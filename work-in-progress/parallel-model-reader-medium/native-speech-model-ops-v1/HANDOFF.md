# Speech model operator inventory: blocked on reliable parser

No operator inventory was produced. Both available/located Python interpreters (Codex runtime and C:/Program Files/Python36/python.exe) returned onnx=None and google=None in actual bounded import probes. Local model/WIP search did not locate an existing importable ONNX parser. No dependency was downloaded or installed, no inference performed, and no strings/grep or guessed protobuf decoder was used as a substitute.

model-inputs.json records freshly streamed SHA256/length of the real Paraformer encoder.int8.onnx, decoder.int8.onnx and VITS model.onnx, all matching the prior native-voice-runtime-audit-v1 hashes. The model files were not copied. Operator/opset/type/external-data fields are null and StringNormalizer is explicitly UNKNOWN_NOT_PARSED; neither presence nor absence can be claimed from this audit.

Minimum unblock: root supplies an existing trusted ONNX Python+protobuf environment, or separately authorizes acquiring a pinned official parser distribution. Parse with onnx.load_model(path, load_external_data=False) so the inventory does not implicitly follow external weight paths. Recursively inspect model.graph, GRAPH/GRAPHS attributes, model.functions and their opset imports; collect domain/op_type counts, graph inputs/outputs/value_info tensor/sequence/optional/map types and initializer data_type/dims. Collect TensorProto external_data and data_location from graph initializers, sparse initializers and tensor/tensors attributes without opening referenced files. Preserve raw declared external locations as metadata and validate path scope separately before any later load.

Declared type metadata is not guaranteed to annotate every intermediate. Report unknown values honestly; do not call shape inference or infer exact kernel type closure without an independently reviewed step. Then use the actual complete model operator list to generate the reduced-op config, keeping all model/subgraph/function domains and opset versions. Check StringNormalizer explicitly from parsed nodes/functions. Tokens/FAR/lexicon are not ONNX graph operators and need separate runtime resource handling. This result cannot justify either enabling or removing Iconv for real speech models.

Reproduce hash/environment probes with python -B probe.py (two subprocess timeouts15s, bounded1MiB read chunks; no model execution). This delivery's completed scope is honest blocker evidence and fixed model identity, not full speech operator readiness.
