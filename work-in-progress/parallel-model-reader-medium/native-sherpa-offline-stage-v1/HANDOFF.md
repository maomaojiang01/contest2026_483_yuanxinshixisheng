# Offline Sherpa ASR source stage and NuttX ORT import

Eight archives were rehashed against frozen v1/v2 accepted SHA256 and safely expanded to this directory's sources/. 2843 regular files, zero skipped links/nonregular entries; extraction.json records all roots and source-manifest.json every file SHA256/size/archive mode. Original full regular source/license contents are preserved. No source CMake/script was executed. Extraction rejects absolute/parent/backslash/colon paths, case-fold duplicates and out-of-root destinations, excludes links/devices, and limits file/archive expansion. A rerun refuses existing target source directories. Root should preserve manifest modes when transferring from Windows.

## Minimal candidate use

candidate.patch changes sherpa cmake/onnxruntime.cmake only for actual NuttX: include the new onnxruntime-nuttx.cmake and return before any preinstalled search/package download. Other platforms retain upstream code. Complete replacement files also supplied. Git apply --check and application passed against a separate exact-input copy, and resulting bytes match candidate files (patch-check.json). No CMake configure/build performed.

Import parameters: K7_ORT_PUBLIC_INCLUDE_DIR points to the real ORT1.17.1 public session header directory; K7_ORT_STATIC_LIBRARIES is a semicolon-separated ordered list of absolute real target .a files representing the full required ORT/dependency closure. The helper requires API17 declaration and the three main public headers, checks every path is a non-directory .a and a nonempty regular ar archive with actual ar magic BEFORE creating any target. Thin archives rejected because member paths would require additional lifetime/provenance checking. It then creates STATIC IMPORTED targets and a nonempty onnxruntime INTERFACE dependency wrapper. No fake library/empty success target is supplied. Public headers' exact1.17.1 provenance must still be checked by caller; API17 alone is necessary, not sufficient version identity.

Archive magic is a format guard, not architecture or symbol-closure proof: caller must provide hashes and verify ARM64 members/symbols with the real toolchain. A nonempty archive lacking required symbols fails final linking, not magically passes here. Preserve normal demand extraction and correct link order; root can group the complete imported closure at final application linkage if necessary. Real NuttX runtime/math/thread libraries belong to the actual application/toolchain; no fake libpthread/libm or forced probe success is created.

asr-initial-cache.cmake references ${K7_SHERPA_DEPS_ROOT}/<dependency-name>. Pass -DK7_SHERPA_DEPS_ROOT=<new-stage>/sources BEFORE -C <candidate>/asr-initial-cache.cmake so it is defined while the cache script executes. Then pass actual NuttX toolchain and real K7_ORT_* parameters. It retains ASR-only options from prior map, disables nested sentencepiece Python/tests and optional host ICU/ZLIB discovery, and maps all eight exact FetchContent names (including SIMPLE-SENTENCEPIECE hyphen). Use a fresh Sherpa build/cache independent from ORT so Eigen3.4.0 does not overwrite ORT's private Eigen tree.

No dependency/source modifications beyond patch-check copies were made. sources/ are pristine archive bytes. Source overrides skip upstream population checks; root verifies source-manifest again and keeps licenses. Actual toolchain/compiler/target CMake compatibility, recursive no-network behavior and final symbol/ODR closure remain pending. No target build or ASR execution claimed.
