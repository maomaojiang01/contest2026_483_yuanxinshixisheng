# USB存储只读首轮诊断

显式命令start/list/read，不自动启动摄像头、格式化、挂载或写入。只接受实际已枚举的/dev/sda至sdz；由主会话对照新增节点及USB枚举身份，不能预先假定盘符。

read要求驱动geometry只读，最多两次读取首扇区（512至4096字节），检查重复一致并打印CRC及引导签名，不输出文件内容。返回成功仅证明该有限读取，不证明完整文件、FAT或全盘正常。

必须与checked只读MSC候选及注册时序一起构建；该应用本身不修复驱动传输。阻塞USB传输仍需外部观察期限，不能强杀任务后释放DMA内存。首次失败保留证据并恢复已验收镜像。
