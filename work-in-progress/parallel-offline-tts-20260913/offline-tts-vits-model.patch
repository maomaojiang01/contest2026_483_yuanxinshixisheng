diff --git "a/E:\\openvela\\VelaVision\\work-in-progress\\native-voice-sources\\sherpa-onnx-26aa2fa93210376a89de3a65a1a4dd320c37f5e9\\sherpa-onnx\\csrc\\offline-tts-vits-model.cc" "b/E:\\openvela\\VelaVision\\work-in-progress\\parallel-offline-tts-20260913\\offline-tts-vits-model.cc"
index 1aee99b..18c3b07 100644
--- "a/E:\\openvela\\VelaVision\\work-in-progress\\native-voice-sources\\sherpa-onnx-26aa2fa93210376a89de3a65a1a4dd320c37f5e9\\sherpa-onnx\\csrc\\offline-tts-vits-model.cc"
+++ "b/E:\\openvela\\VelaVision\\work-in-progress\\parallel-offline-tts-20260913\\offline-tts-vits-model.cc"
@@ -3,6 +3,9 @@
 // Copyright (c)  2023  Xiaomi Corporation
 
 #include "sherpa-onnx/csrc/offline-tts-vits-model.h"
+#ifdef K7_TTS_NATIVE_STORAGE
+#include "native_tts_model_storage.hpp"
+#endif
 
 #include <algorithm>
 #include <string>
@@ -22,9 +25,6 @@
 #include "sherpa-onnx/csrc/macros.h"
 #include "sherpa-onnx/csrc/onnx-utils.h"
 #include "sherpa-onnx/csrc/session.h"
-#ifdef K7_TTS_NATIVE_STORAGE
-#include "native_tts_storage.hpp"
-#endif
 
 namespace sherpa_onnx {
 
@@ -32,16 +32,12 @@ class OfflineTtsVitsModel::Impl {
  public:
   explicit Impl(const OfflineTtsModelConfig &config)
       : config_(config),
-#ifdef K7_TTS_NATIVE_STORAGE
-        native_storage_(),
-#endif
         env_(ORT_LOGGING_LEVEL_ERROR),
         sess_opts_(GetSessionOptions(config)),
         allocator_{} {
 #ifdef K7_TTS_NATIVE_STORAGE
-    native_storage_.initialize(env_, sess_opts_);
-    Init(const_cast<void *>(native_storage_.model_data()),
-         native_storage_.model_size());
+    native_storage_.initialize(env_, sess_opts_, config.vits.model.c_str());
+    Init(native_storage_.model_bytes, native_storage_.expected_model_size);
 #else
     auto buf = ReadFile(config.vits.model);
     Init(buf.data(), buf.size());
@@ -344,7 +340,7 @@ class OfflineTtsVitsModel::Impl {
  private:
   OfflineTtsModelConfig config_;
 #ifdef K7_TTS_NATIVE_STORAGE
-  // Declared before Env so Env and Session are destroyed before the allocator.
+  // Must outlive Env and Session; declaration order is intentional.
   k7_tts::ModelStorage native_storage_;
 #endif
   Ort::Env env_;
